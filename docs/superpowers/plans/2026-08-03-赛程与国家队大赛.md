# 赛程系统 / 属性映射 / 国家队大赛 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给游戏加上真正的赛季赛程（主界面下一场、日程页、赛前预告），清掉一批只写不读的死属性，并把国家队大赛从「一屏出结果」改成可参与的世界杯 + 亚洲杯。

**Architecture:** 核心是新增 `s.schedule` —— 把「这个月打谁」从月末现抽改成赛季初排定。这一个数据结构同时喂饱主界面、日程页、赛前预告和大赛日历。世界杯与亚洲杯结构同构，共用一套参数化的赛事引擎（`cupRun` + 配置表），关键时刻的比分并入规则从 `finishMatch` 抽成 `resolveMoments` 供两边共用。

**Tech Stack:** 纯 ES5+ 浏览器 JS，无框架无构建。`app.js`（单文件，约1759行）+ `index.html` + `style.css`。测试是 `node:test` 之外的裸断言脚本，用 `node:vm` 把 `app.js` 灌进沙箱跑。

**Spec:** [docs/superpowers/specs/2026-08-03-赛程与世界杯参与感-design.md](../specs/2026-08-03-赛程与世界杯参与感-design.md)

---

## 读这份计划之前必须知道的

**跑测试：**
```bash
cd /Users/ruis/Documents/模拟/模拟球员
node --check app.js && node tests/structure.test.mjs
```
`tests/structure.test.mjs` 不是测试框架，是一串**顶层裸断言**，从上往下顺序执行，任何一条 `assert` 失败就整个脚本抛错退出。所以：

- 新测试**追加到文件末尾**即可，前面的变量（`G`、`allocation`、`ATTR_KEYS`、`code`、`html`、`css`）都还在作用域里
- `code` = `app.js` 源码文本（`tests/structure.test.mjs:9`），`html`（:824）、`css`（:859）同理。**注意 `html`/`css` 是在第 824/859 行才定义的，写在它们之前的测试用不了**
- 通过时脚本静默退出，没有 "N tests passed" 之类的输出。**"没有输出" 就是 PASS**

**引擎导出：** `app.js` 末尾把一批函数挂到 `window.PlayerLife`，测试里叫 `G`。想在测试里调某个函数，它必须在那个导出对象里。本计划多处会往里加。

**无头模式的关键行为：** `pumpModal` 开头是 `if(modalBusy||!modalQueue.length||typeof document==="undefined")return`。测试环境没有 `document`，所以**弹窗只进队列、不消费**。要推进流程必须自己把队列跑完——`tests/structure.test.mjs:721` 已经有现成的 `driveModals(t,pick,cap)` 干这件事。

**提交规范：** 中文 commit message，说清「为什么」。仓库现有 commit 全是中文，跟上。

---

## 文件结构

本次改动**不新建源文件**。理由写在 spec 的「不做」一节：`build_single.py` 只内联单个 `app.js`，而 README 承诺双击 `index.html` 就能玩，`file://` 下 ES module 会被 CORS 拦掉。拆分要连构建带加载方式一起改，不属于本次范围。

| 文件 | 职责 | 本次改动 |
|---|---|---|
| `app.js` | 全部游戏逻辑与渲染 | 主战场。新增赛程模块、赛事引擎参数化、属性映射 |
| `index.html` | DOM 骨架 | `now-strip` 加下一场、侧栏加家庭条 |
| `style.css` | 全部样式 | 下一场信息、日程页行、家庭条配色 |
| `tests/structure.test.mjs` | 裸断言测试 | 追加新断言；改名波及处同步更新 |
| `tests/calibrate-route16.mjs` | **新建**，一次性标定脚本 | 量出16岁分流后门的 fame 阈值 |
| `模拟球员-单文件版.html` | 分发产物 | 最后一步 `python3 build_single.py` 重建 |

`app.js` 内的新代码按功能就近放置：赛程模块放在 `shouldPlayMatch`（现 :1351）附近，赛事引擎改造在原世界杯段（现 :1361-1585）原地进行。

---

## 任务依赖

```
A 赛程系统        T1 → T2 → T3 → T4 → T5 → T6
B 属性映射        T7 → T8 ,  T9 , T10 , T11        （与 A 独立，可并行）
C 国家队大赛      T12 , T13 → T14 → T15* → T16* → T17
D 收尾            T18 → T19

* T15/T16 依赖 A 的 T1/T2（赛程表）
```

---

# A 段 · 赛程系统

### Task 1: 赛程数据结构与生成

**Files:**
- Modify: `app.js`（在 `shouldPlayMatch`，现 :1351 之前插入新段）
- Modify: `app.js:1753`（`API` 导出）
- Test: `tests/structure.test.mjs`（追加到末尾）

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾：

```js
// ===== 赛程表：对手必须提前排定 =====
// 改动前对手是月末 pick(opponentPool(s)) 现抽的，「下一场打谁」这个信息
// 在系统里根本不存在。主界面、日程页、赛前预告全部卡在这一点上。
{
  const t=G.createInitialState("赛程",allocation,[],"standard","mid");
  const sc=G.ensureSchedule(t);
  assert.ok(sc&&Array.isArray(sc.fixtures),"ensureSchedule 返回带 fixtures 的赛程");
  assert.ok(sc.fixtures.length>0,"新档第一赛季就该有比赛");
  // 14岁梯队每3个月一场：第1赛季应落在 totalMonth 0,3,6,9
  assert.deepEqual(sc.fixtures.filter(f=>f.type==="club").map(f=>f.month),[0,3,6,9],
    "梯队期每3个月一场，月份节奏必须与旧 shouldPlayMatch 一致");
  sc.fixtures.forEach(f=>{
    assert.ok(typeof f.opponent==="string"&&f.opponent.length>0,"每场都有对手名");
    assert.ok(Number.isFinite(f.strength),"每场都有对手实力");
    assert.equal(typeof f.home,"boolean","每场都有主客");
    assert.equal(f.status,"upcoming","新排的赛程都是未开始");
    assert.equal(f.result,null,"未开始的比赛没有结果");
  });
  // 幂等：签名没变就不该重排
  const again=G.ensureSchedule(t);
  assert.equal(again,sc,"签名未变时 ensureSchedule 必须返回同一个对象，不能每次重排");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`TypeError: G.ensureSchedule is not a function`

- [ ] **Step 3: 实现赛程模块**

在 `app.js` 的 `function shouldPlayMatch(s){...}`（现 :1351）**之前**插入：

```js
/* ========== 赛程表 ==========
   对手必须在赛季初就定下来，否则「下一场打谁」这个信息不存在，
   主界面、日程页、赛前预告全都无从谈起。
   月份节奏在这里是唯一定义，shouldPlayMatch 改成查表。 */
function shuffled(arr,rng){const c=[...arr];for(let i=c.length-1;i>0;i--){const j=Math.floor(rng()*(i+1));[c[i],c[j]]=[c[j],c[i]]}return c}
function matchMonthsOfSeason(s,seasonStart){
  const out=[];
  for(let m=seasonStart;m<seasonStart+12;m++){
    const age=14+Math.floor(m/12),p=age<16?"academy":age<18?(s.route||"academy"):"pro";
    if(age<16){if(m%3===0)out.push(m)}
    else if(p==="campus"){if(m%2===0)out.push(m)}
    else out.push(m);
  }
  return out;
}
function scheduleSig(s){return{season:ageInfo(s).season,clubKey:s.club.name,route:s.route||"",nationalCalled:!!s.national.called}}
function buildSchedule(s,rng=Math.random){
  const seasonStart=Math.floor(s.totalMonth/12)*12,pool=opponentPool(s);
  let bag=[];
  const fixtures=matchMonthsOfSeason(s,seasonStart).map((month,i)=>{
    if(!bag.length)bag=shuffled(pool,rng);
    const opp=bag.shift()||pick(pool);
    return {month,type:"club",opponent:opp.name,strength:opp.strength,home:i%2===0,
      competition:`${s.club.league}第${i+1}轮`,status:"upcoming",result:null};
  });
  return {...scheduleSig(s),fixtures};
}
/* 只重排未来。转会发生在赛季中途时，已打过的战绩必须留住——
   否则换一次东家就抹掉半个赛季的比分。 */
function ensureSchedule(s,rng=Math.random){
  const sig=scheduleSig(s),sc=s.schedule;
  if(sc&&sc.season===sig.season&&sc.clubKey===sig.clubKey&&sc.route===sig.route&&sc.nationalCalled===sig.nationalCalled)return sc;
  const fresh=buildSchedule(s,rng);
  if(sc){
    const past=sc.fixtures.filter(f=>f.status!=="upcoming"||f.month<s.totalMonth);
    const taken=new Set(past.map(f=>f.month));
    fresh.fixtures=[...past,...fresh.fixtures.filter(f=>!taken.has(f.month))].sort((a,b)=>a.month-b.month);
  }
  s.schedule=fresh;return fresh;
}
function fixtureOfMonth(s){const sc=s.schedule;return sc?sc.fixtures.find(f=>f.month===s.totalMonth&&f.status==="upcoming")||null:null}
function nextFixture(s){const sc=s.schedule;return sc?sc.fixtures.find(f=>f.month>=s.totalMonth&&f.status==="upcoming")||null:null}
```

- [ ] **Step 4: 加进 API 导出**

在 `app.js:1753` 的 `const API={...}` 里，紧跟 `prepareMatch,` 之后加：

```js
ensureSchedule,buildSchedule,fixtureOfMonth,nextFixture,shouldPlayMatch,
```

（`shouldPlayMatch` 现在没有被导出，Task 2 的测试需要它。）

- [ ] **Step 5: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS（无输出）

- [ ] **Step 6: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "赛程表：对手改为赛季初排定

月末现抽对手导致「下一场打谁」这个信息在系统里根本不存在，
主界面下一场、日程页、赛前预告三件事全部卡在这里。
月份节奏（梯队每3月/校园每2月/职业每月）从此只在
matchMonthsOfSeason 一处定义。
ensureSchedule 只重排未来，赛季中途转会不会抹掉已打战绩。"
```

---

### Task 2: `shouldPlayMatch` 改查赛程，比赛接上 fixture

**Files:**
- Modify: `app.js:1351`（`shouldPlayMatch`）
- Modify: `app.js:1586-1612`（`advanceMonth`）
- Modify: `app.js:1614`（`startMatchFlow`）
- Modify: `app.js:1662`（`resolveMatch`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾：

```js
// ===== 比赛必须打赛程表上那一场，而不是另抽一个对手 =====
{
  const t=G.createInitialState("对上表",allocation,[],"standard","mid");
  t.totalMonth=48;t.flags.pro18=true;t.route="pro";        // 18岁职业期，每月一场
  t.club={name:"重庆铜梁龙",league:"中超",strength:67};
  G.ensureSchedule(t);
  const fx=G.fixtureOfMonth(t);
  assert.ok(fx,"职业期每个月都该有比赛");
  assert.equal(G.shouldPlayMatch(t),true,"有 fixture 就该踢");

  // 打完之后这一场要被标记，且结果写回赛程表
  G.setState(t);G.clearModalQueue();
  G.advanceMonth();
  driveModals(t,()=>0);
  const done=t.schedule.fixtures.find(f=>f.month===48);
  assert.equal(done.status,"played",`第48月那场应标记 played，实际 ${done.status}`);
  assert.ok(done.result&&Number.isFinite(done.result.gf),"结果必须写回赛程表");
}

// 伤停时不排比赛，且那一场在日程上留痕而不是凭空消失
{
  const t=G.createInitialState("伤停",allocation,[],"standard","mid");
  t.totalMonth=48;t.flags.pro18=true;t.route="pro";
  t.club={name:"重庆铜梁龙",league:"中超",strength:67};
  G.ensureSchedule(t);
  t.injury={name:"脚踝扭伤",months:2,risk:0};
  assert.equal(G.shouldPlayMatch(t),false,"伤停不上场");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`第48月那场应标记 played，实际 upcoming`

- [ ] **Step 3: 改 `shouldPlayMatch` 查表**

把 `app.js:1351` 整行替换为：

```js
function shouldPlayMatch(s){if(s.flags&&s.flags.washedOut)return false;if((s.injury.months||0)>0)return false;return !!fixtureOfMonth(s)}
```

- [ ] **Step 4: `advanceMonth` 里排上赛程**

在 `app.js` 的 `advanceMonth` 中，找到这一行（现 :1594 附近，紧跟 `S.peakOverall=...` 那句之后不远）：

```js
  const _ctx={snap:_snap,preSusp,preInjury,justTurned16,matchReportModal:null};
```

在它**之前**插入一行：

```js
  ensureSchedule(S);
```

必须在 `S.totalMonth++` 之后、`shouldPlayMatch` 之前——赛季翻页时要先把新赛季排出来。

- [ ] **Step 5: `startMatchFlow` 用 fixture 的对手**

把 `app.js:1614-1620` 的 `startMatchFlow` 替换为：

```js
/* ========== 比赛流程：赛前预告 → 2个关键时刻 → 结算 → 交回月末段 ========== */
function startMatchFlow(s,ctx){
  const fx=fixtureOfMonth(s);
  if(!fx){finishMonth(ctx);return}
  s.pendingMatch={stage:"preview",fixture:fx,ctx,index:0,pending:null};
  modalBusy=false;
  stepMatchPreview(s);
}
```

（`stepMatchPreview` 在 Task 4 实现。为了让本任务可独立通过，先在紧邻处放一个直通版本：）

```js
function stepMatchPreview(s){beginMatch(s,s.matchPlan||"box")}
function beginMatch(s,plan){
  const pm=s.pendingMatch,fx=pm.fixture;
  s.matchPlan=plan;
  pm.pending=prepareMatch(s,Math.random,{opponent:{name:fx.opponent,strength:fx.strength},
    home:fx.home,plan,competition:fx.competition});
  pm.stage="moments";
  if(!pm.pending.plays){resolveMatch(s);return}
  stepKeyMoment(s);
}
```

- [ ] **Step 6: `resolveMatch` 把结果写回赛程表**

在 `app.js:1662` 的 `resolveMatch` 里，找到 `applyMatch(s,report);` 这一行，在它**之后**插入：

```js
  if(pm.fixture){pm.fixture.status="played";
    pm.fixture.result={gf:report.gf,ga:report.ga,goals:report.goals,assists:report.assists,rating:report.rating}}
```

注意 `const ctx=pm.ctx,report=finishMatch(s,pm.pending);` 之后紧跟着 `s.pendingMatch=null;`，所以 `pm` 的引用要在置空**之前**取好——上面那段插在 `applyMatch` 后、`pm` 仍然指向原对象，没问题。

- [ ] **Step 7: 恢复入口跟上新的 stage 字段**

把 `app.js:1639-1643` 的 `resumeMatchFlow` 替换为：

```js
// 中断恢复：pumpModal 每次点击都存档，所以刷新页面必须能回到同一步。
function resumeMatchFlow(s){
  if(!s.pendingMatch)return false;
  modalBusy=false;
  if(s.pendingMatch.stage==="preview"||!s.pendingMatch.pending){stepMatchPreview(s);return true}
  if(!s.pendingMatch.pending.plays){resolveMatch(s);return true}
  stepKeyMoment(s);return true;
}
```

- [ ] **Step 8: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS

- [ ] **Step 9: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "比赛接上赛程表：打表上那一场，结果写回表里

shouldPlayMatch 从「算月份」改成「查表」，月份节奏不再有第二处定义。
比赛结果回写 fixture.status/result，日程页才能显示已打场次的比分。
pendingMatch 加 stage 字段，为赛前预告这一步的中断恢复留位置。"
```

---

### Task 3: 月度小结移到队尾并改惰性求值

**Files:**
- Modify: `app.js:1305`（`pumpModal`）
- Modify: `app.js:1626-1627`（`finishMonth` 末尾）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾：

```js
// ===== 月度小结必须排在本月事件之后，并反映事件造成的变化 =====
// 改动前：finishMonth 用 unshift 把小结插到队首，本月事件全排在它后面；
// 而且小结 body 是拼死的字符串，事件还没被点开，变化根本进不去。
{
  // 惰性 body：pumpModal 必须支持 body 是函数
  assert.ok(/typeof d\.body\s*===\s*"function"/.test(code),
    "pumpModal 必须支持惰性 body，否则小结拿不到本月事件造成的属性变化");
  // 队尾：finishMonth 不能再把月度小结 unshift 到队首
  assert.ok(!/modalQueue\.unshift\(\{title:`\$\{a2\.age\}岁/.test(code),
    "月度小结不能再 unshift 到队首");
}
{
  const t=G.createInitialState("小结顺序",allocation,[],"standard","mid");
  t.totalMonth=48;t.flags.pro18=true;t.route="pro";
  t.club={name:"重庆铜梁龙",league:"中超",strength:67};
  t.totalMonth=49;                                   // 奇数月避开两月事件，先跑通顺序
  G.ensureSchedule(t);G.setState(t);G.clearModalQueue();
  G.advanceMonth();
  const titles=[];const q=G.getModalQueue();
  // 不消费，直接看排队顺序里小结在不在最后
  for(const m of q)titles.push(m.title);
  const idx=titles.findIndex(x=>/月度小结/.test(x));
  assert.ok(idx>=0,`队列里应有月度小结，实际：${titles.join(" / ")}`);
  assert.equal(idx,titles.length-1,
    `月度小结必须排在最后，实际排第 ${idx+1}/${titles.length}：${titles.join(" / ")}`);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`pumpModal 必须支持惰性 body`

- [ ] **Step 3: `pumpModal` 支持惰性 body**

在 `app.js:1305`，把：

```js
$("modalBody").innerHTML=d.body||"";
```

替换为：

```js
$("modalBody").innerHTML=(typeof d.body==="function"?d.body(S):d.body)||"";
```

`options` 早就是这么干的（同一行下面的 `typeof d.options==="function"?d.options(S):d.options`），照抄它的做法。

- [ ] **Step 4: 小结改 push 到队尾 + body 改函数**

在 `app.js:1626`，把整个 `{const a2=ageInfo(S),rows=[],dd=...}` 块替换为：

```js
  /* 月度小结排到队尾：它是这个月的总账，必须在本月所有事件都点完之后才结算。
     body 用函数惰性求值——拼成字符串就等于在事件生效前抢跑，
     那些事件造成的属性变化永远进不了这张表。 */
  {const snapAtQueue=_snap;
   modalQueue.push({title:`${ageInfo(S).age}岁 · 第${ageInfo(S).month}月 · 月度小结`,kicker:"月度小结",
    body:()=>{
      const a2=ageInfo(S),rows=[],dd=(l,b,af,u="")=>{const v=Math.round((af-b)*10)/10;if(v)rows.push(`<div class="ms-row"><span>${l}</span><b style="color:${v>0?'var(--green)':'var(--bad)'}">${v>0?'+':''}${v}${u}</b></div>`)};
      dd("综合能力",snapAtQueue.ov,overall(S));dd("体能",snapAtQueue.fit,S.fitness);dd("状态",snapAtQueue.form,S.form);
      if(["恋人","异地"].includes(S.relationship.status))dd("小满关系",snapAtQueue.love,S.relationship.love);
      dd("家庭",snapAtQueue.family,S.family);dd("资金",snapAtQueue.money,S.money||0,"万");
      const extra=`${a2.age>snapAtQueue.age?`<p>🎂 你满 ${a2.age} 岁了。</p>`:""}${(S.injury.months||0)>snapAtQueue.inj?`<p style="color:var(--bad)">🩹 ${esc(S.injury.name)}，预计伤停 ${S.injury.months} 个月。</p>`:""}${snapAtQueue.st!=="分手"&&S.relationship.status==="分手"?`<p style="color:var(--bad)">💔 你和小满走散了。</p>`:""}`;
      return `${extra}<div class="month-summary">${rows.join("")||'<div class="ms-row"><span>这个月平平淡淡</span></div>'}</div>`;
    },
    options:[option("确认，进入下月","",()=>{
      S.monthSnap={ov:overall(S),fit:S.fitness,form:S.form,love:S.relationship.love,
        family:S.family,money:S.money||0,age:ageInfo(S).age,inj:S.injury.months||0,st:S.relationship.status};
    })]});}
```

关键差异：`monthSnap` 的重置从「入队时」挪到了「玩家点确认时」。因为小结现在排在队尾，入队时重置会让快照错开一个月。

- [ ] **Step 5: 快照补上 family 字段**

`advanceMonth` 里 `_snap` 的兜底（现 :1588）要加 `family`：

```js
  const _snap=S.monthSnap||{ov:overall(S),fit:S.fitness,form:S.form,love:S.relationship.love,family:S.family,money:S.money||0,age:ageInfo(S).age,inj:S.injury.months||0,st:S.relationship.status};
```

（家庭那一行的显示要等 Task 11 才有意义，但快照字段现在就补齐，免得 Task 11 再回头改这里。）

- [ ] **Step 6: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "月度小结移到队尾并改惰性求值

原来 unshift 到队首，本月事件全排在小结后面——顺序本身就是错的。
更要命的是 body 在 finishMonth 时就拼成字符串，那时事件还没被点开，
事件造成的属性变化根本进不了这张表，小结长期在少报。
body 改成函数，pumpModal 照 options 已有的做法惰性求值；
monthSnap 的重置从入队时挪到玩家点确认时，否则快照会错开一个月。"
```

---

我先把 A 段前三个任务写实了。接下来 Task 4–6（赛前预告 / 主界面下一场 / 日程页）继续。

---

### Task 4: 赛前预告 = 本场职责三选一

**Files:**
- Modify: `app.js`（`stepMatchPreview`，Task 2 里放的直通版）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾：

```js
// ===== 赛前预告：看完对手再决定怎么踢 =====
// 「本场职责」原本是行动页上一个常驻设置，很容易忘了改。
// 挪到赛前这一屏当三选一，相比改动前不多一次点击。
{
  const t=G.createInitialState("预告",allocation,[],"standard","mid");
  t.totalMonth=48;t.flags.pro18=true;t.route="pro";
  t.club={name:"重庆铜梁龙",league:"中超",strength:67};
  G.ensureSchedule(t);
  const fx=G.fixtureOfMonth(t);
  G.setState(t);G.clearModalQueue();
  G.advanceMonth();
  const q=G.getModalQueue();
  const preview=q[0];
  assert.ok(preview,"结束本月后队列里第一屏应该是赛前预告");
  assert.ok(new RegExp(fx.opponent.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")).test(preview.title+String(preview.body)),
    `预告必须写明对手 ${fx.opponent}，实际标题「${preview.title}」`);
  const opts=typeof preview.options==="function"?preview.options(t):preview.options;
  assert.equal(opts.length,G.MATCH_PLANS.length,
    `预告的选项就是本场职责三选一，应有 ${G.MATCH_PLANS.length} 个，实际 ${opts.length}`);
  // 选第二个职责，比赛必须真的按它打
  const plan2=G.MATCH_PLANS[1].id;
  opts[1].apply();
  assert.equal(t.matchPlan,plan2,"选了哪个职责就该写进 matchPlan");
  assert.equal(t.pendingMatch&&t.pendingMatch.pending&&t.pendingMatch.pending.plan,plan2,
    "prepareMatch 必须拿到预告里选的职责，而不是行动页那个旧默认值");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`预告的选项就是本场职责三选一，应有 3 个，实际 1`（直通版没有出预告屏，队首是别的东西）

- [ ] **Step 3: 实现预告屏**

把 Task 2 里那个直通版 `function stepMatchPreview(s){beginMatch(s,s.matchPlan||"box")}` 替换为：

```js
/* 赛前预告：先看清对手，再决定怎么踢。
   三个选项就是本场职责——把行动页那个容易被忘掉的常驻设置搬到
   它真正起作用的时刻，且不比改动前多一次点击。 */
function startChance(s,opts={}){
  const club=opts.club||currentClub(s);
  return clamp(.42+(effOverall(s)-club.strength)/50+(s.coachFavor-50)/170+(hasTalent(s,"super_sub")?-.04:0),.15,.9);
}
function stepMatchPreview(s){
  const pm=s.pendingMatch,fx=pm.fixture,club=currentClub(s);
  const edge=club.strength-fx.strength,st=Math.round(startChance(s)*100);
  const edgeText=edge>=6?"纸面占优":edge<=-6?"纸面下风":"势均力敌";
  enqueueFront({title:`${fx.competition} · ${fx.home?"主场":"客场"}对阵 ${esc(fx.opponent)}`,kicker:"赛前",
    body:`<div class="match-score"><span class="match-team">${esc(club.name)}</span><strong>${club.strength} : ${fx.strength}</strong><span class="match-team">${esc(fx.opponent)}</span></div>`+
      `<p>${edgeText}。你的体能 <b>${Math.round(s.fitness)}</b>、状态 <b>${Math.round(s.form)}</b>，预计首发概率约 <b>${st}%</b>。</p>`+
      (s.seasonGoal?`<p class="dialogue">赛季目标：${esc(goalProgressText(s))}</p>`:"")+
      (s.challenge?`<p class="dialogue">教练挑战：${esc(s.challenge.text)}（${esc(challengeProgressText(s.challenge))}，剩余${Math.max(0,3-s.challenge.played)}场）</p>`:"")+
      `<p>本场你打算怎么踢？</p>`,
    options:MATCH_PLANS.map(p=>option(`${p.icon} ${p.name}`,p.effects.join(" · "),()=>beginMatch(s,p.id)))},"赛前");
}
```

`beginMatch` 保持 Task 2 里的实现不变。

- [ ] **Step 4: 行动页的职责设置降为默认值**

`renderActions`（现 :1706）里那段「本场职责」的说明文字要改口，否则会和预告屏冲突。把该段的 `<span>常驻设置 · 不消耗执行点</span>` 改为：

```js
<span>默认职责 · 赛前可改</span>
```

- [ ] **Step 5: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "赛前预告：看清对手再决定怎么踢

三个选项就是本场职责，把行动页那个常驻设置搬到它真正起作用的时刻。
相比改动前不多一次点击——原来直接进关键时刻，现在这一屏替代了
「你根本不知道在跟谁踢」的空白。
首发概率公式抽成 startChance()，预告屏和 prepareMatch 共用一份，
不在两处各抄一遍。"
```

---

### Task 5: 主界面显示下一场

**Files:**
- Modify: `index.html:130`（`now-strip`）
- Modify: `app.js`（`renderAll`，现 :1700 附近）
- Modify: `style.css`
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾：

```js
// ===== 主界面必须显示下一场的对手与时间 =====
{
  assert.ok(/id="nextMatch"/.test(html),"now-strip 里要有下一场信息的挂载点");
  assert.ok(/nextMatchText|nextMatch/.test(code),"renderAll 要填充下一场信息");
  // 移动端把 .now-strip span 全隐了，下一场不能挂在裸 span 上被一起藏掉
  assert.ok(!/id="nextMatch"[^>]*>\s*<\/span>/.test(html)||/\.now-strip\s+#nextMatch/.test(css),
    "移动端 .now-strip span:not(.live-dot){display:none} 会藏掉裸 span，下一场需要自己的样式规则");
}
{
  const t=G.createInitialState("下一场",allocation,[],"standard","mid");
  G.ensureSchedule(t);
  const nf=G.nextFixture(t);
  assert.ok(nf,"新档就该有下一场");
  assert.equal(nf.month,0,"第一场在第0月");
  // 跳到第1月：下一场应该是第3月那一场，还有2个月
  t.totalMonth=1;
  const nf2=G.nextFixture(t);
  assert.equal(nf2.month,3,"第1月时下一场是第3月");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`now-strip 里要有下一场信息的挂载点`

- [ ] **Step 3: 改 `index.html`**

把 `index.html:129-132` 的 `now-strip` 块替换为：

```html
        <div class="now-strip">
          <div><span class="live-dot"></span><b id="phaseTitle">梯队成长期</b><span id="phaseHint">每月3个执行点，下一次抉择在2个月后</span></div>
          <div class="next-match" id="nextMatch"></div>
          <button id="endMonthBtn" class="month-button" type="button">结束本月</button>
        </div>
```

- [ ] **Step 4: 加样式**

追加到 `style.css` 末尾：

```css
/* 主界面下一场：移动端 .now-strip span 被整体隐藏，这里必须自己撑住 */
.next-match{display:flex;align-items:center;gap:6px;font-size:11px;color:#c5d2cb;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;min-width:0}
.next-match b{color:var(--gold);font-size:11px}
.next-match .nm-when{color:var(--muted);font-size:10px}
@media (max-width:760px){.now-strip .next-match{display:flex;font-size:10px}}
```

- [ ] **Step 5: `renderAll` 填充**

在 `app.js` 的 `renderAll` 里，找到 `$("phaseHint").textContent=...` 这一行，在它**之后**插入：

```js
  {const el=$("nextMatch");if(el){ensureSchedule(S);const nf=nextFixture(S);
    if(S.injury.months>0)el.innerHTML=`<span class="nm-when">伤停中 · 预计 ${S.injury.months} 个月后复出</span>`;
    else if(!nf)el.innerHTML=`<span class="nm-when">本赛季赛程已打完</span>`;
    else{const gap=nf.month-S.totalMonth;
      el.innerHTML=`下一场 · ${nf.home?"主场":"客场"} vs <b>${esc(nf.opponent)}</b>`+
        `<span class="nm-when">${nf.competition==="联赛"?"":`（${esc(nf.competition)}）`} · ${gap<=0?"本月末":`还有 ${gap} 个月`}</span>`}}}
```

- [ ] **Step 6: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add app.js index.html style.css tests/structure.test.mjs
git commit -m "主界面显示下一场：对手、主客、还有几个月

.next-match 自带样式而不是挂裸 span——移动端有一条
.now-strip span:not(.live-dot){display:none}，挂上去会被整条藏掉。
伤停时显示复出倒计时，赛季打完时说清楚，不留空白。"
```

---

### Task 6: 日程页

**Files:**
- Modify: `app.js:1713`（`renderMatches`）
- Modify: `index.html:157`（导航按钮文案）
- Modify: `style.css`
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾：

```js
// ===== 日程页：一整季的赛程看得见 =====
{
  assert.ok(/本赛季日程/.test(code),"「比赛」tab 要扩成赛程页，含本赛季日程");
  assert.ok(/data-tab="matches"[^>]*>\s*赛程/.test(html)||/>赛程</.test(html),
    "导航按钮文案改成「赛程」");
  assert.ok(/\.fixture-row/.test(css),"日程行要有样式");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`「比赛」tab 要扩成赛程页`

- [ ] **Step 3: 改导航文案**

`index.html:157`，把 `<button data-tab="matches">比赛</button>` 改为：

```html
          <button data-tab="matches">赛程</button>
```

- [ ] **Step 4: 加样式**

追加到 `style.css` 末尾：

```css
/* 日程页：一行一场 */
.fixture-row{display:grid;grid-template-columns:58px 1fr auto;gap:10px;align-items:center;padding:11px 13px;border:1px solid var(--line);border-radius:13px;background:#0b1b15;font-size:11px}
.fixture-row.now{border-color:var(--gold);background:#f0bd5e0d}
.fixture-row.done{opacity:.72}
.fixture-row .fx-when{color:var(--muted);font-size:9px}
.fixture-row .fx-opp b{font-size:12px}
.fixture-row .fx-meta{color:var(--muted);font-size:9px;display:block;margin-top:2px}
.fixture-row .fx-score{font-weight:900;font-size:13px;color:var(--gold);white-space:nowrap}
.fixture-row .fx-score.win{color:var(--green)}
.fixture-row .fx-score.loss{color:var(--bad)}
.fixture-row .fx-score.pending{color:var(--muted);font-size:10px;font-weight:700}
@media (max-width:760px){.fixture-row{grid-template-columns:46px 1fr auto;padding:9px 10px}}
```

- [ ] **Step 5: 重写 `renderMatches`**

把 `app.js:1713` 整个 `renderMatches` 替换为：

```js
function fixtureRow(f,cur){
  const done=f.status==="played"&&f.result,r=f.result;
  const cls=done?(r.gf>r.ga?"win":r.gf<r.ga?"loss":""):"pending";
  const score=done?`${r.gf} : ${r.ga}`:f.month===cur?"本月":`${f.month-cur}个月后`;
  const age=14+Math.floor(f.month/12),mon=f.month%12+1;
  return `<div class="fixture-row ${f.month===cur?"now":""} ${done?"done":""}">`+
    `<div class="fx-when">${age}岁<br>第${mon}月</div>`+
    `<div class="fx-opp"><b>${f.home?"主":"客"} vs ${esc(f.opponent)}</b>`+
    `<span class="fx-meta">${esc(f.competition)}${done?` · 你 ${r.goals}球 ${r.assists}助 · 评分 ${r.rating}`:` · 对手实力 ${f.strength}`}</span></div>`+
    `<div class="fx-score ${cls}">${score}</div></div>`;
}
function renderMatches(){
  const c=S.statsCareer;ensureSchedule(S);
  const fx=(S.schedule&&S.schedule.fixtures)||[],played=fx.filter(f=>f.status==="played").length;
  $("panel").innerHTML=`<section class="hero-panel"><span class="eyebrow">FIXTURES</span><h2>强不等于稳赢</h2><p>能力越高，发挥通常越稳；但状态、疲劳、伤停、对手强弱和临场运气都会影响结果。赛程在赛季初就排定，你可以提前为硬仗调整体能与状态。</p>${heroMetrics([[`${played}/${fx.length}`,"本赛季已打"],[c.goals,"生涯进球"],[c.matches,"生涯出场"],[c.bestRating.toFixed?.(1)||c.bestRating,"最佳评分"]])}</section>`+
    `<div class="section-head"><h2>本赛季日程</h2><span>第${ageInfo(S).season}赛季 · ${fx.length}场</span></div>`+
    `<div class="card-list">${fx.length?fx.map(f=>fixtureRow(f,S.totalMonth)).join(""):'<div class="empty-state">赛程尚未排定。</div>'}</div>`+
    `<div class="section-head"><h2>最近比赛</h2><span>${S.matches.length}场已归档</span></div>`+
    `<div class="card-list">${S.matches.length?S.matches.slice(0,12).map(matchCard).join(""):'<div class="empty-state">结束月份后，第一场简报会出现在这里。</div>'}</div>`;
}
```

- [ ] **Step 6: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add app.js index.html style.css tests/structure.test.mjs
git commit -m "日程页：本赛季赛程 + 归档，不新增导航按钮

「比赛」tab 原来只有一个归档列表，扩成「赛程」：上半是本赛季日程
（已打显示比分评分，未打显示对手主客与还有几个月，当月高亮），
下半保留原来的简报归档。
不加第10个导航按钮——现在这9个在移动端已经要横向滚动了。"
```

- [ ] **Step 8: A 段整体验收**

```bash
python3 -m http.server 8766
```
浏览器开 `http://127.0.0.1:8766/`，新建一档，确认：
1. 主界面 now-strip 右侧显示「下一场 · 客场 vs XXX · 本月末」
2. 点「赛程」tab 能看到一整季日程，当月高亮
3. 点「结束本月」先出赛前预告（写着对手和三个职责），选一个后才进关键时刻
4. 比赛简报之后是本月事件，**月度小结在最后**
5. 回到赛程页，刚打完那场显示比分

---

# B 段 · 属性映射

B 段与 A 段互不依赖，可以并行。

### Task 7: 标定16岁分流的 fame 后门阈值

这是一个**测量任务**，不改游戏逻辑。spec 明确要求这个数实测得出、不能估算。

**Files:**
- Create: `tests/calibrate-route16.mjs`

- [ ] **Step 1: 写标定脚本**

创建 `tests/calibrate-route16.mjs`：

```js
/* 一次性标定脚本：16岁分流的后门阈值从 scout 换成 fame，阈值必须重新量。
 *
 * 今天的门槛（app.js:1088）：overall>=63+d.threshold || scout>=28+d.threshold
 * 改动后 scout 被删，每一次 change(s,"scout",N) 变成等量的 change(s,"fame",N)。
 * 所以新版的 fame ≈ 旧版 fame + (旧版 scout - 起始5)，clamp 到 100。
 * 这让我们能在「当前代码」上一次跑出两个分布，求出等宽的新阈值。
 *
 * 跑法：node tests/calibrate-route16.mjs
 */
import fs from "node:fs";
import path from "node:path";
import vm from "node:vm";
import {fileURLToPath} from "node:url";

const here=path.dirname(fileURLToPath(import.meta.url));
const root=path.resolve(here,"..");
const code=fs.readFileSync(path.join(root,"app.js"),"utf8");
const sandbox={console,Date,Math,setTimeout,clearTimeout,window:{},globalThis:{}};
vm.createContext(sandbox);
vm.runInContext(code,sandbox);
const G=sandbox.window.PlayerLife;

const allocation={PAC:4,SHO:4,PAS:3,DRI:3,DEF:3,PHY:3,WIL:4};
const N=600;
const clamp=(n)=>Math.max(0,Math.min(100,n));

function drive(t,rand,cap=400){
  const q=G.getModalQueue();let n=0;
  while(q.length&&n++<cap){
    const m=q.shift();
    const opts=typeof m.options==="function"?m.options(t):m.options;
    if(!opts||!opts.length)continue;
    const c=opts[Math.floor(rand()*opts.length)]||opts[0];
    if(c&&c.apply)try{c.apply()}catch(e){}
  }
}

// 可复现的 LCG，免得每次跑出不同的阈值
let seed=20260803;
const rand=()=>((seed=(seed*1664525+1013904223)>>>0)/4294967296);

const rows=[];
for(let i=0;i<N;i++){
  const t=G.createInitialState(`标定${i}`,allocation,
    G.TALENTS.slice(i%18,i%18+3).map(x=>x.id),"standard","mid");
  G.setState(t);G.clearModalQueue();
  for(let m=0;m<24&&!t.retired;m++){
    // 随机花掉本月执行点，模拟真实玩家
    const avail=G.ACTIONS.filter(a=>a.phases.includes(G.phaseOf(t)));
    for(let k=0;k<3&&avail.length;k++){
      const a=avail[Math.floor(rand()*avail.length)];
      try{a.run(t)}catch(e){}
    }
    G.setState(t);
    try{G.advanceMonth()}catch(e){}
    drive(t,rand);
  }
  rows.push({scout:t.scout,fame:t.fame});
}

const D=G.DIFFICULTIES.standard.threshold;
const oldGate=28+D;
const passOld=rows.filter(r=>r.scout>=oldGate).length/N;
const fameNew=rows.map(r=>clamp(r.fame+(r.scout-5))).sort((a,b)=>a-b);

// 求 T 使 P(fameNew>=T) 最接近 passOld
let best=null;
for(let T=0;T<=100;T++){
  const p=fameNew.filter(v=>v>=T).length/N;
  const d=Math.abs(p-passOld);
  if(!best||d<best.d)best={T,p,d};
}

const pct=(x)=>(x*100).toFixed(1)+"%";
console.log(`样本 N=${N}，标准难度 threshold=${D}`);
console.log(`旧后门 scout>=${oldGate} 通过率：${pct(passOld)}`);
console.log(`旧 scout 分位：p50=${[...rows].map(r=>r.scout).sort((a,b)=>a-b)[Math.floor(N/2)]} p90=${[...rows].map(r=>r.scout).sort((a,b)=>a-b)[Math.floor(N*.9)]}`);
console.log(`新 fame 分位：p50=${fameNew[Math.floor(N/2)]} p90=${fameNew[Math.floor(N*.9)]}`);
console.log(`==> 等宽的新阈值：fame >= ${best.T} + d.threshold（通过率 ${pct(best.p)}）`);
console.log(`   setRoute 那道 scout>=38 的门按同比例换算：fame >= ${Math.round(best.T*38/28)} + d.threshold`);
```

- [ ] **Step 2: 跑标定**

Run: `node tests/calibrate-route16.mjs`
Expected: 输出形如

```
样本 N=600，标准难度 threshold=0
旧后门 scout>=28 通过率：XX.X%
...
==> 等宽的新阈值：fame >= NN + d.threshold（通过率 XX.X%）
   setRoute 那道 scout>=38 的门按同比例换算：fame >= MM + d.threshold
```

**把输出里的 `NN` 和 `MM` 记下来，Task 8 要用。** 如果通过率是 0% 或 100%，说明驱动没跑通（多半是 `advanceMonth` 在比赛那步卡住了），先排查再往下走——**不要拿一个没意义的数去改门槛**。

- [ ] **Step 3: 提交标定脚本与结果**

```bash
git add tests/calibrate-route16.mjs
git commit -m "标定脚本：16岁分流后门从 scout 换 fame 的等宽阈值

scout 起始5、只靠事件涨；fame 起始3、除事件外每场比赛还拿
goals*1.3+assists*.7。两条曲线形状不同，把 28 直接抄成 fame
阈值必然错。这个脚本在当前代码上一次跑出两个分布，求等宽阈值。
LCG 固定种子，结果可复现。"
```

---

### Task 8: `scout` → `fame`

**Files:**
- Modify: `app.js`（6处 `change(s,"scout",...)`、门槛2处、`createInitialState`、`normalizeSave`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾。**把 `NN`/`MM` 换成 Task 7 量出来的数**：

```js
// ===== 球探已删，后门接到声望 =====
{
  assert.ok(!/["']scout["']/.test(code.replace(/tags:\[[^\]]*\]/g,"")),
    "scout 字段必须零残留（天赋 tag 里的 'scout' 是另一回事，不算）");
  const t=G.createInitialState("无球探",allocation,[],"standard","mid");
  assert.equal("scout" in t,false,"新档不该再有 scout 字段");
  // 老档搬运：练了一年球探的档不能在16岁突然撞墙
  const old={version:3,scout:33,fame:12,national:{},attrs:{},styles:{},relationship:{},injury:{},risks:{}};
  const mig=G.normalizeSave(old);
  assert.equal("scout" in mig,false,"normalizeSave 要删掉 scout");
  assert.equal(mig.fame,12+(33-5),"scout 超出起始值的部分要折进 fame");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`scout 字段必须零残留`

- [ ] **Step 3: 替换6处写入点**

以下每一处，把 `change(s,"scout",N)` 改成 `change(s,"fame",N)`，**并把选项文案里的「球探」改成「声望」**：

| 行 | 原文案片段 | 改后 |
|---|---|---|
| :314 | `"球探关注+8；可能抓住机会，也可能伤停2—4个月"` | `"声望+8；可能抓住机会，也可能伤停2—4个月"` |
| :318 | `"球探+9，状态+5；感情-10"` | `"声望+9，状态+5；感情-10"` |
| :329 | `"声望+10，球探+4；媒体风险+8"` | `"声望+14；话题会跟着你"` ⚠️ 见下 |
| :395 | `"球探+18，职业机会大增；感情-14"` | `"声望+18，职业机会大增；感情-14"` |
| :396 | `"感情+15，意志+1；试训成功率下降"`（内含 `change(s,"scout",-6)`） | 文案不变，`change` 改 `fame` |
| :417 | `"教练信任+6，球探+4；状态-3"` | `"教练信任+6，声望+4；状态-3"` |
| :756 | `"球探+8，体能-12；感情+8"` | `"声望+8，体能-12；感情+8"` |
| :757 | `"感情+16；球探-10，教练信任-5"` | `"感情+16；声望-10，教练信任-5"` |

⚠️ **:329 特殊**：原本是 `change(s,"fame",10);change(s,"scout",4);change(s.risks,"media",8)`。三个都往 fame 归并会变成 `fame+14` 再被媒体风险扣掉——直接合成一次 `change(s,"fame",14)` 即可，媒体风险那部分在 Task 9 处理。

用这条命令确认没有漏网的：

```bash
grep -n 'change(s,"scout"' app.js
```
Expected: 无输出

- [ ] **Step 4: 换掉两道门槛**

`app.js:1088`，把：
```js
eligibleLocal=o>=63+d.threshold||s.scout>=28+d.threshold,
```
改为（`NN` 替换成 Task 7 的数）：
```js
eligibleLocal=o>=63+d.threshold||s.fame>=NN+d.threshold,
```

`app.js:1102`，把：
```js
success=overall(s)>=61+d.threshold||s.scout>=38+d.threshold;
```
改为（`MM` 替换成 Task 7 的数）：
```js
success=overall(s)>=61+d.threshold||s.fame>=MM+d.threshold;
```

- [ ] **Step 5: 删初始字段**

`app.js:272` 的 `createInitialState` 里，把 `scout:5,` 整段删掉。

- [ ] **Step 6: `normalizeSave` 搬运老档**

在 `app.js:1288` 的 `normalizeSave` 里，`return d;` 之前插入：

```js
  /* scout 已删，后门改读 fame。老档不迁移的话，一个练了一年球探的
     存档会在16岁突然撞墙——而且不升 VERSION，它不会被清档。 */
  if("scout" in d){d.fame=clamp((d.fame||0)+((d.scout||5)-5));delete d.scout}
```

- [ ] **Step 7: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS

- [ ] **Step 8: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "删掉球探，16岁分流的后门接到声望

球探只在16岁分流时被读一次，之后所有「球探+9」全是空转。
「有人在看你」本来就是声望的语义，fame 从14岁就存在、界面可见、
已有49处事件在写它。
阈值不是估的：tests/calibrate-route16.mjs 实测旧后门通过率后反解，
后门宽度与改动前持平。
normalizeSave 把老档已积累的球探折进声望——不升 VERSION，
不迁移就会撞墙。"
```

---

### Task 9: 删 `study` / `risks.health` / `risks.media`

**Files:**
- Modify: `app.js`（写入点、`createInitialState`、`riskSettlement`、`normalizeSave`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 三个只写不读的字段清干净 =====
{
  assert.ok(!/change\(s,"study"/.test(code),"study 一处都没被读过，删干净");
  assert.ok(!/risks\.health|change\(s\.risks,"health"/.test(code),"健康风险只写不读，归到 injury.risk");
  assert.ok(!/risks\.media|change\(s\.risks,"media"/.test(code),"媒体风险只有一条隐形随机扣分，归到声望");
  const t=G.createInitialState("清场",allocation,[],"standard","mid");
  assert.equal("study" in t,false,"新档没有 study");
  assert.deepEqual(Object.keys(t.risks),["gambling"],"risks 只剩赌博一项");
  // 玩家看不见的骰子不该存在
  assert.ok(!/一轮集中舆论消耗了你的公众信用/.test(code),
    "媒体风险≥65 每月20%概率扣声望，是个玩家看不见的骰子，删掉");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`study 一处都没被读过，删干净`

- [ ] **Step 3: `study` → `WIL`**

`app.js` 有3处写 `study`（含 `setRoute` 里 :1097 的一处）。逐处改：

- 事件里的 `change(s,"study",6)` → `gain(s,"WIL",.4,"will")`，文案 `学业+6` → `意志+0.4`
- 事件里的 `change(s,"study",15)` → `gain(s,"WIL",1,"will")`，文案 `学业+15` → `意志+1`
- `setRoute` 里 :1097 的 `change(s,"study",...)` 直接删（校园线的学业已经由 `campus` 阶段本身表达）

确认：
```bash
grep -n '"study"' app.js
```
Expected: 无输出

- [ ] **Step 4: `risks.health` → `injury.risk`**

4处 `change(s.risks,"health",N)` 全部改为 `s.injury.risk=Math.max(0,(s.injury.risk||0)+N)`，文案 `健康风险` → `伤病风险`：

| 行 | 改法 |
|---|---|
| :326 | `change(s.risks,"health",-8)` → `s.injury.risk=Math.max(0,(s.injury.risk||0)-8)`，文案「职业风险下降」→「伤病风险-8」 |
| :327 | 同上，`-6` |
| :370 | 同上，大幅下降改 `-20`，文案「健康风险大幅下降」→「伤病风险-20」 |
| :371 | 同上，`+12`，文案「健康与隐私风险上升」→「伤病风险+12」 |
| :460 | `-15`，文案「健康风险-15」→「伤病风险-15」 |
| :550 | `-20`，文案「健康风险清空」→「伤病风险-20」 |

`riskSettlement`（:1323）里那条 `if(s.flags.hivDiagnosed&&s.flags.hivIntermittent){change(s,"fitness",-30);change(s.risks,"health",8)}` 改为：

```js
  if(s.flags.hivDiagnosed&&s.flags.hivIntermittent){change(s,"fitness",-30);s.injury.risk=Math.min(100,(s.injury.risk||0)+8)}
```

- [ ] **Step 5: `risks.media` → `fame`**

6处写入点，把 `change(s.risks,"media",N)` 删掉并把影响折进同一选项的 `fame`：

| 行 | 改法 |
|---|---|
| :329 | Task 8 已合成 `fame+14`，此处只需删掉 `change(s.risks,"media",8)` |
| :342 | `change(s,"fame",5);change(s.risks,"media",12)` → `change(s,"fame",-7)`，文案「声望+5，转会报价概率上升；媒体风险+12」→「声望-7，转会报价概率上升」 |
| :345 | `change(s.risks,"media",8)` 删，文案「隐私被公开」保留（它是叙事，不是数值） |
| :361 | `change(s.risks,"media",-6)` 删，文案「媒体风险-6；感情-20」→「感情-20，风波按俱乐部口径压下去」 |
| :387 | `change(s.risks,"media",18)` → `change(s,"fame",-10)`，文案「教练信任+6；若后续反转，媒体风险+18」→「教练信任+6；若后续反转，声望-10」 |
| :389 | `change(s.risks,"media",-8)` 删，文案「声望短期-4，媒体风险下降」→「声望短期-4」 |
| :401 | `change(s.risks,"media",8)` → `change(s,"fame",-6)`，文案「月薪上调；教练信任-8，媒体风险+8」→「月薪上调；教练信任-8，声望-6」 |
| :539 | `change(s.risks,"media",-10)` 删，改为 `change(s,"fame",6)`，文案「若声望>80则舆论平息，否则更糟」保留 |

删掉 `riskSettlement`（:1322）里整行：
```js
  if(s.risks.media>=65&&chance(.2)){change(s,"fame",-8);change(s.risks,"media",-12);log(s,"warn","一轮集中舆论消耗了你的公众信用。")}
```

- [ ] **Step 6: 清初始字段与老档**

`createInitialState`（:272）：`study:55,` 删掉，`risks:{gambling:0,health:0,media:0}` 改为 `risks:{gambling:0}`。

`normalizeSave` 里 Task 8 那段之后追加：

```js
  if("study" in d)delete d.study;
  if(d.risks){delete d.risks.health;delete d.risks.media;if(typeof d.risks.gambling!=="number")d.risks.gambling=0}
```

- [ ] **Step 7: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS

- [ ] **Step 8: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "删掉学业、健康风险、媒体风险三个只写不读的字段

学业零处被读；健康风险只写不读；媒体风险只有一条
「≥65 每月20%概率扣声望」的隐形骰子——玩家看不见的随机不该存在。
健康风险归到 injury.risk 是白捡的：prepareMatch 的伤病公式
已经在读 (s.injury.risk||0)/900，语义完全对得上，接过去当场生效。
事件的效果文案同步改——数值改了标签没改，比不改更糟。"
```

---

### Task 10: `language` 收窄到18岁前

**Files:**
- Modify: `app.js:153`（`english` 行动）、pro期写 language 的事件
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 语言只在海外线16→18那两年有意义 =====
{
  const en=G.ACTIONS.find(a=>a.id==="english");
  assert.ok(en,"英语行动还在");
  assert.equal(en.phases.includes("pro"),false,
    "18岁后没有任何代码读 language，这个行动不该继续出现在职业期骗执行点");
  assert.ok(en.phases.includes("overseas"),"海外线仍然需要它");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`18岁后没有任何代码读 language`

- [ ] **Step 3: 收窄行动**

`app.js:153`，把 `english` 的 `phases:["overseas","pro"]` 改为 `phases:["overseas"]`。

- [ ] **Step 4: pro 期写 language 的事件改写**

`app.js:348` 那处 `change(s,"language",hasTalent(s,"language_gift")?30:18)` 属于 pro 期事件，改为：

```js
gain(s,"WIL",hasTalent(s,"language_gift")?1.6:1,"will")
```
文案 `"语言+18，教练信任-2；意志+1"` → `"意志+1，教练信任-2"`。

`app.js:413` 那处 `change(s,"language",6)` 在 overseas 阶段，**保留不动**。

- [ ] **Step 5: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "语言收窄到海外线16→18那两年

18岁 enterProAt18 的海外晋升门槛是最后一处读 language 的地方，
之后它只涨不用。「认真学英语」继续挂在职业期就是在骗执行点。
海外线内保留——那两年它是真门槛。"
```

---

### Task 11: 家庭接 form 基线 + 侧栏

**Files:**
- Modify: `app.js`（`familySupport`、`advanceMonth` 基线、`renderAll`、API 导出）
- Modify: `index.html`（侧栏）
- Modify: `style.css`
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 家庭必须真的起作用，而不只是33个事件写完就沉底 =====
{
  assert.equal(typeof G.familySupport,"function","家庭要有一个像 loveSupport 那样的支撑函数");
  const mk=(f)=>{const t=G.createInitialState("家",allocation,[],"standard","mid");t.family=f;return t};
  assert.ok(G.familySupport(mk(95))>G.familySupport(mk(60)),"家里越稳，支撑越高");
  assert.ok(G.familySupport(mk(10))<0,"家里出事要是负的");
  // 绝不能写成每月固定 +N：form 每月回归22%，固定加成会把均衡点顶高 4.5N
  assert.ok(!/change\(S,"form",\s*familySupport/.test(code),
    "家庭必须抬高基线，不能每月固定加一笔——那会把均衡点顶高 4.5 倍");
  assert.ok(/base=52\+loveSupport\(S\)\+familySupport\(S\)/.test(code.replace(/\s/g,"").replace(/base=52\+loveSupport\(S\)\+familySupport\(S\)/,"base=52+loveSupport(S)+familySupport(S)"))
    ||/familySupport\(S\)/.test(code),"基线里要带上 familySupport");
  // 侧栏
  assert.ok(/id="familyText"/.test(html)&&/id="familyBar"/.test(html),"侧栏要有家庭那一行");
}
// 长跑：家里塌了的档，状态均衡点必须明显低于家里稳的档
{
  const run=(fam)=>{
    const t=G.createInitialState("均衡家",allocation,[],"standard","mid");
    t.relationship.status="分手";t.relationship.love=0;t.assets.house=false;t.form=52;
    const origR=Math.random;let x=7;Math.random=()=>((x=(x*1664525+1013904223)>>>0)/4294967296);
    try{for(let m=0;m<120&&!t.retired;m++){t.family=fam;G.setState(t);G.advanceMonth()}}catch(e){}finally{Math.random=origR}
    return t.form;
  };
  const hi=run(95),lo=run(10);
  assert.ok(hi-lo>=4,`家里稳(${hi.toFixed(1)}) 与家里塌(${lo.toFixed(1)}) 的状态均衡点差距太小，家庭没真正起作用`);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`家庭要有一个像 loveSupport 那样的支撑函数`

- [ ] **Step 3: 实现 `familySupport`**

在 `app.js:766` 的 `loveSupport` 紧邻处加：

```js
/* 家庭同样只走抬高基线这一条路。33个事件已经在写 family，
   接上基线就等于白捡——但绝不能写成每月固定 +N，
   form 每月回归 22%，那会把均衡点顶高 N/0.22≈4.5N。 */
function familySupport(s){const f=s.family;return f>=90?4:f>=70?2:f>=50?1:f>=30?0:-3}
```

- [ ] **Step 4: 接进基线**

`app.js:1598` 附近，把：
```js
  const base=52+loveSupport(S)+(S.assets&&S.assets.house?2:0);
```
改为：
```js
  const base=52+loveSupport(S)+familySupport(S)+(S.assets&&S.assets.house?2:0);
```

- [ ] **Step 5: 侧栏 DOM**

`index.html`，在 `loveBar` 那一组之后（:142 之后）插入：

```html
          <div class="pulse-row"><span title="家里的安稳程度，影响你状态的长期基线">家庭</span><b id="familyText">86</b></div>
          <div class="mini-track sky"><i id="familyBar"></i></div>
```

- [ ] **Step 6: 样式**

追加到 `style.css` 末尾：

```css
.mini-track.sky i{background:linear-gradient(90deg,#3d7fa8,#5cc8e8)}
```

⚠️ `tests/structure.test.mjs:866` 有孤儿配色护栏——`.mini-track.<色>` 定义了就必须有地方引用。上一步的 `class="mini-track sky"` 正是那个引用，两步必须一起做完。

- [ ] **Step 7: `renderAll` 填充**

在 `renderAll` 里 `$("loveBar").style.width=...` 那一行之后插入：

```js
  {const ft=$("familyText"),fb=$("familyBar");if(ft){ft.textContent=Math.round(S.family)}if(fb){fb.style.width=`${S.family}%`}}
```

- [ ] **Step 8: 导出 `familySupport`**

`app.js:1753` 的 `API` 里，`loveSupport,` 之后加 `familySupport,`。

- [ ] **Step 9: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS

- [ ] **Step 10: 提交**

```bash
git add app.js index.html style.css tests/structure.test.mjs
git commit -m "家庭接上状态基线，并上侧栏

33个事件在写 family，但它此前只做3个事件的触发条件，没有任何数值作用。
照抄 loveSupport 的分档形状抬高 form 基线：家里安稳你状态长期更稳，
家里出事你持续下滑。那33个事件一行不用改。
绝不写成每月固定 +N——form 每月回归22%，固定加成会把均衡点顶高
4.5倍，tests 里已有一条断言钉死这一点。
初始86落在 +2 档，上有 +4 下有 -3，两头都够得着。"
```

- [ ] **Step 11: B 段验收**

```bash
grep -nE '"scout"|"study"|risks\.(health|media)' app.js
```
Expected: 无输出

```bash
grep -n '球探\|学业\|媒体风险\|健康风险' app.js
```
Expected: 无输出（若有残留，是漏改的效果文案）

---

# C 段 · 国家队大赛

C 段的 T15/T16 依赖 A 段的赛程表。T12/T13/T14 可以先做。

### Task 12: 抽出 `resolveMoments`

**Files:**
- Modify: `app.js:1008-1050`（`finishMatch`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 关键时刻的并入规则只能有一份 =====
// 稳妥选项只能认领模型已算出的进球，只有高风险能凭空造球且每场至多一个。
// 世界杯要复用这套规则，绝不能在那边写一套近似版——两套必然漂移。
{
  assert.equal(typeof G.resolveMoments,"function","并入规则抽成共享函数");
  const t=G.createInitialState("并入",allocation,[],"standard","mid");
  ATTR_KEYS.forEach(k=>t.attrs[k]=80);
  const m=G.pickMoments(t,()=>.5);
  assert.ok(m.length>0,"sanity: 能抽到关键时刻");
  // 稳妥选项 + 球队 0 球 → 不许凭空造出进球
  const p={gf:0,ga:0,goals:0,assists:0,keyWins:0,failures:0,timeline:[],
    moments:m,choices:m.map(()=>-1),oppStrength:70,plan:"box"};
  const out=G.resolveMoments(t,p,()=>0);          // rng=0 → 一定成功
  assert.equal(out.gf,0,"稳妥选项不许把球队比分吹上去");
  // 高风险选项 + 球队 0 球 → 至多造一个
  const bold={gf:0,ga:0,goals:0,assists:0,keyWins:0,failures:0,timeline:[],
    moments:m,choices:m.map(()=>0),oppStrength:70,plan:"box"};
  const out2=G.resolveMoments(t,bold,()=>0);
  assert.ok(out2.gf<=1,`高风险每场至多造一个球，实际 ${out2.gf}`);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`并入规则抽成共享函数`

- [ ] **Step 3: 抽函数**

把 `app.js:1008` 的 `finishMatch` 里，从 `let ratingDelta=0,...` 到 `p.moments.forEach(...)` 结束那一整块（现 :1009-1045）搬进新函数，放在 `finishMatch` **之前**：

```js
/* 关键时刻结算：由 finishMatch（俱乐部/世预赛）与大赛决赛圈共用。
   规则的要害是 claimGoal——稳妥选择只是把球队已有的机会转化到你名下，
   只有高风险才能创造 xG 之外的进球，且每场至多一个 overflow。
   世界杯那边绝不能写一套近似版，两套必然漂移。
   传入的 p 会被就地修改并返回；ratingDelta/classic 等结算量挂在返回值上。 */
function resolveMoments(s,p,rng=Math.random){
  let ratingDelta=0,boldTotal=0,boldWon=0,classic=false,fitExtra=0,favorExtra=0,overflow=0;
  const claimGoal=bold=>{if(p.goals<p.gf){p.goals++;return true}if(bold&&overflow<1){overflow++;p.gf++;p.goals++;return true}return false};
  const claimAssist=bold=>{if(p.goals+p.assists<p.gf){p.assists++;return true}if(bold&&overflow<1){overflow++;p.gf++;p.assists++;return true}return false};
  (p.moments||[]).forEach((slot,i)=>{
    const m=MOMENTS.find(x=>x.id===slot.id);if(!m)return;
    const opts=momentOptions(s,m);
    const choiceIdx=p.choices[i];
    const o=opts[choiceIdx]!==undefined?opts[choiceIdx]:opts.find(x=>x.risk==="safe")||opts[0];
    const behind=p.gf<p.ga;
    const ok=rng()<momentSuccessRate(s,m,o,p.oppStrength,behind);
    if(o.risk==="bold")boldTotal++;
    if(ok){
      boldWon+=o.risk==="bold"?1:0;p.keyWins++;
      let up=o.up,denied=false;
      const bold=o.risk==="bold";
      if(o.goal){
        if(claimGoal(bold)){
          if(styleOf(s,"box")>=3&&m.id==="late_chase"&&o.stat==="SHO")up+=.3;
          if(styleOf(s,"burst")>=3&&o.stat==="DRI"&&rng()<.12&&claimGoal(bold))
            p.timeline.push({minute:Math.min(89,slot.minute+2),text:"过掉人之后你顺势又补了一脚——这球也进了。",kind:"goal"});
        }else denied=true;
      }else if(o.assist&&!claimAssist(bold))denied=true;
      if(!denied&&(o.teamGoal||(styleOf(s,"target")>=3&&o.style==="target"))&&overflow<1&&rng()<.45){
        overflow++;p.gf++;
        p.timeline.push({minute:Math.min(89,slot.minute+1),text:"你做的球被队友转化成了进球。",kind:"goal"});
      }
      if(o.classic&&!denied)classic=true;
      if(denied)up*=.5;
      ratingDelta+=up;favorExtra+=o.favor||0;fitExtra+=(o.fitGain||0)-(o.fitCost||0);
      if(o.style)addStyleExp(s,o.style,5);
      p.timeline.push({minute:slot.minute,
        text:denied?`【${o.text}】${o.goal?"你完成了这次攻门，但球在门线前被解围，比分没有变化。":"你把球做到位了，队友却没能把它转化成进球。"}`:`【${o.text}】${o.win}`,
        kind:denied?"turn":o.goal?"goal":"good"});
    }else{
      p.failures++;
      ratingDelta+=o.down*(hasTalent(s,"pressure_proof")?.5:1);
      fitExtra-=(o.fitCost||0)+3;
      p.timeline.push({minute:slot.minute,text:`【${o.text}】${o.fail}`,kind:"turn"});
      if(o.risk==="bold"&&rng()<.35){p.ga++;
        p.timeline.push({minute:Math.min(89,slot.minute+2),text:"丢球之后对手立刻打起反击，我们的防线还没站好——他们扳回一球。",kind:"bad"})}
    }
  });
  p.ratingDelta=ratingDelta;p.boldTotal=boldTotal;p.boldWon=boldWon;
  p.momentClassic=classic;p.fitExtra=fitExtra;p.favorExtra=favorExtra;
  return p;
}
```

- [ ] **Step 4: `finishMatch` 改成调用它**

把 `finishMatch` 开头替换为：

```js
function finishMatch(s,pending,rng=Math.random){
  const p={...pending,timeline:pending.timeline.slice()};
  resolveMoments(s,p,rng);
  const ratingDelta=p.ratingDelta,boldTotal=p.boldTotal,boldWon=p.boldWon;
  let classic=p.momentClassic;
  const fitExtra=p.fitExtra,favorExtra=p.favorExtra;
```

后面从 `if(p.injuredInMatch)...` 开始的部分保持原样不动。

- [ ] **Step 5: 导出**

`API` 里加 `resolveMoments,`。

- [ ] **Step 6: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS。**特别注意原有的比赛相关断言必须全绿**——这一步是纯抽取，行为不能有任何变化。

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "抽出 resolveMoments：关键时刻的并入规则只留一份

规则的要害是 claimGoal：稳妥选择只能把球队已有的机会转化到你名下，
只有高风险才能创造 xG 之外的进球，且每场至多一个 overflow。
世界杯马上要复用它。在那边写一套近似版，两套必然漂移——
这是纯抽取，行为零变化。"
```

---

C 段剩下的 T13–T17（`wc*`→`cup*` 更名与 `cupRun`、亚洲杯对手池与配置表、大赛日历排期、世预赛摊成6轮、决赛圈关键时刻）以及 D 段收尾（文案、README、重建单文件版），我接着往下写。
