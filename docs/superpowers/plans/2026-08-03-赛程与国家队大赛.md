# 赛程系统 / 属性映射 / 国家队大赛 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给游戏加上真正的赛季赛程（主界面下一场、日程页、赛前预告），清掉一批只写不读的死属性，并把国家队大赛从「一屏出结果」改成可参与的世界杯 + 亚洲杯。

**Architecture:** 核心是新增 `s.schedule` —— 把「这个月打谁」从月末现抽改成赛季初排定。这一个数据结构同时喂饱主界面、日程页、赛前预告和大赛日历。世界杯与亚洲杯结构同构，共用一套参数化的赛事引擎（`cupRun` + 配置表），关键时刻的比分并入规则从 `finishMatch` 抽成 `resolveMoments` 供两边共用。

**Tech Stack:** 纯 ES5+ 浏览器 JS，无框架无构建。`app.js`（单文件，约1759行）+ `index.html` + `style.css`。测试是 `node:test` 之外的裸断言脚本，用 `node:vm` 把 `app.js` 灌进沙箱跑。

**Spec:** [docs/superpowers/specs/2026-08-03-赛程与世界杯参与感-design.md](../specs/2026-08-03-赛程与世界杯参与感-design.md)

---

## 读这份计划之前必须知道的

### 五个已知陷阱（Task 1–3 实际踩过，Task 4–19 已提前修好）

**陷阱1：vm realm —— 跨沙箱 `deepEqual` 必然失败**
`app.js` 跑在 `node:vm` 沙箱里，沙箱内创建的数组原型与外部 realm 不同。
`assert.deepEqual`（strict 模式）会报 `"Values have same structure but are not reference-equal"`，即使数值完全正确。
**修法：** 比较前用 `[...]` 把数组展开到外部 realm：`assert.deepEqual([...G.qualifierMonths(96)],[84,...],...)`.
例外：`Object.keys(x)` 在外部调用，返回外部数组，**不需要**展开。

**陷阱2：俱乐部比赛流程是异步的**
`app.js` 关键时刻选项里有 `setTimeout(()=>stepKeyMoment(s),0)`。同步 `while` 循环（`driveModals`）消费完第一个关键时刻就因队列空而退出，比赛永远不结算。
**修法：** 凡是驱动「俱乐部比赛 / 世预赛 / 会经过 `stepKeyMoment` 的流程」，一律用 `await driveMatch(...)`。
世界杯决赛圈（`cupPlayMatch` 里 `enqueueFront` 同步添加）可用 `driveModals`；但 Task 17 引入 `resolveMoments` 后决赛圈也会有异步关键时刻，届时改 `driveMatch`（本计划 Task 17 已评估并保持 `driveModals`，待 Task 17 完成后确认）。
任何独立的标定脚本（如 `calibrate-route16.mjs`）里的同步驱动器也必须改成异步版。

**陷阱3：`advanceMonth` 先 `++` 再判定比赛**
`app.js` 里 `S.totalMonth++` 在 `shouldPlayMatch` 之前。所以设 `t.totalMonth=48` 再调 `G.advanceMonth()`，踢的是**第49月**那场，不是第48月。断言月份时必须按此偏移。

**陷阱4：测试有随机性，一次绿不算绿**
`prepareMatch` 里约 22% 概率球员未出场，此时比赛当场结算、无弹窗、`s.pendingMatch` 当场为 null。任何假设「一定有待决比赛」或「一定进球」的断言会随机翻车。
**修法：** 用有界重试循环（最多40次）造出需要的局面，或只断言不变量。
**所有任务的验证步骤都改成跑30次：**
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
预期30个点，出现任何一个 `F` 都不算完成。

**陷阱5：事件选项的 `apply()` 会再往队列里塞东西**
选某些事件（如「不经意的碰撞」→「立刻要求检查」）会调用 `sufferInjury` → `enqueueDecision` 推额外弹窗。Task 3 已通过改 `enqueueDecision`（遇到待显示的月度小结就 splice 插到它前面）解决。写涉及弹窗顺序的断言时，`()=>0` 固定选第一个选项可能产生额外弹窗，需留意。

---

**跑测试：**
```bash
cd /Users/ruis/Documents/模拟/模拟球员
node --check app.js && node tests/structure.test.mjs
```
`tests/structure.test.mjs` 不是测试框架，是一串**顶层裸断言**，从上往下顺序执行，任何一条 `assert` 失败就整个脚本抛错退出。所以：

- 新测试**追加到文件末尾**即可，前面的变量（`G`、`allocation`、`ATTR_KEYS`、`code`、`html`、`css`）都还在作用域里
- `code` = `app.js` 源码文本（`tests/structure.test.mjs:9`），`html`（:824）、`css`（:859）同理。**注意 `html`/`css` 是在第 824/859 行才定义的，写在它们之前的测试用不了**
- 通过时脚本静默退出，没有 "N tests passed" 之类的输出。**"没有输出" 就是 PASS**

**引擎导出：** `app.js` 末尾把一批函数挂到 `window.PlayerLife`，测试里叫 `G`。想在测试里调某个函数，它必须在那个导出对象里。本计划多处会往里加。

**无头模式的关键行为：** `pumpModal` 开头是 `if(modalBusy||!modalQueue.length||typeof document==="undefined")return`。测试环境没有 `document`，所以**弹窗只进队列、不消费**。要推进流程必须自己把队列跑完——`tests/structure.test.mjs:721` 已经有现成的 `driveModals(t,pick,cap)` 干这件事。

**提交规范：** 中文 commit message，说清「为什么」。仓库现有 commit 全是中文，跟上。

---

## 文件结构

本次改动**不新建源文件**。理由写在 spec 的「不做」一节：`build_single.py` 只内联单个 `app.js`，而 README 承诺双击 `index.html` 就能玩，`file://` 下 ES module 会被 CORS 拦掉。拆分要连构建带加载方式一起改，不属于本次范围。

| 文件 | 职责 | 本次改动 |
|---|---|---|
| `app.js` | 全部游戏逻辑与渲染 | 主战场。新增赛程模块、赛事引擎参数化、属性映射 |
| `index.html` | DOM 骨架 | `now-strip` 加下一场、侧栏加家庭条 |
| `style.css` | 全部样式 | 下一场信息、日程页行、家庭条配色 |
| `tests/structure.test.mjs` | 裸断言测试 | 追加新断言；改名波及处同步更新 |
| `tests/calibrate-route16.mjs` | **新建**，一次性标定脚本 | 量出16岁分流后门的 fame 阈值 |
| `模拟球员-单文件版.html` | 分发产物 | 最后一步 `python3 build_single.py` 重建 |

`app.js` 内的新代码按功能就近放置：赛程模块放在 `shouldPlayMatch`（现 :1351）附近，赛事引擎改造在原世界杯段（现 :1361-1585）原地进行。

---

## 任务依赖

```
A 赛程系统        T1 → T2 → T3 → T4 → T5 → T6
B 属性映射        T7 → T8 ,  T9 , T10 , T11        （与 A 独立，可并行）
C 国家队大赛      T12 , T13 → T14 → T15* → T16* → T17
D 收尾            T18 → T19

* T15/T16 依赖 A 的 T1/T2（赛程表）
```

---

# A 段 · 赛程系统

### Task 1: 赛程数据结构与生成

**Files:**
- Modify: `app.js`（在 `shouldPlayMatch`，现 :1351 之前插入新段）
- Modify: `app.js:1753`（`API` 导出）
- Test: `tests/structure.test.mjs`（追加到末尾）

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾：

```js
// ===== 赛程表：对手必须提前排定 =====
// 改动前对手是月末 pick(opponentPool(s)) 现抽的，「下一场打谁」这个信息
// 在系统里根本不存在。主界面、日程页、赛前预告全部卡在这一点上。
{
  const t=G.createInitialState("赛程",allocation,[],"standard","mid");
  const sc=G.ensureSchedule(t);
  assert.ok(sc&&Array.isArray(sc.fixtures),"ensureSchedule 返回带 fixtures 的赛程");
  assert.ok(sc.fixtures.length>0,"新档第一赛季就该有比赛");
  // 14岁梯队每3个月一场。第0月不排——advanceMonth 先 S.totalMonth++ 再判定
  // （app.js:1632 在 :1652 之前），第一次判定就是 totalMonth=1，第0月永远打不到。
  // ⚠️ 外面那层 [...] 不能省：app.js 跑在 vm 沙箱里，沙箱内数组的原型与
  // 外部 realm 不同，deepStrictEqual 会报 "same structure but not reference-equal"。
  assert.deepEqual([...sc.fixtures.filter(f=>f.type==="club").map(f=>f.month)],[3,6,9],
    "梯队期每3个月一场，且不含打不到的第0月");
  assert.ok(!sc.fixtures.some(f=>f.month===0),
    "第0月永远打不到，排了它只会留一个永远未打的幽灵场次，主界面还会跟着说谎");
  sc.fixtures.forEach(f=>{
    assert.ok(typeof f.opponent==="string"&&f.opponent.length>0,"每场都有对手名");
    assert.ok(Number.isFinite(f.strength),"每场都有对手实力");
    assert.equal(typeof f.home,"boolean","每场都有主客");
    assert.equal(f.status,"upcoming","新排的赛程都是未开始");
    assert.equal(f.result,null,"未开始的比赛没有结果");
  });
  // 幂等：签名没变就不该重排
  const again=G.ensureSchedule(t);
  assert.equal(again,sc,"签名未变时 ensureSchedule 必须返回同一个对象，不能每次重排");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`TypeError: G.ensureSchedule is not a function`

- [ ] **Step 3: 实现赛程模块**

在 `app.js` 的 `function shouldPlayMatch(s){...}`（现 :1351）**之前**插入：

```js
/* ========== 赛程表 ==========
   对手必须在赛季初就定下来，否则「下一场打谁」这个信息不存在，
   主界面、日程页、赛前预告全都无从谈起。
   月份节奏在这里是唯一定义，shouldPlayMatch 改成查表。 */
function shuffled(arr,rng){const c=[...arr];for(let i=c.length-1;i>0;i--){const j=Math.floor(rng()*(i+1));[c[i],c[j]]=[c[j],c[i]]}return c}
function matchMonthsOfSeason(s,seasonStart){
  const out=[];
  for(let m=seasonStart;m<seasonStart+12;m++){
    const age=14+Math.floor(m/12),p=age<16?"academy":age<18?(s.route||"academy"):"pro";
    if(age<16){if(m%3===0)out.push(m)}
    else if(p==="campus"){if(m%2===0)out.push(m)}
    else out.push(m);
  }
  return out;
}
function scheduleSig(s){return{season:ageInfo(s).season,clubKey:s.club.name,route:s.route||"",nationalCalled:!!s.national.called}}
function buildSchedule(s,rng=Math.random){
  const seasonStart=Math.floor(s.totalMonth/12)*12,pool=opponentPool(s);
  let bag=[];
  const fixtures=matchMonthsOfSeason(s,seasonStart).map((month,i)=>{
    if(!bag.length)bag=shuffled(pool,rng);
    const opp=bag.shift()||pick(pool);
    return {month,type:"club",opponent:opp.name,strength:opp.strength,home:i%2===0,
      competition:`${s.club.league}第${i+1}轮`,status:"upcoming",result:null};
  });
  return {...scheduleSig(s),fixtures};
}
/* 只重排未来。转会发生在赛季中途时，已打过的战绩必须留住——
   否则换一次东家就抹掉半个赛季的比分。 */
function ensureSchedule(s,rng=Math.random){
  const sig=scheduleSig(s),sc=s.schedule;
  if(sc&&sc.season===sig.season&&sc.clubKey===sig.clubKey&&sc.route===sig.route&&sc.nationalCalled===sig.nationalCalled)return sc;
  const fresh=buildSchedule(s,rng);
  if(sc){
    const past=sc.fixtures.filter(f=>f.status!=="upcoming"||f.month<s.totalMonth);
    const taken=new Set(past.map(f=>f.month));
    fresh.fixtures=[...past,...fresh.fixtures.filter(f=>!taken.has(f.month))].sort((a,b)=>a.month-b.month);
  }
  s.schedule=fresh;return fresh;
}
function fixtureOfMonth(s){const sc=s.schedule;return sc?sc.fixtures.find(f=>f.month===s.totalMonth&&f.status==="upcoming")||null:null}
function nextFixture(s){const sc=s.schedule;return sc?sc.fixtures.find(f=>f.month>=s.totalMonth&&f.status==="upcoming")||null:null}
```

- [ ] **Step 4: 加进 API 导出**

在 `app.js:1753` 的 `const API={...}` 里，紧跟 `prepareMatch,` 之后加：

```js
ensureSchedule,buildSchedule,fixtureOfMonth,nextFixture,shouldPlayMatch,
```

（`shouldPlayMatch` 现在没有被导出，Task 2 的测试需要它。）

- [ ] **Step 5: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS（无输出）

- [ ] **Step 6: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "赛程表：对手改为赛季初排定

月末现抽对手导致「下一场打谁」这个信息在系统里根本不存在，
主界面下一场、日程页、赛前预告三件事全部卡在这里。
月份节奏（梯队每3月/校园每2月/职业每月）从此只在
matchMonthsOfSeason 一处定义。
ensureSchedule 只重排未来，赛季中途转会不会抹掉已打战绩。"
```

---

### Task 2: `shouldPlayMatch` 改查赛程，比赛接上 fixture

**Files:**
- Modify: `app.js:1351`（`shouldPlayMatch`）
- Modify: `app.js:1586-1612`（`advanceMonth`）
- Modify: `app.js:1614`（`startMatchFlow`）
- Modify: `app.js:1662`（`resolveMatch`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾：

```js
// ===== 比赛必须打赛程表上那一场，而不是另抽一个对手 =====
{
  const t=G.createInitialState("对上表",allocation,[],"standard","mid");
  t.totalMonth=48;t.flags.pro18=true;t.route="pro";        // 18岁职业期，每月一场
  t.club={name:"重庆铜梁龙",league:"中超",strength:67};
  G.ensureSchedule(t);
  const fx=G.fixtureOfMonth(t);
  assert.ok(fx,"职业期每个月都该有比赛");
  assert.equal(G.shouldPlayMatch(t),true,"有 fixture 就该踢");

  // 打完之后这一场要被标记，且结果写回赛程表
  G.setState(t);G.clearModalQueue();
  G.advanceMonth();
  driveModals(t,()=>0);
  const done=t.schedule.fixtures.find(f=>f.month===48);
  assert.equal(done.status,"played",`第48月那场应标记 played，实际 ${done.status}`);
  assert.ok(done.result&&Number.isFinite(done.result.gf),"结果必须写回赛程表");
}

// 伤停时不排比赛，且那一场在日程上留痕而不是凭空消失
{
  const t=G.createInitialState("伤停",allocation,[],"standard","mid");
  t.totalMonth=48;t.flags.pro18=true;t.route="pro";
  t.club={name:"重庆铜梁龙",league:"中超",strength:67};
  G.ensureSchedule(t);
  t.injury={name:"脚踝扭伤",months:2,risk:0};
  assert.equal(G.shouldPlayMatch(t),false,"伤停不上场");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`第48月那场应标记 played，实际 upcoming`

- [ ] **Step 3: 改 `shouldPlayMatch` 查表**

把 `app.js:1351` 整行替换为：

```js
function shouldPlayMatch(s){if(s.flags&&s.flags.washedOut)return false;if((s.injury.months||0)>0)return false;return !!fixtureOfMonth(s)}
```

- [ ] **Step 4: `advanceMonth` 里排上赛程**

在 `app.js` 的 `advanceMonth` 中，找到这一行（现 :1594 附近，紧跟 `S.peakOverall=...` 那句之后不远）：

```js
  const _ctx={snap:_snap,preSusp,preInjury,justTurned16,matchReportModal:null};
```

在它**之前**插入：

```js
  ensureSchedule(S);
  /* 有排定的比赛但上不了场（伤停/雪藏/已退役）：在日程上标记 missed 留痕。
     不标的话这一场会永远停在 upcoming，变成「月份在过去却未开打」的幽灵场次，
     还会被 ensureSchedule 的 past 条件一路带进下个赛季。 */
  {const _fx=fixtureOfMonth(S);
   if(_fx&&(S.retired||!shouldPlayMatch(S))){_fx.status="missed";
     _fx.missReason=(S.injury.months||0)>0?`伤停·${S.injury.name||"伤病"}`:(S.flags&&S.flags.washedOut)?"被雪藏":"未出战"}}
```

必须在 `S.totalMonth++` 之后、`shouldPlayMatch` 之前——赛季翻页时要先把新赛季排出来。

⚠️ `missed` 是 spec 里定义的三种状态之一（`upcoming`/`played`/`missed`），spec 明确写了「伤停/雪藏的场次**日程上要留痕**，不能静默消失」。少了这一步，`status` 的第三种取值就没有任何代码在写。

- [ ] **Step 5: `startMatchFlow` 用 fixture 的对手**

把 `app.js:1614-1620` 的 `startMatchFlow` 替换为：

```js
/* ========== 比赛流程：赛前预告 → 2个关键时刻 → 结算 → 交回月末段 ========== */
function startMatchFlow(s,ctx){
  const fx=fixtureOfMonth(s);
  if(!fx){finishMonth(ctx);return}
  s.pendingMatch={stage:"preview",fixture:fx,ctx,index:0,pending:null};
  modalBusy=false;
  stepMatchPreview(s);
}
```

（`stepMatchPreview` 在 Task 4 实现。为了让本任务可独立通过，先在紧邻处放一个直通版本：）

```js
function stepMatchPreview(s){beginMatch(s,s.matchPlan||"box")}
function beginMatch(s,plan){
  const pm=s.pendingMatch,fx=pm.fixture;
  s.matchPlan=plan;
  pm.pending=prepareMatch(s,Math.random,{opponent:{name:fx.opponent,strength:fx.strength},
    home:fx.home,plan,competition:fx.competition});
  pm.stage="moments";
  if(!pm.pending.plays){resolveMatch(s);return}
  stepKeyMoment(s);
}
```

- [ ] **Step 6: `resolveMatch` 把结果写回赛程表**

在 `app.js:1662` 的 `resolveMatch` 里，找到 `applyMatch(s,report);` 这一行，在它**之后**插入：

```js
  if(pm.fixture){pm.fixture.status="played";
    pm.fixture.result={gf:report.gf,ga:report.ga,goals:report.goals,assists:report.assists,rating:report.rating}}
```

注意 `const ctx=pm.ctx,report=finishMatch(s,pm.pending);` 之后紧跟着 `s.pendingMatch=null;`，所以 `pm` 的引用要在置空**之前**取好——上面那段插在 `applyMatch` 后、`pm` 仍然指向原对象，没问题。

- [ ] **Step 7: 恢复入口跟上新的 stage 字段**

把 `app.js:1639-1643` 的 `resumeMatchFlow` 替换为：

```js
// 中断恢复：pumpModal 每次点击都存档，所以刷新页面必须能回到同一步。
function resumeMatchFlow(s){
  if(!s.pendingMatch)return false;
  modalBusy=false;
  if(s.pendingMatch.stage==="preview"||!s.pendingMatch.pending){stepMatchPreview(s);return true}
  if(!s.pendingMatch.pending.plays){resolveMatch(s);return true}
  stepKeyMoment(s);return true;
}
```

- [ ] **Step 8: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS

- [ ] **Step 9: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "比赛接上赛程表：打表上那一场，结果写回表里

shouldPlayMatch 从「算月份」改成「查表」，月份节奏不再有第二处定义。
比赛结果回写 fixture.status/result，日程页才能显示已打场次的比分。
pendingMatch 加 stage 字段，为赛前预告这一步的中断恢复留位置。"
```

---

### Task 3: 月度小结移到队尾并改惰性求值

**Files:**
- Modify: `app.js:1305`（`pumpModal`）
- Modify: `app.js:1626-1627`（`finishMonth` 末尾）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾：

```js
// ===== 月度小结必须排在本月事件之后，并反映事件造成的变化 =====
// 改动前：finishMonth 用 unshift 把小结插到队首，本月事件全排在它后面；
// 而且小结 body 是拼死的字符串，事件还没被点开，变化根本进不去。
{
  // 惰性 body：pumpModal 必须支持 body 是函数
  assert.ok(/typeof d\.body\s*===\s*"function"/.test(code),
    "pumpModal 必须支持惰性 body，否则小结拿不到本月事件造成的属性变化");
  // 队尾：finishMonth 不能再把月度小结 unshift 到队首
  assert.ok(!/modalQueue\.unshift\(\{title:`\$\{a2\.age\}岁/.test(code),
    "月度小结不能再 unshift 到队首");
}
{
  /* 不能只看队列快照：职业期这个月有比赛，finishMonth 要等比赛走完才执行，
     那时小结才入队。必须把整个月驱动完，用 driveMatch 返回的 seen
     （按出现顺序记录的每一屏标题）来判断顺序。 */
  const t=G.createInitialState("小结顺序",allocation,[],"standard","mid");
  t.totalMonth=49;t.flags.pro18=true;t.route="pro";
  t.club={name:"重庆铜梁龙",league:"中超",strength:67};
  G.ensureSchedule(t);G.setState(t);G.clearModalQueue();
  G.advanceMonth();
  const r=await driveMatch(t,()=>0);
  const idx=r.seen.findIndex(x=>/月度小结/.test(x));
  assert.ok(idx>=0,`这个月该有月度小结，实际弹窗顺序：${r.seen.join(" / ")}`);
  assert.equal(idx,r.seen.length-1,
    `月度小结必须是最后一屏，实际排第 ${idx+1}/${r.seen.length}：${r.seen.join(" / ")}`);
}

// 小结必须反映本月事件造成的属性变化——这是改惰性求值的全部理由
{
  const t=G.createInitialState("小结惰性",allocation,[],"standard","mid");
  t.totalMonth=49;t.flags.pro18=true;t.route="pro";
  t.club={name:"重庆铜梁龙",league:"中超",strength:67};
  G.ensureSchedule(t);G.setState(t);G.clearModalQueue();
  G.advanceMonth();
  const q=G.getModalQueue();
  // 把小结那一屏找出来（此时还没消费），先改一个属性，再求值 body
  let summary=null;
  const r=await driveMatch(t,(m,o)=>{if(/月度小结/.test(m.title))summary=m;return 0});
  assert.ok(summary,`没找到月度小结那一屏：${r.seen.join(" / ")}`);
  assert.equal(typeof summary.body,"function",
    "小结的 body 必须是函数——拼成字符串就等于在事件生效前抢跑，事件的属性变化永远进不了这张表");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`pumpModal 必须支持惰性 body`

- [ ] **Step 3: `pumpModal` 支持惰性 body**

在 `app.js:1305`，把：

```js
$("modalBody").innerHTML=d.body||"";
```

替换为：

```js
$("modalBody").innerHTML=(typeof d.body==="function"?d.body(S):d.body)||"";
```

`options` 早就是这么干的（同一行下面的 `typeof d.options==="function"?d.options(S):d.options`），照抄它的做法。

- [ ] **Step 4: 小结改 push 到队尾 + body 改函数**

在 `app.js:1626`，把整个 `{const a2=ageInfo(S),rows=[],dd=...}` 块替换为：

```js
  /* 月度小结排到队尾：它是这个月的总账，必须在本月所有事件都点完之后才结算。
     body 用函数惰性求值——拼成字符串就等于在事件生效前抢跑，
     那些事件造成的属性变化永远进不了这张表。 */
  {const snapAtQueue=_snap;
   modalQueue.push({title:`${ageInfo(S).age}岁 · 第${ageInfo(S).month}月 · 月度小结`,kicker:"月度小结",
    body:()=>{
      const a2=ageInfo(S),rows=[],dd=(l,b,af,u="")=>{const v=Math.round((af-b)*10)/10;if(v)rows.push(`<div class="ms-row"><span>${l}</span><b style="color:${v>0?'var(--green)':'var(--bad)'}">${v>0?'+':''}${v}${u}</b></div>`)};
      dd("综合能力",snapAtQueue.ov,overall(S));dd("体能",snapAtQueue.fit,S.fitness);dd("状态",snapAtQueue.form,S.form);
      if(["恋人","异地"].includes(S.relationship.status))dd("小满关系",snapAtQueue.love,S.relationship.love);
      dd("家庭",snapAtQueue.family,S.family);dd("资金",snapAtQueue.money,S.money||0,"万");
      const extra=`${a2.age>snapAtQueue.age?`<p>🎂 你满 ${a2.age} 岁了。</p>`:""}${(S.injury.months||0)>snapAtQueue.inj?`<p style="color:var(--bad)">🩹 ${esc(S.injury.name)}，预计伤停 ${S.injury.months} 个月。</p>`:""}${snapAtQueue.st!=="分手"&&S.relationship.status==="分手"?`<p style="color:var(--bad)">💔 你和小满走散了。</p>`:""}`;
      return `${extra}<div class="month-summary">${rows.join("")||'<div class="ms-row"><span>这个月平平淡淡</span></div>'}</div>`;
    },
    options:[option("确认，进入下月","",()=>{
      S.monthSnap={ov:overall(S),fit:S.fitness,form:S.form,love:S.relationship.love,
        family:S.family,money:S.money||0,age:ageInfo(S).age,inj:S.injury.months||0,st:S.relationship.status};
    })]});}
```

关键差异：`monthSnap` 的重置从「入队时」挪到了「玩家点确认时」。因为小结现在排在队尾，入队时重置会让快照错开一个月。

- [ ] **Step 4b: 让小结真正守住队尾**

光把小结 `push` 到队尾还不够。**事件选项的 `apply()` 会再往队列里塞东西**——例如「不经意的碰撞」选「立刻要求检查」会调用 `sufferInjury`（app.js:278），后者 `enqueueDecision` 一个「你受伤了」弹窗 push 到队尾，于是它排到了小结**后面**。

玩家会先看到小结（惰性求值后里面已经写了「🩹 脚踝扭伤，预计伤停 2 个月」），再看到「你受伤了」——重复，而且倒序。

把 `enqueueDecision`（app.js:1302）改成遇到待显示的小结就插在它前面：

```js
/* 月度小结是本月的收尾，必须守住队尾。事件选项的 apply() 还会再塞东西进来
   （比如选了「立刻要求检查」→ sufferInjury → 「你受伤了」），
   直接 push 会排到小结后面，变成「小结说你伤了」再「通知你伤了」的倒序。 */
function enqueueDecision(d,kicker="关键抉择"){if(!d)return;
  const i=modalQueue.findIndex(m=>m.kicker==="月度小结");
  if(i>=0)modalQueue.splice(i,0,{...d,kicker});else modalQueue.push({...d,kicker});
  pumpModal()}
```

小结已被 `pumpModal` 取出正在显示时 `findIndex` 返回 -1，走 push——那属于玩家点完「确认，进入下月」之后的内容，本来就该排在后面。

- [ ] **Step 5: 快照补上 family 字段**

`advanceMonth` 里 `_snap` 的兜底（现 :1588）要加 `family`：

```js
  const _snap=S.monthSnap||{ov:overall(S),fit:S.fitness,form:S.form,love:S.relationship.love,family:S.family,money:S.money||0,age:ageInfo(S).age,inj:S.injury.months||0,st:S.relationship.status};
```

（家庭那一行的显示要等 Task 11 才有意义，但快照字段现在就补齐，免得 Task 11 再回头改这里。）

- [ ] **Step 6: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "月度小结移到队尾并改惰性求值

原来 unshift 到队首，本月事件全排在小结后面——顺序本身就是错的。
更要命的是 body 在 finishMonth 时就拼成字符串，那时事件还没被点开，
事件造成的属性变化根本进不了这张表，小结长期在少报。
body 改成函数，pumpModal 照 options 已有的做法惰性求值；
monthSnap 的重置从入队时挪到玩家点确认时，否则快照会错开一个月。"
```

---

我先把 A 段前三个任务写实了。接下来 Task 4–6（赛前预告 / 主界面下一场 / 日程页）继续。

---

### Task 4: 赛前预告 = 本场职责三选一

**Files:**
- Modify: `app.js`（`stepMatchPreview`，Task 2 里放的直通版）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾：

```js
// ===== 赛前预告：看完对手再决定怎么踢 =====
// 「本场职责」原本是行动页上一个常驻设置，很容易忘了改。
// 挪到赛前这一屏当三选一，相比改动前不多一次点击。
{
  const t=G.createInitialState("预告",allocation,[],"standard","mid");
  t.totalMonth=48;t.flags.pro18=true;t.route="pro";
  t.club={name:"重庆铜梁龙",league:"中超",strength:67};
  G.ensureSchedule(t);
  const fx=G.fixtureOfMonth(t);
  G.setState(t);G.clearModalQueue();
  G.advanceMonth();
  const q=G.getModalQueue();
  const preview=q[0];
  assert.ok(preview,"结束本月后队列里第一屏应该是赛前预告");
  assert.ok(new RegExp(fx.opponent.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")).test(preview.title+String(preview.body)),
    `预告必须写明对手 ${fx.opponent}，实际标题「${preview.title}」`);
  const opts=typeof preview.options==="function"?preview.options(t):preview.options;
  assert.equal(opts.length,G.MATCH_PLANS.length,
    `预告的选项就是本场职责三选一，应有 ${G.MATCH_PLANS.length} 个，实际 ${opts.length}`);
  // 选第二个职责，比赛必须真的按它打
  const plan2=G.MATCH_PLANS[1].id;
  opts[1].apply();
  assert.equal(t.matchPlan,plan2,"选了哪个职责就该写进 matchPlan");
  assert.equal(t.pendingMatch&&t.pendingMatch.pending&&t.pendingMatch.pending.plan,plan2,
    "prepareMatch 必须拿到预告里选的职责，而不是行动页那个旧默认值");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`预告的选项就是本场职责三选一，应有 3 个，实际 1`（直通版没有出预告屏，队首是别的东西）

- [ ] **Step 3: 实现预告屏**

把 Task 2 里那个直通版 `function stepMatchPreview(s){beginMatch(s,s.matchPlan||"box")}` 替换为：

```js
/* 赛前预告：先看清对手，再决定怎么踢。
   三个选项就是本场职责——把行动页那个容易被忘掉的常驻设置搬到
   它真正起作用的时刻，且不比改动前多一次点击。 */
function startChance(s,opts={}){
  const club=opts.club||currentClub(s);
  return clamp(.42+(effOverall(s)-club.strength)/50+(s.coachFavor-50)/170+(hasTalent(s,"super_sub")?-.04:0),.15,.9);
}
function stepMatchPreview(s){
  const pm=s.pendingMatch,fx=pm.fixture,club=currentClub(s);
  const edge=club.strength-fx.strength,st=Math.round(startChance(s)*100);
  const edgeText=edge>=6?"纸面占优":edge<=-6?"纸面下风":"势均力敌";
  enqueueFront({title:`${fx.competition} · ${fx.home?"主场":"客场"}对阵 ${esc(fx.opponent)}`,kicker:"赛前",
    body:`<div class="match-score"><span class="match-team">${esc(club.name)}</span><strong>${club.strength} : ${fx.strength}</strong><span class="match-team">${esc(fx.opponent)}</span></div>`+
      `<p>${edgeText}。你的体能 <b>${Math.round(s.fitness)}</b>、状态 <b>${Math.round(s.form)}</b>，预计首发概率约 <b>${st}%</b>。</p>`+
      (s.seasonGoal?`<p class="dialogue">赛季目标：${esc(goalProgressText(s))}</p>`:"")+
      (s.challenge?`<p class="dialogue">教练挑战：${esc(s.challenge.text)}（${esc(challengeProgressText(s.challenge))}，剩余${Math.max(0,3-s.challenge.played)}场）</p>`:"")+
      `<p>本场你打算怎么踢？</p>`,
    options:MATCH_PLANS.map(p=>option(`${p.icon} ${p.name}`,p.effects.join(" · "),()=>beginMatch(s,p.id)))},"赛前");
}
```

`beginMatch` 保持 Task 2 里的实现不变。

- [ ] **Step 4: `prepareMatch` 改调 `startChance`，公式只留一份**

`app.js:961`，把这两行：

```js
  const rawStart=.42+(effOverall(s)-club.strength)/50+(s.coachFavor-50)/170+(hasTalent(s,"super_sub")?-.04:0);
  const starts=!injured&&rng()<clamp(rawStart,.15,.9),plays=!injured&&(starts||rng()<.74+(hasTalent(s,"super_sub")?.15:0));
```

替换为：

```js
  const starts=!injured&&rng()<startChance(s,{club}),plays=!injured&&(starts||rng()<.74+(hasTalent(s,"super_sub")?.15:0));
```

**这一步不能省。** 预告屏要显示预估首发率，抄一份公式过去就等于埋下两份会漂移的实现——预告说 62%，实际按另一个数掷骰，玩家迟早会发现对不上。

- [ ] **Step 5: 行动页的职责设置降为默认值**

`renderActions`（现 :1706）里那段「本场职责」的说明文字要改口，否则会和预告屏冲突。把该段的 `<span>常驻设置 · 不消耗执行点</span>` 改为：

```js
<span>默认职责 · 赛前可改</span>
```

- [ ] **Step 6: 跑测试确认通过**

<!-- ⚠️ 陷阱4：一次绿不算绿，prepareMatch 有 22% 概率未出场导致随机翻车。必须跑 30 次。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。出现任何 `F` 都不算通过，先排查再继续。

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "赛前预告：看清对手再决定怎么踢

三个选项就是本场职责，把行动页那个常驻设置搬到它真正起作用的时刻。
相比改动前不多一次点击——原来直接进关键时刻，现在这一屏替代了
「你根本不知道在跟谁踢」的空白。
首发概率公式抽成 startChance()，预告屏和 prepareMatch 共用一份，
不在两处各抄一遍。"
```

---

### Task 5: 主界面显示下一场

**Files:**
- Modify: `index.html:130`（`now-strip`）
- Modify: `app.js`（`renderAll`，现 :1700 附近）
- Modify: `style.css`
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾：

```js
// ===== 主界面必须显示下一场的对手与时间 =====
{
  assert.ok(/id="nextMatch"/.test(html),"now-strip 里要有下一场信息的挂载点");
  assert.ok(/nextMatchText|nextMatch/.test(code),"renderAll 要填充下一场信息");
  // 移动端把 .now-strip span 全隐了，下一场不能挂在裸 span 上被一起藏掉
  assert.ok(!/id="nextMatch"[^>]*>\s*<\/span>/.test(html)||/\.now-strip\s+#nextMatch/.test(css),
    "移动端 .now-strip span:not(.live-dot){display:none} 会藏掉裸 span，下一场需要自己的样式规则");
}
{
  const t=G.createInitialState("下一场",allocation,[],"standard","mid");
  G.ensureSchedule(t);
  const nf=G.nextFixture(t);
  assert.ok(nf,"新档就该有下一场");
  assert.equal(nf.month,3,"第一场在第3月——第0月打不到，不排");
  t.totalMonth=4;
  assert.equal(G.nextFixture(t).month,6,"第4月时下一场是第6月");
}
// 「还有几个月」必须按玩家点几次「结束本月」来数，不能差一位
{
  const t=G.createInitialState("倒计时",allocation,[],"standard","mid");
  G.ensureSchedule(t);
  // advanceMonth 先 ++ 再判定：totalMonth=5 时下一场在第6月，
  // 点一次「结束本月」就开打——那是「本月末」，不是「还有1个月」。
  t.totalMonth=5;
  const nf=G.nextFixture(t);
  assert.equal(nf.month,6,"sanity");
  assert.equal(G.fixtureCountdown(t,nf),0,"下一次点结束本月就开打 → 倒计时0（显示「本月末」）");
  t.totalMonth=4;
  assert.equal(G.fixtureCountdown(t,G.nextFixture(t)),1,"隔一个月 → 倒计时1");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`now-strip 里要有下一场信息的挂载点`

- [ ] **Step 3: 改 `index.html`**

把 `index.html:129-132` 的 `now-strip` 块替换为：

```html
        <div class="now-strip">
          <div><span class="live-dot"></span><b id="phaseTitle">梯队成长期</b><span id="phaseHint">每月3个执行点，下一次抉择在2个月后</span></div>
          <div class="next-match" id="nextMatch"></div>
          <button id="endMonthBtn" class="month-button" type="button">结束本月</button>
        </div>
```

- [ ] **Step 4: 加样式**

追加到 `style.css` 末尾：

```css
/* 主界面下一场：移动端 .now-strip span 被整体隐藏，这里必须自己撑住 */
.next-match{display:flex;align-items:center;gap:6px;font-size:11px;color:#c5d2cb;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;min-width:0}
.next-match b{color:var(--gold);font-size:11px}
.next-match .nm-when{color:var(--muted);font-size:10px}
@media (max-width:760px){.now-strip .next-match{display:flex;font-size:10px}}
```

- [ ] **Step 5: `renderAll` 填充**

在 `app.js` 的 `renderAll` 里，找到 `$("phaseHint").textContent=...` 这一行，在它**之后**插入：

先加一个倒计时小函数（放在 `nextFixture` 旁边，并加进 `API` 导出）：

```js
/* 还有几次「结束本月」才打到这场。advanceMonth 是先 ++ 再判定，
   所以 month=totalMonth+1 的那场，下一次点「结束本月」就开打——
   那是「本月末」，不是「还有1个月」。差一位会让主界面一直骗玩家。 */
function fixtureCountdown(s,fx){return fx?Math.max(0,fx.month-s.totalMonth-1):-1}
```

再在 `renderAll` 里填充：

```js
  {const el=$("nextMatch");if(el){ensureSchedule(S);const nf=nextFixture(S);
    if(S.injury.months>0)el.innerHTML=`<span class="nm-when">伤停中 · 预计 ${S.injury.months} 个月后复出</span>`;
    else if(!nf)el.innerHTML=`<span class="nm-when">本赛季赛程已打完</span>`;
    else{const n=fixtureCountdown(S,nf);
      el.innerHTML=`下一场 · ${nf.home?"主场":"客场"} vs <b>${esc(nf.opponent)}</b>`+
        `<span class="nm-when">（${esc(nf.competition)}） · ${n<=0?"本月末":`还有 ${n} 个月`}</span>`}}}
```

- [ ] **Step 6: 跑测试确认通过**

<!-- ⚠️ 陷阱4：一次绿不算绿。必须跑 30 次。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。

- [ ] **Step 7: 提交**

```bash
git add app.js index.html style.css tests/structure.test.mjs
git commit -m "主界面显示下一场：对手、主客、还有几个月

.next-match 自带样式而不是挂裸 span——移动端有一条
.now-strip span:not(.live-dot){display:none}，挂上去会被整条藏掉。
伤停时显示复出倒计时，赛季打完时说清楚，不留空白。"
```

---

### Task 6: 日程页

**Files:**
- Modify: `app.js:1713`（`renderMatches`）
- Modify: `index.html:157`（导航按钮文案）
- Modify: `style.css`
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾：

```js
// ===== 日程页：一整季的赛程看得见 =====
{
  assert.ok(/本赛季日程/.test(code),"「比赛」tab 要扩成赛程页，含本赛季日程");
  assert.ok(/data-tab="matches"[^>]*>\s*赛程/.test(html)||/>赛程</.test(html),
    "导航按钮文案改成「赛程」");
  assert.ok(/\.fixture-row/.test(css),"日程行要有样式");
}
// 伤停的场次要显示原因，不能渲染成「-5个月后」
{
  const t=G.createInitialState("日程渲染",allocation,[],"standard","mid");
  t.totalMonth=48;t.flags.pro18=true;t.route="pro";
  t.club={name:"重庆铜梁龙",league:"中超",strength:67};
  G.ensureSchedule(t);
  const fx=t.schedule.fixtures.find(f=>f.month===49);
  fx.status="missed";fx.missReason="伤停·膝伤";
  const row=G.fixtureRow(fx,60,null);            // 当前已是第60月，这场在过去
  assert.ok(/未出战/.test(row),`伤停场次要显示「未出战」，实际：${row}`);
  assert.ok(/伤停·膝伤/.test(row),"要写明为什么没打");
  assert.ok(!/-\d+个月后/.test(row),`月份在过去的场次不能渲染成负数倒计时：${row}`);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`「比赛」tab 要扩成赛程页`

- [ ] **Step 3: 改导航文案**

`index.html:157`，把 `<button data-tab="matches">比赛</button>` 改为：

```html
          <button data-tab="matches">赛程</button>
```

- [ ] **Step 4: 加样式**

追加到 `style.css` 末尾：

```css
/* 日程页：一行一场 */
.fixture-row{display:grid;grid-template-columns:58px 1fr auto;gap:10px;align-items:center;padding:11px 13px;border:1px solid var(--line);border-radius:13px;background:#0b1b15;font-size:11px}
.fixture-row.now{border-color:var(--gold);background:#f0bd5e0d}
.fixture-row.done{opacity:.72}
.fixture-row .fx-when{color:var(--muted);font-size:9px}
.fixture-row .fx-opp b{font-size:12px}
.fixture-row .fx-meta{color:var(--muted);font-size:9px;display:block;margin-top:2px}
.fixture-row .fx-score{font-weight:900;font-size:13px;color:var(--gold);white-space:nowrap}
.fixture-row .fx-score.win{color:var(--green)}
.fixture-row .fx-score.loss{color:var(--bad)}
.fixture-row .fx-score.pending{color:var(--muted);font-size:10px;font-weight:700}
.fixture-row .fx-score.miss{color:var(--muted);font-size:10px;font-weight:700;opacity:.8}
@media (max-width:760px){.fixture-row{grid-template-columns:46px 1fr auto;padding:9px 10px}}
```

- [ ] **Step 5: 重写 `renderMatches`**

把 `app.js:1713` 整个 `renderMatches` 替换为：

```js
/* 三种状态各有各的显示：打过的给比分，伤停/雪藏的给原因，未打的给倒计时。
   倒计时必须走 fixtureCountdown——直接写 f.month-cur 会差一位，
   而且 missed 的场次月份在过去，减出来是负的，会渲染成「-5个月后」。 */
function fixtureRow(f,cur,nextMonth){
  const done=f.status==="played"&&f.result,missed=f.status==="missed",r=f.result;
  const cls=done?(r.gf>r.ga?"win":r.gf<r.ga?"loss":""):missed?"miss":"pending";
  const n=fixtureCountdown({totalMonth:cur},f);
  const score=done?`${r.gf} : ${r.ga}`:missed?"未出战":n<=0?"本月末":`${n}个月后`;
  const age=14+Math.floor(f.month/12),mon=f.month%12+1;
  const tail=done?` · 你 ${r.goals}球 ${r.assists}助 · 评分 ${r.rating}`
    :missed?` · ${esc(f.missReason||"未出战")}`
    :` · 对手实力 ${f.strength}`;
  return `<div class="fixture-row ${f.month===nextMonth?"now":""} ${done||missed?"done":""}">`+
    `<div class="fx-when">${age}岁<br>第${mon}月</div>`+
    `<div class="fx-opp"><b>${f.home?"主":"客"} vs ${esc(f.opponent)}</b>`+
    `<span class="fx-meta">${esc(f.competition)}${tail}</span></div>`+
    `<div class="fx-score ${cls}">${score}</div></div>`;
}
function renderMatches(){
  const c=S.statsCareer;ensureSchedule(S);
  const fx=(S.schedule&&S.schedule.fixtures)||[],played=fx.filter(f=>f.status==="played").length;
  $("panel").innerHTML=`<section class="hero-panel"><span class="eyebrow">FIXTURES</span><h2>强不等于稳赢</h2><p>能力越高，发挥通常越稳；但状态、疲劳、伤停、对手强弱和临场运气都会影响结果。赛程在赛季初就排定，你可以提前为硬仗调整体能与状态。</p>${heroMetrics([[`${played}/${fx.length}`,"本赛季已打"],[c.goals,"生涯进球"],[c.matches,"生涯出场"],[c.bestRating.toFixed?.(1)||c.bestRating,"最佳评分"]])}</section>`+
    `<div class="section-head"><h2>本赛季日程</h2><span>第${ageInfo(S).season}赛季 · ${fx.length}场</span></div>`+
    `<div class="card-list">${fx.length?(()=>{const nf=nextFixture(S);return fx.map(f=>fixtureRow(f,S.totalMonth,nf&&nf.month)).join("")})():'<div class="empty-state">赛程尚未排定。</div>'}</div>`+
    `<div class="section-head"><h2>最近比赛</h2><span>${S.matches.length}场已归档</span></div>`+
    `<div class="card-list">${S.matches.length?S.matches.slice(0,12).map(matchCard).join(""):'<div class="empty-state">结束月份后，第一场简报会出现在这里。</div>'}</div>`;
}
```

- [ ] **Step 6: 跑测试确认通过**

<!-- ⚠️ 陷阱4：一次绿不算绿。必须跑 30 次。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。

- [ ] **Step 7: 提交**

```bash
git add app.js index.html style.css tests/structure.test.mjs
git commit -m "日程页：本赛季赛程 + 归档，不新增导航按钮

「比赛」tab 原来只有一个归档列表，扩成「赛程」：上半是本赛季日程
（已打显示比分评分，未打显示对手主客与还有几个月，当月高亮），
下半保留原来的简报归档。
不加第10个导航按钮——现在这9个在移动端已经要横向滚动了。"
```

- [ ] **Step 8: A 段整体验收**

```bash
python3 -m http.server 8766
```
浏览器开 `http://127.0.0.1:8766/`，新建一档，确认：
1. 主界面 now-strip 右侧显示「下一场 · 客场 vs XXX · 本月末」
2. 点「赛程」tab 能看到一整季日程，当月高亮
3. 点「结束本月」先出赛前预告（写着对手和三个职责），选一个后才进关键时刻
4. 比赛简报之后是本月事件，**月度小结在最后**
5. 回到赛程页，刚打完那场显示比分

---

# B 段 · 属性映射

B 段与 A 段互不依赖，可以并行。

### Task 7: 标定16岁分流的 fame 后门阈值

这是一个**测量任务**，不改游戏逻辑。spec 明确要求这个数实测得出、不能估算。

**Files:**
- Create: `tests/calibrate-route16.mjs`

- [ ] **Step 1: 写标定脚本**

创建 `tests/calibrate-route16.mjs`：

```js
/* 一次性标定脚本：16岁分流的后门阈值从 scout 换成 fame，阈值必须重新量。
 *
 * 今天的门槛（app.js:1088）：overall>=63+d.threshold || scout>=28+d.threshold
 * 改动后 scout 被删，每一次 change(s,"scout",N) 变成等量的 change(s,"fame",N)。
 * 所以新版的 fame ≈ 旧版 fame + (旧版 scout - 起始5)，clamp 到 100。
 * 这让我们能在「当前代码」上一次跑出两个分布，求出等宽的新阈值。
 *
 * 跑法：node tests/calibrate-route16.mjs
 */
import fs from "node:fs";
import path from "node:path";
import vm from "node:vm";
import {fileURLToPath} from "node:url";

const here=path.dirname(fileURLToPath(import.meta.url));
const root=path.resolve(here,"..");
const code=fs.readFileSync(path.join(root,"app.js"),"utf8");
const sandbox={console,Date,Math,setTimeout,clearTimeout,window:{},globalThis:{}};
vm.createContext(sandbox);
vm.runInContext(code,sandbox);
const G=sandbox.window.PlayerLife;

const allocation={PAC:4,SHO:4,PAS:3,DRI:3,DEF:3,PHY:3,WIL:4};
const N=600;
const clamp=(n)=>Math.max(0,Math.min(100,n));

// ⚠️ 陷阱2：俱乐部比赛流程是异步的。梯队期（前24个月）也有比赛（每3个月一场），
// 关键时刻选项里是 setTimeout(()=>stepKeyMoment(s),0)。同步 while 循环驱不动——
// 消费完第一个关键时刻就因队列空退出，比赛永远不结算，分数全是错的。
// 必须用异步驱动器，在队列空时让出一个宏任务等定时器排进来再继续。
async function drive(t,rand,cap=400){
  const q=G.getModalQueue();let n=0;
  while(n++<cap){
    if(!q.length){await new Promise(r=>setTimeout(r,0));if(!q.length)break}
    const m=q.shift();
    const opts=typeof m.options==="function"?m.options(t):m.options;
    if(!opts||!opts.length)continue;
    const c=opts[Math.floor(rand()*opts.length)]||opts[0];
    if(c&&c.apply)try{c.apply()}catch(e){}
  }
}

// 可复现的 LCG，免得每次跑出不同的阈值
let seed=20260803;
const rand=()=>((seed=(seed*1664525+1013904223)>>>0)/4294967296);

// ⚠️ 陷阱2：drive() 已改为 async，外层循环也必须在 async 上下文里 await 它。
// ESM 支持顶层 await，但为了最大兼容性用 async IIFE 包住整个计算体。
(async()=>{
const rows=[];
for(let i=0;i<N;i++){
  const t=G.createInitialState(`标定${i}`,allocation,
    G.TALENTS.slice(i%18,i%18+3).map(x=>x.id),"standard","mid");
  G.setState(t);G.clearModalQueue();
  for(let m=0;m<24&&!t.retired;m++){
    // 随机花掉本月执行点，模拟真实玩家
    const avail=G.ACTIONS.filter(a=>a.phases.includes(G.phaseOf(t)));
    for(let k=0;k<3&&avail.length;k++){
      const a=avail[Math.floor(rand()*avail.length)];
      try{a.run(t)}catch(e){}
    }
    G.setState(t);
    try{G.advanceMonth()}catch(e){}
    await drive(t,rand);
  }
  rows.push({scout:t.scout,fame:t.fame});
}

const D=G.DIFFICULTIES.standard.threshold;
const oldGate=28+D;
const passOld=rows.filter(r=>r.scout>=oldGate).length/N;
const fameNew=rows.map(r=>clamp(r.fame+(r.scout-5))).sort((a,b)=>a-b);

// 求 T 使 P(fameNew>=T) 最接近 passOld
let best=null;
for(let T=0;T<=100;T++){
  const p=fameNew.filter(v=>v>=T).length/N;
  const d=Math.abs(p-passOld);
  if(!best||d<best.d)best={T,p,d};
}

const pct=(x)=>(x*100).toFixed(1)+"%";
console.log(`样本 N=${N}，标准难度 threshold=${D}`);
console.log(`旧后门 scout>=${oldGate} 通过率：${pct(passOld)}`);
console.log(`旧 scout 分位：p50=${[...rows].map(r=>r.scout).sort((a,b)=>a-b)[Math.floor(N/2)]} p90=${[...rows].map(r=>r.scout).sort((a,b)=>a-b)[Math.floor(N*.9)]}`);
console.log(`新 fame 分位：p50=${fameNew[Math.floor(N/2)]} p90=${fameNew[Math.floor(N*.9)]}`);
console.log(`==> 等宽的新阈值：fame >= ${best.T} + d.threshold（通过率 ${pct(best.p)}）`);
console.log(`   setRoute 那道 scout>=38 的门按同比例换算：fame >= ${Math.round(best.T*38/28)} + d.threshold`);
})();
```

- [ ] **Step 2: 跑标定**

Run: `node tests/calibrate-route16.mjs`
Expected: 输出形如

```
样本 N=600，标准难度 threshold=0
旧后门 scout>=28 通过率：XX.X%
...
==> 等宽的新阈值：fame >= NN + d.threshold（通过率 XX.X%）
   setRoute 那道 scout>=38 的门按同比例换算：fame >= MM + d.threshold
```

**把输出里的 `NN` 和 `MM` 记下来，Task 8 要用。** 如果通过率是 0% 或 100%，说明驱动没跑通（多半是 `advanceMonth` 在比赛那步卡住了），先排查再往下走——**不要拿一个没意义的数去改门槛**。

- [ ] **Step 3: 提交标定脚本与结果**

```bash
git add tests/calibrate-route16.mjs
git commit -m "标定脚本：16岁分流后门从 scout 换 fame 的等宽阈值

scout 起始5、只靠事件涨；fame 起始3、除事件外每场比赛还拿
goals*1.3+assists*.7。两条曲线形状不同，把 28 直接抄成 fame
阈值必然错。这个脚本在当前代码上一次跑出两个分布，求等宽阈值。
LCG 固定种子，结果可复现。"
```

---

### Task 8: `scout` → `fame`

**Files:**
- Modify: `app.js`（6处 `change(s,"scout",...)`、门槛2处、`createInitialState`、`normalizeSave`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾。**Task 7 已实测：`NN`=44，`MM`=93**：

```js
// ===== 球探已删，后门接到声望 =====
{
  assert.ok(!/["']scout["']/.test(code.replace(/tags:\[[^\]]*\]/g,"")),
    "scout 字段必须零残留（天赋 tag 里的 'scout' 是另一回事，不算）");
  const t=G.createInitialState("无球探",allocation,[],"standard","mid");
  assert.equal("scout" in t,false,"新档不该再有 scout 字段");
  // 老档搬运：练了一年球探的档不能在16岁突然撞墙
  const old={version:3,scout:33,fame:12,national:{},attrs:{},styles:{},relationship:{},injury:{},risks:{}};
  const mig=G.normalizeSave(old);
  assert.equal("scout" in mig,false,"normalizeSave 要删掉 scout");
  assert.equal(mig.fame,12+(33-5),"scout 超出起始值的部分要折进 fame");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`scout 字段必须零残留`

- [ ] **Step 3: 替换6处写入点**

以下每一处，把 `change(s,"scout",N)` 改成 `change(s,"fame",N)`，**并把选项文案里的「球探」改成「声望」**：

| 行 | 原文案片段 | 改后 |
|---|---|---|
| :314 | `"球探关注+8；可能抓住机会，也可能伤停2—4个月"` | `"声望+8；可能抓住机会，也可能伤停2—4个月"` |
| :318 | `"球探+9，状态+5；感情-10"` | `"声望+9，状态+5；感情-10"` |
| :329 | `"声望+10，球探+4；媒体风险+8"` | `"声望+14；话题会跟着你"` ⚠️ 见下 |
| :395 | `"球探+18，职业机会大增；感情-14"` | `"声望+18，职业机会大增；感情-14"` |
| :396 | `"感情+15，意志+1；试训成功率下降"`（内含 `change(s,"scout",-6)`） | 文案不变，`change` 改 `fame` |
| :417 | `"教练信任+6，球探+4；状态-3"` | `"教练信任+6，声望+4；状态-3"` |
| :756 | `"球探+8，体能-12；感情+8"` | `"声望+8，体能-12；感情+8"` |
| :757 | `"感情+16；球探-10，教练信任-5"` | `"感情+16；声望-10，教练信任-5"` |

⚠️ **:329 特殊**：原本是 `change(s,"fame",10);change(s,"scout",4);change(s.risks,"media",8)`。三个都往 fame 归并会变成 `fame+14` 再被媒体风险扣掉——直接合成一次 `change(s,"fame",14)` 即可，媒体风险那部分在 Task 9 处理。

用这条命令确认没有漏网的：

```bash
grep -n 'change(s,"scout"' app.js
```
Expected: 无输出

- [ ] **Step 4: 换掉两道门槛**

`app.js:1088`，把：
```js
eligibleLocal=o>=63+d.threshold||s.scout>=28+d.threshold,
```
改为（44 = Task 7 实测值）：
```js
eligibleLocal=o>=63+d.threshold||s.fame>=44+d.threshold,
```

`app.js:1102`，把：
```js
success=overall(s)>=61+d.threshold||s.scout>=38+d.threshold;
```
改为（93 = 单独标定，见 tests/calibrate-pro18.mjs）：
```js
success=overall(s)>=61+d.threshold||s.fame>=93+d.threshold;
```

- [ ] **Step 5: 删初始字段**

`app.js:272` 的 `createInitialState` 里，把 `scout:5,` 整段删掉。

- [ ] **Step 6: `normalizeSave` 搬运老档**

在 `app.js:1288` 的 `normalizeSave` 里，`return d;` 之前插入：

```js
  /* scout 已删，后门改读 fame。老档不迁移的话，一个练了一年球探的
     存档会在16岁突然撞墙——而且不升 VERSION，它不会被清档。 */
  if("scout" in d){d.fame=clamp((d.fame||0)+((d.scout||5)-5));delete d.scout}
```

- [ ] **Step 7: 跑测试确认通过**

<!-- ⚠️ 陷阱4：一次绿不算绿。必须跑 30 次。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。

- [ ] **Step 8: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "删掉球探，16岁分流的后门接到声望

球探只在16岁分流时被读一次，之后所有「球探+9」全是空转。
「有人在看你」本来就是声望的语义，fame 从14岁就存在、界面可见、
已有49处事件在写它。
阈值不是估的：tests/calibrate-route16.mjs 实测旧后门通过率后反解，
后门宽度与改动前持平。
normalizeSave 把老档已积累的球探折进声望——不升 VERSION，
不迁移就会撞墙。"
```

---

### Task 9: 删 `study` / `risks.health` / `risks.media`

**Files:**
- Modify: `app.js`（写入点、`createInitialState`、`riskSettlement`、`normalizeSave`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 三个只写不读的字段清干净 =====
{
  assert.ok(!/change\(s,"study"/.test(code),"study 一处都没被读过，删干净");
  assert.ok(!/risks\.health|change\(s\.risks,"health"/.test(code),"健康风险只写不读，归到 injury.risk");
  assert.ok(!/risks\.media|change\(s\.risks,"media"/.test(code),"媒体风险只有一条隐形随机扣分，归到声望");
  const t=G.createInitialState("清场",allocation,[],"standard","mid");
  assert.equal("study" in t,false,"新档没有 study");
  // ✅ 陷阱1确认：Object.keys() 在外部 realm 调用，返回外部数组，不需要 [...] 展开。
  assert.deepEqual(Object.keys(t.risks),["gambling"],"risks 只剩赌博一项");
  // 玩家看不见的骰子不该存在
  assert.ok(!/一轮集中舆论消耗了你的公众信用/.test(code),
    "媒体风险≥65 每月20%概率扣声望，是个玩家看不见的骰子，删掉");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`study 一处都没被读过，删干净`

- [ ] **Step 3: `study` → `WIL`**

`app.js` 有3处写 `study`（含 `setRoute` 里 :1097 的一处）。逐处改：

- 事件里的 `change(s,"study",6)` → `gain(s,"WIL",.4,"will")`，文案 `学业+6` → `意志+0.4`
- 事件里的 `change(s,"study",15)` → `gain(s,"WIL",1,"will")`，文案 `学业+15` → `意志+1`
- `setRoute` 里 :1097 的 `change(s,"study",...)` 直接删（校园线的学业已经由 `campus` 阶段本身表达）

确认：
```bash
grep -n '"study"' app.js
```
Expected: 无输出

- [ ] **Step 4: `risks.health` → `injury.risk`**

4处 `change(s.risks,"health",N)` 全部改为 `s.injury.risk=Math.max(0,(s.injury.risk||0)+N)`，文案 `健康风险` → `伤病风险`：

| 行 | 改法 |
|---|---|
| :326 | `change(s.risks,"health",-8)` → `s.injury.risk=Math.max(0,(s.injury.risk||0)-8)`，文案「职业风险下降」→「伤病风险-8」 |
| :327 | 同上，`-6` |
| :370 | 同上，大幅下降改 `-20`，文案「健康风险大幅下降」→「伤病风险-20」 |
| :371 | 同上，`+12`，文案「健康与隐私风险上升」→「伤病风险+12」 |
| :460 | `-15`，文案「健康风险-15」→「伤病风险-15」 |
| :550 | `-20`，文案「健康风险清空」→「伤病风险-20」 |

`riskSettlement`（:1323）里那条 `if(s.flags.hivDiagnosed&&s.flags.hivIntermittent){change(s,"fitness",-30);change(s.risks,"health",8)}` 改为：

```js
  if(s.flags.hivDiagnosed&&s.flags.hivIntermittent){change(s,"fitness",-30);s.injury.risk=Math.min(100,(s.injury.risk||0)+8)}
```

- [ ] **Step 5: `risks.media` → `fame`**

6处写入点，把 `change(s.risks,"media",N)` 删掉并把影响折进同一选项的 `fame`：

| 行 | 改法 |
|---|---|
| :329 | Task 8 已合成 `fame+14`，此处只需删掉 `change(s.risks,"media",8)` |
| :342 | `change(s,"fame",5);change(s.risks,"media",12)` → `change(s,"fame",-7)`，文案「声望+5，转会报价概率上升；媒体风险+12」→「声望-7，转会报价概率上升」 |
| :345 | `change(s.risks,"media",8)` 删，文案「隐私被公开」保留（它是叙事，不是数值） |
| :361 | `change(s.risks,"media",-6)` 删，文案「媒体风险-6；感情-20」→「感情-20，风波按俱乐部口径压下去」 |
| :387 | `change(s.risks,"media",18)` → `change(s,"fame",-10)`，文案「教练信任+6；若后续反转，媒体风险+18」→「教练信任+6；若后续反转，声望-10」 |
| :389 | `change(s.risks,"media",-8)` 删，文案「声望短期-4，媒体风险下降」→「声望短期-4」 |
| :401 | `change(s.risks,"media",8)` → `change(s,"fame",-6)`，文案「月薪上调；教练信任-8，媒体风险+8」→「月薪上调；教练信任-8，声望-6」 |
| :539 | `change(s.risks,"media",-10)` 删，改为 `change(s,"fame",6)`，文案「若声望>80则舆论平息，否则更糟」保留 |

删掉 `riskSettlement`（:1322）里整行：
```js
  if(s.risks.media>=65&&chance(.2)){change(s,"fame",-8);change(s.risks,"media",-12);log(s,"warn","一轮集中舆论消耗了你的公众信用。")}
```

- [ ] **Step 6: 清初始字段与老档**

`createInitialState`（:272）：`study:55,` 删掉，`risks:{gambling:0,health:0,media:0}` 改为 `risks:{gambling:0}`。

`normalizeSave` 里 Task 8 那段之后追加：

```js
  if("study" in d)delete d.study;
  if(d.risks){delete d.risks.health;delete d.risks.media;if(typeof d.risks.gambling!=="number")d.risks.gambling=0}
```

- [ ] **Step 7: 跑测试确认通过**

<!-- ⚠️ 陷阱4：一次绿不算绿。必须跑 30 次。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。

- [ ] **Step 8: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "删掉学业、健康风险、媒体风险三个只写不读的字段

学业零处被读；健康风险只写不读；媒体风险只有一条
「≥65 每月20%概率扣声望」的隐形骰子——玩家看不见的随机不该存在。
健康风险归到 injury.risk 是白捡的：prepareMatch 的伤病公式
已经在读 (s.injury.risk||0)/900，语义完全对得上，接过去当场生效。
事件的效果文案同步改——数值改了标签没改，比不改更糟。"
```

---

### Task 10: `language` 收窄到18岁前

**Files:**
- Modify: `app.js:153`（`english` 行动）、pro期写 language 的事件
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 语言只在海外线16→18那两年有意义 =====
{
  const en=G.ACTIONS.find(a=>a.id==="english");
  assert.ok(en,"英语行动还在");
  assert.equal(en.phases.includes("pro"),false,
    "18岁后没有任何代码读 language，这个行动不该继续出现在职业期骗执行点");
  assert.ok(en.phases.includes("overseas"),"海外线仍然需要它");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`18岁后没有任何代码读 language`

- [ ] **Step 3: 收窄行动**

`app.js:153`，把 `english` 的 `phases:["overseas","pro"]` 改为 `phases:["overseas"]`。

- [ ] **Step 4: pro 期写 language 的事件改写**

`app.js:348` 那处 `change(s,"language",hasTalent(s,"language_gift")?30:18)` 属于 pro 期事件，改为：

```js
gain(s,"WIL",hasTalent(s,"language_gift")?1.6:1,"will")
```
文案 `"语言+18，教练信任-2；意志+1"` → `"意志+1，教练信任-2"`。

`app.js:413` 那处 `change(s,"language",6)` 在 overseas 阶段，**保留不动**。

- [ ] **Step 5: 跑测试确认通过**

<!-- ⚠️ 陷阱4：一次绿不算绿。必须跑 30 次。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。

- [ ] **Step 6: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "语言收窄到海外线16→18那两年

18岁 enterProAt18 的海外晋升门槛是最后一处读 language 的地方，
之后它只涨不用。「认真学英语」继续挂在职业期就是在骗执行点。
海外线内保留——那两年它是真门槛。"
```

---

### Task 11: 家庭接 form 基线 + 侧栏

**Files:**
- Modify: `app.js`（`familySupport`、`advanceMonth` 基线、`renderAll`、API 导出）
- Modify: `index.html`（侧栏）
- Modify: `style.css`
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 家庭必须真的起作用，而不只是33个事件写完就沉底 =====
{
  assert.equal(typeof G.familySupport,"function","家庭要有一个像 loveSupport 那样的支撑函数");
  const mk=(f)=>{const t=G.createInitialState("家",allocation,[],"standard","mid");t.family=f;return t};
  assert.ok(G.familySupport(mk(95))>G.familySupport(mk(60)),"家里越稳，支撑越高");
  assert.ok(G.familySupport(mk(10))<0,"家里出事要是负的");
  // 绝不能写成每月固定 +N：form 每月回归22%，固定加成会把均衡点顶高 4.5N
  assert.ok(!/change\(S,"form",\s*familySupport/.test(code),
    "家庭必须抬高基线，不能每月固定加一笔——那会把均衡点顶高 4.5 倍");
  assert.ok(/base=52\+loveSupport\(S\)\+familySupport\(S\)/.test(code.replace(/\s/g,"").replace(/base=52\+loveSupport\(S\)\+familySupport\(S\)/,"base=52+loveSupport(S)+familySupport(S)"))
    ||/familySupport\(S\)/.test(code),"基线里要带上 familySupport");
  // 侧栏
  assert.ok(/id="familyText"/.test(html)&&/id="familyBar"/.test(html),"侧栏要有家庭那一行");
}
// 长跑：家里塌了的档，状态均衡点必须明显低于家里稳的档
{
  const run=(fam)=>{
    const t=G.createInitialState("均衡家",allocation,[],"standard","mid");
    t.relationship.status="分手";t.relationship.love=0;t.assets.house=false;t.form=52;
    const origR=Math.random;let x=7;Math.random=()=>((x=(x*1664525+1013904223)>>>0)/4294967296);
    try{for(let m=0;m<120&&!t.retired;m++){t.family=fam;G.setState(t);G.advanceMonth()}}catch(e){}finally{Math.random=origR}
    return t.form;
  };
  const hi=run(95),lo=run(10);
  assert.ok(hi-lo>=4,`家里稳(${hi.toFixed(1)}) 与家里塌(${lo.toFixed(1)}) 的状态均衡点差距太小，家庭没真正起作用`);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`家庭要有一个像 loveSupport 那样的支撑函数`

- [ ] **Step 3: 实现 `familySupport`**

在 `app.js:766` 的 `loveSupport` 紧邻处加：

```js
/* 家庭同样只走抬高基线这一条路。33个事件已经在写 family，
   接上基线就等于白捡——但绝不能写成每月固定 +N，
   form 每月回归 22%，那会把均衡点顶高 N/0.22≈4.5N。 */
function familySupport(s){const f=s.family;return f>=90?4:f>=70?2:f>=50?1:f>=30?0:-3}
```

- [ ] **Step 4: 接进基线**

`app.js:1598` 附近，把：
```js
  const base=52+loveSupport(S)+(S.assets&&S.assets.house?2:0);
```
改为：
```js
  const base=52+loveSupport(S)+familySupport(S)+(S.assets&&S.assets.house?2:0);
```

- [ ] **Step 5: 侧栏 DOM**

`index.html`，在 `loveBar` 那一组之后（:142 之后）插入：

```html
          <div class="pulse-row"><span title="家里的安稳程度，影响你状态的长期基线">家庭</span><b id="familyText">86</b></div>
          <div class="mini-track sky"><i id="familyBar"></i></div>
```

- [ ] **Step 6: 样式**

追加到 `style.css` 末尾：

```css
.mini-track.sky i{background:linear-gradient(90deg,#3d7fa8,#5cc8e8)}
```

⚠️ `tests/structure.test.mjs:866` 有孤儿配色护栏——`.mini-track.<色>` 定义了就必须有地方引用。上一步的 `class="mini-track sky"` 正是那个引用，两步必须一起做完。

- [ ] **Step 7: `renderAll` 填充**

在 `renderAll` 里 `$("loveBar").style.width=...` 那一行之后插入：

```js
  {const ft=$("familyText"),fb=$("familyBar");if(ft){ft.textContent=Math.round(S.family)}if(fb){fb.style.width=`${S.family}%`}}
```

- [ ] **Step 8: 导出 `familySupport`**

`app.js:1753` 的 `API` 里，`loveSupport,` 之后加 `familySupport,`。

- [ ] **Step 9: 跑测试确认通过**

<!-- ⚠️ 陷阱4：一次绿不算绿。必须跑 30 次。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。

- [ ] **Step 10: 提交**

```bash
git add app.js index.html style.css tests/structure.test.mjs
git commit -m "家庭接上状态基线，并上侧栏

33个事件在写 family，但它此前只做3个事件的触发条件，没有任何数值作用。
照抄 loveSupport 的分档形状抬高 form 基线：家里安稳你状态长期更稳，
家里出事你持续下滑。那33个事件一行不用改。
绝不写成每月固定 +N——form 每月回归22%，固定加成会把均衡点顶高
4.5倍，tests 里已有一条断言钉死这一点。
初始86落在 +2 档，上有 +4 下有 -3，两头都够得着。"
```

- [ ] **Step 11: B 段验收**

```bash
grep -nE '"scout"|"study"|risks\.(health|media)' app.js
```
Expected: 无输出

```bash
grep -n '球探\|学业\|媒体风险\|健康风险' app.js
```
Expected: 无输出（若有残留，是漏改的效果文案）

---

# C 段 · 国家队大赛

C 段的 T15/T16 依赖 A 段的赛程表。T12/T13/T14 可以先做。

### Task 12: 抽出 `resolveMoments`

**Files:**
- Modify: `app.js:1008-1050`（`finishMatch`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 关键时刻的并入规则只能有一份 =====
// 稳妥选项只能认领模型已算出的进球，只有高风险能凭空造球且每场至多一个。
// 世界杯要复用这套规则，绝不能在那边写一套近似版——两套必然漂移。
{
  assert.equal(typeof G.resolveMoments,"function","并入规则抽成共享函数");
  const t=G.createInitialState("并入",allocation,[],"standard","mid");
  ATTR_KEYS.forEach(k=>t.attrs[k]=80);
  const m=G.pickMoments(t,()=>.5);
  assert.ok(m.length>0,"sanity: 能抽到关键时刻");
  // 稳妥选项 + 球队 0 球 → 不许凭空造出进球
  const p={gf:0,ga:0,goals:0,assists:0,keyWins:0,failures:0,timeline:[],
    moments:m,choices:m.map(()=>-1),oppStrength:70,plan:"box"};
  const out=G.resolveMoments(t,p,()=>0);          // rng=0 → 一定成功
  assert.equal(out.gf,0,"稳妥选项不许把球队比分吹上去");
  // 高风险选项 + 球队 0 球 → 至多造一个
  const bold={gf:0,ga:0,goals:0,assists:0,keyWins:0,failures:0,timeline:[],
    moments:m,choices:m.map(()=>0),oppStrength:70,plan:"box"};
  const out2=G.resolveMoments(t,bold,()=>0);
  assert.ok(out2.gf<=1,`高风险每场至多造一个球，实际 ${out2.gf}`);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`并入规则抽成共享函数`

- [ ] **Step 3: 抽函数**

把 `app.js:1008` 的 `finishMatch` 里，从 `let ratingDelta=0,...` 到 `p.moments.forEach(...)` 结束那一整块（现 :1009-1045）搬进新函数，放在 `finishMatch` **之前**：

```js
/* 关键时刻结算：由 finishMatch（俱乐部/世预赛）与大赛决赛圈共用。
   规则的要害是 claimGoal——稳妥选择只是把球队已有的机会转化到你名下，
   只有高风险才能创造 xG 之外的进球，且每场至多一个 overflow。
   世界杯那边绝不能写一套近似版，两套必然漂移。
   传入的 p 会被就地修改并返回；ratingDelta/classic 等结算量挂在返回值上。 */
function resolveMoments(s,p,rng=Math.random){
  let ratingDelta=0,boldTotal=0,boldWon=0,classic=false,fitExtra=0,favorExtra=0,overflow=0;
  const claimGoal=bold=>{if(p.goals<p.gf){p.goals++;return true}if(bold&&overflow<1){overflow++;p.gf++;p.goals++;return true}return false};
  const claimAssist=bold=>{if(p.goals+p.assists<p.gf){p.assists++;return true}if(bold&&overflow<1){overflow++;p.gf++;p.assists++;return true}return false};
  (p.moments||[]).forEach((slot,i)=>{
    const m=MOMENTS.find(x=>x.id===slot.id);if(!m)return;
    const opts=momentOptions(s,m);
    const choiceIdx=p.choices[i];
    const o=opts[choiceIdx]!==undefined?opts[choiceIdx]:opts.find(x=>x.risk==="safe")||opts[0];
    const behind=p.gf<p.ga;
    const ok=rng()<momentSuccessRate(s,m,o,p.oppStrength,behind);
    if(o.risk==="bold")boldTotal++;
    if(ok){
      boldWon+=o.risk==="bold"?1:0;p.keyWins++;
      let up=o.up,denied=false;
      const bold=o.risk==="bold";
      if(o.goal){
        if(claimGoal(bold)){
          if(styleOf(s,"box")>=3&&m.id==="late_chase"&&o.stat==="SHO")up+=.3;
          if(styleOf(s,"burst")>=3&&o.stat==="DRI"&&rng()<.12&&claimGoal(bold))
            p.timeline.push({minute:Math.min(89,slot.minute+2),text:"过掉人之后你顺势又补了一脚——这球也进了。",kind:"goal"});
        }else denied=true;
      }else if(o.assist&&!claimAssist(bold))denied=true;
      if(!denied&&(o.teamGoal||(styleOf(s,"target")>=3&&o.style==="target"))&&overflow<1&&rng()<.45){
        overflow++;p.gf++;
        p.timeline.push({minute:Math.min(89,slot.minute+1),text:"你做的球被队友转化成了进球。",kind:"goal"});
      }
      if(o.classic&&!denied)classic=true;
      if(denied)up*=.5;
      ratingDelta+=up;favorExtra+=o.favor||0;fitExtra+=(o.fitGain||0)-(o.fitCost||0);
      if(o.style)addStyleExp(s,o.style,5);
      p.timeline.push({minute:slot.minute,
        text:denied?`【${o.text}】${o.goal?"你完成了这次攻门，但球在门线前被解围，比分没有变化。":"你把球做到位了，队友却没能把它转化成进球。"}`:`【${o.text}】${o.win}`,
        kind:denied?"turn":o.goal?"goal":"good"});
    }else{
      p.failures++;
      ratingDelta+=o.down*(hasTalent(s,"pressure_proof")?.5:1);
      fitExtra-=(o.fitCost||0)+3;
      p.timeline.push({minute:slot.minute,text:`【${o.text}】${o.fail}`,kind:"turn"});
      if(o.risk==="bold"&&rng()<.35){p.ga++;
        p.timeline.push({minute:Math.min(89,slot.minute+2),text:"丢球之后对手立刻打起反击，我们的防线还没站好——他们扳回一球。",kind:"bad"})}
    }
  });
  p.ratingDelta=ratingDelta;p.boldTotal=boldTotal;p.boldWon=boldWon;
  p.momentClassic=classic;p.fitExtra=fitExtra;p.favorExtra=favorExtra;
  return p;
}
```

- [ ] **Step 4: `finishMatch` 改成调用它**

把 `finishMatch` 开头替换为：

```js
function finishMatch(s,pending,rng=Math.random){
  const p={...pending,timeline:pending.timeline.slice()};
  resolveMoments(s,p,rng);
  const ratingDelta=p.ratingDelta,boldTotal=p.boldTotal,boldWon=p.boldWon;
  let classic=p.momentClassic;
  const fitExtra=p.fitExtra,favorExtra=p.favorExtra;
```

后面从 `if(p.injuredInMatch)...` 开始的部分保持原样不动。

- [ ] **Step 5: 导出**

`API` 里加 `resolveMoments,`。

- [ ] **Step 6: 跑测试确认通过**

<!-- ⚠️ 陷阱4：一次绿不算绿。特别注意原有的比赛相关断言必须每次都绿。必须跑 30 次。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。这一步是纯抽取，行为不能有任何变化。

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "抽出 resolveMoments：关键时刻的并入规则只留一份

规则的要害是 claimGoal：稳妥选择只能把球队已有的机会转化到你名下，
只有高风险才能创造 xG 之外的进球，且每场至多一个 overflow。
世界杯马上要复用它。在那边写一套近似版，两套必然漂移——
这是纯抽取，行为零变化。"
```

---

### Task 13: `wc*` → `cup*` 更名与 `cupRun`

纯机械更名，**行为零变化**。单独成一个任务，是为了让后面几个真正改行为的任务有一个干净的 diff。

**Files:**
- Modify: `app.js:1361-1585`（世界杯整段）、`app.js:1288`（`normalizeSave`）、`app.js:1753`（`API`）
- Modify: `tests/structure.test.mjs`（36处引用）

- [ ] **Step 1: 写失败的测试**

追加到 `tests/structure.test.mjs` 末尾：

```js
// ===== 赛事引擎参数化：亚洲杯与世界杯同构，不许复制粘贴 =====
{
  assert.equal(typeof G.resumeCup,"function","恢复入口更名 resumeCup");
  assert.ok(!/\bwcRun\b/.test(code),"wcRun 必须全部更名 cupRun，留一半旧名字比全不改更糟");
  assert.ok(!/function wc[A-Z]/.test(code),"wc* 函数一律更名 cup*");
  // 在飞存档：正打到半决赛的档不能凭空丢掉整届赛事
  const stale={version:3,national:{wcRun:{stage:5,group:[],ko:[],results:[],groupWins:2,groupPts:6,alive:true}},
    attrs:{},styles:{},relationship:{},injury:{},risks:{}};
  const mig=G.normalizeSave(stale);
  assert.ok(mig.national.cupRun,"老档的 wcRun 要迁成 cupRun");
  assert.equal(mig.national.cupRun.cup,"world","迁过来的赛事是世界杯");
  assert.equal(mig.national.cupRun.stage,5,"进度不能丢");
  assert.equal("wcRun" in mig.national,false,"迁完要删掉旧字段，不能留僵尸");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`恢复入口更名 resumeCup`

- [ ] **Step 3: 机械更名 `app.js`**

```bash
cd /Users/ruis/Documents/模拟/模拟球员
python3 - <<'PY'
import pathlib,re
p=pathlib.Path("app.js");s=p.read_text(encoding="utf-8")
pairs=[("resumeWorldCup","resumeCup"),("setupWcFinals","setupCupFinals"),
 ("wcShootoutFinish","cupShootoutFinish"),("wcShootoutStep","cupShootoutStep"),
 ("wcKnockoutChoice","cupKnockoutChoice"),("wcPlayMatch","cupPlayMatch"),
 ("wcMatchSim","cupMatchSim"),("wcFinalEve","cupFinalEve"),("wcOutroScene","cupOutroScene"),
 ("wcFinish","cupFinish"),("wcNext","cupNext"),("wcDraw","cupDraw"),
 ("WC_STAGE_NAMES","CUP_STAGE_NAMES"),("wcRun","cupRun")]
for a,b in pairs: s=re.sub(r'\b%s\b'%a,b,s)
p.write_text(s,encoding="utf-8");print("done")
PY
```

⚠️ 顺序有讲究：`wcShootoutFinish` 必须排在 `wcShootoutStep` 之前替换吗？不需要——用了 `\b...\b` 词边界，两者互不为前缀。但 `wcFinish` 必须排在 `wcShootoutFinish` **之后**，否则 `wcShootoutFinish` 会先被内部匹配破坏。上面的顺序已经排好，照抄即可。

`startWorldCup` **保持原名不变**——它是世界杯特有的入口（世预赛），不是通用赛事逻辑。

- [ ] **Step 4: `cupRun` 加 `cup` 字段**

`setupCupFinals` 里创建 run 的那一行（原 :1462），把：
```js
  s.national.cupRun={stage:0,group:d.group,ko:d.ko,results:[],groupWins:0,groupPts:0,alive:true};
```
改为：
```js
  s.national.cupRun={cup:"world",stage:0,group:d.group,ko:d.ko,results:[],groupWins:0,groupPts:0,alive:true,moments:[],choices:[]};
```

- [ ] **Step 5: `normalizeSave` 迁移在飞存档**

在 `normalizeSave` 里 Task 9 那段之后追加：

```js
  /* wcRun → cupRun。不升 VERSION，所以一个正打到半决赛的档不会被清掉，
     不迁移就会变成 cupRun 空、wcRun 还挂着的僵尸状态。 */
  if(d.national&&d.national.wcRun&&!d.national.cupRun){
    d.national.cupRun={cup:"world",moments:[],choices:[],...d.national.wcRun};
    delete d.national.wcRun;
  }
```

- [ ] **Step 6: 同步更名测试文件**

```bash
python3 - <<'PY'
import pathlib,re
p=pathlib.Path("tests/structure.test.mjs");s=p.read_text(encoding="utf-8")
for a,b in [("resumeWorldCup","resumeCup"),("wcFinalEve","cupFinalEve"),
 ("wcFinish","cupFinish"),("wcMatchSim","cupMatchSim"),("wcDraw","cupDraw"),("wcRun","cupRun")]:
    s=re.sub(r'\b%s\b'%a,b,s)
p.write_text(s,encoding="utf-8");print("done")
PY
```

注意测试里的 `wcProbe` 这个**局部函数名不用改**（它是探针的名字，不是引擎的一部分），但它内部 `t.national.wcRun={...}` 会被上面的规则改成 `cupRun`——正确。改完后要给探针造的 run 补上 `cup` 字段：

在 `tests/structure.test.mjs` 的 `wcProbe` 里，把 `t.national.cupRun={stage,...}` 改为：
```js
  t.national.cupRun={cup:"world",stage,group:[],ko:KO_POOL,results:[],groupWins:2,groupPts:6,alive:true,moments:[],choices:[]};
```

同理第 613 段那个 `wcSave.national.cupRun={stage:4,...}` 补上 `cup:"world",`。

- [ ] **Step 7: 更新 API 导出**

`app.js:1753` 的 `API` 里，旧名字已被脚本替换。确认这些名字都在：`cupMatchSim,cupDraw,startWorldCup,resumeCup,cupFinalEve,cupOutroScene,cupFinish`。

- [ ] **Step 8: 跑测试确认通过**

<!-- ⚠️ 陷阱4：一次绿不算绿。纯更名但测试有随机性（点球随机路径）。必须跑 30 次。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。这一步是纯更名，原有的世界杯/点球断言必须每次都绿——任何一条挂了都说明替换伤到了逻辑，先修再走。

```bash
grep -nE '\bwcRun\b|function wc[A-Z]|resumeWorldCup' app.js tests/structure.test.mjs
```
Expected: 无输出

- [ ] **Step 9: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "wc* 更名 cup*，wcRun 加 cup 字段

亚洲杯与世界杯决赛圈结构完全同构，照抄一份改个名字必然漂移。
这一步是纯机械更名、行为零变化，单独成一个 commit 是为了让
后面真正改行为的改动有一个干净的 diff。
startWorldCup 保持原名——它是世界杯特有的入口（世预赛），不是通用逻辑。
normalizeSave 迁移在飞存档：不升 VERSION，一个正打到半决赛的档
不迁移就会变成 cupRun 空、wcRun 还挂着的僵尸状态。"
```

---

### Task 14: 亚洲杯对手池与赛事配置表

**Files:**
- Modify: `app.js:83`（`WC_QUAL_POOL`）、世界杯段
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 亚洲杯：对手池分层，难度不靠拍脑袋的补正 =====
{
  assert.ok(G.ASIA_POOL&&G.ASIA_POOL.length>=11,"亚洲球队强度只留一处定义");
  assert.ok(G.AC_GROUP_POOL.length>=3,"小组赛池够抽3个");
  assert.ok(G.AC_ELITE_POOL.length>=4,"淘汰赛池够抽4个");
  assert.ok(Math.max(...G.AC_GROUP_POOL.map(x=>x.strength))<Math.min(...G.AC_ELITE_POOL.map(x=>x.strength)),
    "小组池必须整体弱于淘汰赛池，否则分层没意义");
  // 两个池子加起来就是亚洲池，不许凭空多出球队
  assert.equal(G.AC_GROUP_POOL.length+G.AC_ELITE_POOL.length,G.ASIA_POOL.length,"分层是划分，不是新增");
  // 配置表
  assert.ok(G.CUP_CONFIG.world&&G.CUP_CONFIG.asian,"两个赛事各有一份配置");
  assert.notEqual(G.CUP_CONFIG.world.title,G.CUP_CONFIG.asian.title,"标题不同");
  assert.notEqual(G.CUP_CONFIG.world.elite,G.CUP_CONFIG.asian.elite,"对手池不同");
}
// 难度差别必须来自对手池，不是判定里的补正
{
  const t=G.createInitialState("难度",allocation,[],"standard","mid");
  ATTR_KEYS.forEach(k=>t.attrs[k]=90);
  let seed=1;const rng=()=>((seed=(seed*1664525+1013904223)>>>0)/4294967296);
  const winRate=(pool)=>{let w=0;for(let i=0;i<400;i++){
    const opp=pool[i%pool.length];const m=G.cupMatchSim(t,opp,"均衡",5,rng);
    if(m.gf>m.ga)w++}return w/400};
  const asia=winRate(G.AC_ELITE_POOL),world=winRate(G.WC_ELITE_POOL);
  assert.ok(asia>world+.12,
    `同一综合下亚洲杯淘汰赛胜率(${(asia*100).toFixed(0)}%)必须明显高于世界杯(${(world*100).toFixed(0)}%)，且差别只来自对手池`);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`亚洲球队强度只留一处定义`

- [ ] **Step 3: 改名并派生池子**

`app.js:83`，把：
```js
// 世预赛（亚洲区）对手池
const WC_QUAL_POOL=[
```
改为：
```js
// 亚洲球队：世预赛用全池，亚洲杯按强度分层。强度只在这里定义一次。
const ASIA_POOL=[
```

并在这个数组的闭合 `];` 之后插入：

```js
const AC_GROUP_POOL=ASIA_POOL.filter(t=>t.strength<78);   // 卡塔尔76 伊拉克73 阿联酋72 阿曼69 越南67 巴林66
const AC_ELITE_POOL=ASIA_POOL.filter(t=>t.strength>=78);  // 日本85 韩国83 伊朗82 澳大利亚80 沙特78
```

然后把全文其余的 `WC_QUAL_POOL` 换成 `ASIA_POOL`：
```bash
python3 -c "
import pathlib,re
p=pathlib.Path('app.js');s=p.read_text(encoding='utf-8')
p.write_text(re.sub(r'\bWC_QUAL_POOL\b','ASIA_POOL',s),encoding='utf-8')"
```

- [ ] **Step 4: 赛事配置表**

在 `CUP_STAGE_NAMES`（原 :1362）之后插入：

```js
/* 两个赛事同构，差别全部收在这张表里。
   判定、点球、出线规则、中断恢复一律共用——绝不为亚洲杯复制一份流程。
   注意这里没有任何难度补正：亚洲杯更容易夺冠，纯粹因为 elite 池弱得多
   （亚洲最强日本85，世界杯淘汰赛池84~93）。 */
const CUP_CONFIG={
  world:{title:"世界杯",group:()=>WC_GROUP_POOL,elite:()=>WC_ELITE_POOL,
    honour:"世界杯冠军",icon:"世",achievement:"world_cup",counter:"worldCups",champFame:25},
  asian:{title:"亚洲杯",group:()=>AC_GROUP_POOL,elite:()=>AC_ELITE_POOL,
    honour:"亚洲杯冠军",icon:"亚",achievement:"asian_cup",counter:"asianCups",champFame:14}
};
function cupCfg(s){const run=s.national.cupRun;return CUP_CONFIG[(run&&run.cup)||"world"]}
```

- [ ] **Step 5: `cupDraw` 接受池子参数**

把 `cupDraw`（原 :1363）改为：

```js
function cupDraw(rng,cfg){
  const take=(pool,n)=>{const c=[...pool],out=[];for(let k=0;k<n&&c.length;k++)out.push(c.splice(Math.floor(rng()*c.length),1)[0]);return out};
  return {group:take(cfg.group(),3),ko:take(cfg.elite(),4).sort((a,b)=>a.strength-b.strength)};
}
```

`setupCupFinals` 里的调用改为 `cupDraw(Math.random,CUP_CONFIG.world)`。

- [ ] **Step 6: `cupFinish` 按配置发奖**

`cupFinish` 里那段：
```js
  if(champion){s.honours.unshift({title:"世界杯冠军",season:ageInfo(s).season,icon:"世",detail:"中国国家队"});s.seasonStats.trophies++;unlock("world_cup");change(s,"fame",25);s.flags.worldChampion=true;}
```
改为：
```js
  const cfg=cupCfg(s);
  if(champion){s.honours.unshift({title:cfg.honour,season:ageInfo(s).season,icon:cfg.icon,detail:"中国国家队"});
    s.seasonStats.trophies++;unlock(cfg.achievement);change(s,"fame",cfg.champFame);
    if(run.cup==="world")s.flags.worldChampion=true;else s.flags.asianChampion=true;}
```

- [ ] **Step 7: 新增成就与计数器**

`ACHIEVEMENTS`（:118 段）里 `world_cup` 那条之后加：
```js
  {id:"asian_cup",icon:"亚",name:"亚洲之巅",desc:"赢得亚洲杯"},
```

`createInitialState` 的 `national:{called:false,adapt:0,caps:0,goals:0,worldCups:0}` 改为：
```js
national:{called:false,adapt:0,caps:0,goals:0,worldCups:0,asianCups:0}
```

`normalizeSave` 里加：
```js
  if(d.national&&typeof d.national.asianCups!=="number")d.national.asianCups=0;
```

- [ ] **Step 8: 导出**

`API` 里加 `ASIA_POOL,AC_GROUP_POOL,AC_ELITE_POOL,WC_GROUP_POOL,WC_ELITE_POOL,CUP_CONFIG,cupCfg,`。

- [ ] **Step 9: 跑测试确认通过**

<!-- ⚠️ 陷阱4：一次绿不算绿。必须跑 30 次。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。

- [ ] **Step 10: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "亚洲杯对手池与赛事配置表

WC_QUAL_POOL 更名 ASIA_POOL，亚洲球队强度只留一处定义；
亚洲杯的小组池与淘汰赛池从它按强度派生，不新增数据。
CUP_CONFIG 收下两个赛事的全部差别（对手池/荣誉/成就/计数器），
判定、点球、出线规则、中断恢复一律共用。
判定里没有任何难度补正：亚洲杯更容易夺冠纯粹因为 elite 池弱得多
（亚洲最强日本85，世界杯淘汰赛池84~93），测试里钉死了这条。"
```

---

### Task 15: 大赛日历排进赛程

**Files:**
- Modify: `app.js`（`buildSchedule`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 大赛日历：三条线两两不撞 =====
{
  assert.equal(G.cupMonthOf(72),"asian","20岁那年是亚洲杯");
  assert.equal(G.cupMonthOf(96),"world","22岁那年是世界杯");
  assert.equal(G.cupMonthOf(120),"asian","24岁");
  assert.equal(G.cupMonthOf(144),"world","26岁");
  assert.equal(G.cupMonthOf(24),null,"16岁太早，亚洲杯要 >=72");
  assert.equal(G.cupMonthOf(108),null,"23岁刻意留空给俱乐部线");
  // 世预赛6轮排在世界杯前一年，与亚洲杯月和世界杯月都不撞
  // ⚠️ [...] 不能省，理由同 Task 1：跨 vm realm 的数组原型不同，
  // deepStrictEqual 会失败，即使数值完全正确。
  const q=[...G.qualifierMonths(96)];
  assert.deepEqual(q,[84,86,88,90,92,94],"世预赛6轮，每两月一场，末轮距世界杯2个月");
  q.forEach(m=>{
    assert.equal(G.cupMonthOf(m),null,`世预赛第${m}月不能撞上大赛月`);
  });
  assert.deepEqual([...G.qualifierMonths(144)],[132,134,136,138,140,142],"下一届同理");
}
// 排进赛程后，那个月的国家队比赛取代俱乐部比赛
{
  const t=G.createInitialState("大赛日历",allocation,[],"standard","mid");
  t.totalMonth=120;t.flags.pro18=true;t.route="pro";t.national.called=true;
  t.club={name:"重庆铜梁龙",league:"中超",strength:67};
  const sc=G.ensureSchedule(t);
  const f=sc.fixtures.filter(x=>x.month===120);
  assert.equal(f.length,1,"一个月只能有一场，大赛取代俱乐部比赛");
  assert.equal(f[0].type,"cup","第120月是亚洲杯");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`G.cupMonthOf is not a function`

- [ ] **Step 3: 实现日历函数**

在 `buildSchedule` **之前**插入：

```js
/* 大赛日历。世界杯四年一届，中间两年只剩友谊赛，空得发慌——亚洲杯填中间。
   亚洲杯 %48===24（20/24/28岁），世预赛在世界杯前一年，世界杯 %48===0（22/26/30岁）。
   23/27岁刻意留空：这游戏主干是俱乐部生涯，每年都有大赛的话
   国家队就从高光变成负担。亚洲杯也因此不设预选赛。 */
function cupMonthOf(m){
  if(m>=96&&m%48===0)return "world";
  if(m>=72&&m%48===24)return "asian";
  return null;
}
function qualifierMonths(wcMonth){return [12,10,8,6,4,2].map(k=>wcMonth-k)}
function qualifierRoundAt(m){
  const wc=Math.ceil((m+2)/48)*48;                    // 该轮归属的世界杯月
  if(wc<96)return null;
  const idx=qualifierMonths(wc).indexOf(m);
  return idx<0?null:{wcMonth:wc,round:idx+1};
}
```

- [ ] **Step 4: `buildSchedule` 叠加大赛**

把 `buildSchedule` 里 `return {...scheduleSig(s),fixtures};` 之前插入：

```js
  // 大赛与世预赛取代当月俱乐部比赛——国际比赛日你不在俱乐部
  if(s.national.called)for(let i=0;i<fixtures.length;i++){
    const m=fixtures[i].month,cup=cupMonthOf(m),q=qualifierRoundAt(m);
    if(cup)fixtures[i]={month:m,type:"cup",cup,opponent:CUP_CONFIG[cup].title,strength:0,home:false,
      competition:`${CUP_CONFIG[cup].title}决赛圈`,status:"upcoming",result:null};
    else if(q)fixtures[i]={month:m,type:"wcq",wcMonth:q.wcMonth,round:q.round,
      opponent:"",strength:0,home:q.round%2===1,
      competition:`世预赛第${q.round}轮`,status:"upcoming",result:null};
  }
```

世预赛的对手留空，由 Task 16 在开打时从 `ASIA_POOL` 抽——因为要保证6轮不重复，抽签逻辑跟 `wcQual` 状态一起建更干净。

- [ ] **Step 5: 导出**

`API` 里加 `cupMonthOf,qualifierMonths,qualifierRoundAt,`。

- [ ] **Step 6: 跑测试确认通过**

<!-- ✅ 陷阱1确认：Task 15 的两处 deepEqual 已用 [...] 展开（const q=[...G.qualifierMonths(96)] 和 assert.deepEqual([...G.qualifierMonths(144)],...)），不需要再改。 -->
<!-- ⚠️ 陷阱4：一次绿不算绿。必须跑 30 次。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "大赛日历排进赛程：亚洲杯填上世界杯之间的空窗

亚洲杯 %48===24（20/24/28岁），世预赛在世界杯前一年6轮，
世界杯 %48===0（22/26/30岁）。三条线逐月验算过两两不撞。
23/27岁刻意留空——这游戏主干是俱乐部生涯，每年都有大赛的话
国家队就从高光变成负担。亚洲杯也因此不设预选赛。
大赛月取代俱乐部比赛：国际比赛日你不在俱乐部。"
```

---

### Task 16: 世预赛摊成6轮，走完整比赛管线

**Files:**
- Modify: `app.js`（`simulateQualifiers` 拆解、`prepareMatch` 加 `opts.club`、`startMatchFlow`）

⚠️ **别忘了赛前预告屏。** `stepMatchPreview` 里现在是 `club=currentClub(s)` 和 `startChance(s)`——
对世预赛这两个都错：预告会用**俱乐部**实力算实力对比和预估首发率，而 `prepareMatch` 实际用的是
`opts.club`（中国队）。两个数对不上，预告就是在骗玩家。本任务必须让 `stepMatchPreview` 按 `fx.type`
取对应的球队对象，并传给 `startChance(s,{club})`。
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 世预赛从一屏幻灯片摊成一整年 =====
{
  assert.ok(!/function simulateQualifiers/.test(code)||/scheduleQualifiers/.test(code),
    "一次性掷完8场的 simulateQualifiers 要拆成 scheduleQualifiers + settleQualifiers");
  assert.equal(typeof G.scheduleQualifiers,"function");
  assert.equal(typeof G.settleQualifiers,"function");
  const t=G.createInitialState("世预赛",allocation,[],"standard","mid");
  ATTR_KEYS.forEach(k=>t.attrs[k]=80);
  t.totalMonth=84;t.flags.pro18=true;t.route="pro";t.national.called=true;
  t.club={name:"重庆铜梁龙",league:"中超",strength:67};
  G.ensureSchedule(t);
  G.scheduleQualifiers(t,96);
  const q=t.national.wcQual;
  assert.ok(q,"世预赛进度必须存档——它跨越6个月");
  assert.equal(q.wcMonth,96);
  assert.equal(q.rounds,6,"排满6轮");
  assert.equal(q.played,0);
  assert.ok(q.threshold>0&&q.threshold<18,`出线线要落在6场18分的区间内，实际 ${q.threshold}`);
  // 6轮对手不重复
  const opps=t.schedule.fixtures.filter(f=>f.type==="wcq").map(f=>f.opponent);
  assert.equal(new Set(opps).size,opps.length,`6轮对手不能重复：${opps.join("/")}`);
}
// 中途才被征召：阈值按剩余轮数折算，否则新晋国脚必然出局
{
  const t=G.createInitialState("晚征召",allocation,[],"standard","mid");
  t.totalMonth=90;t.flags.pro18=true;t.route="pro";t.national.called=true;
  t.club={name:"重庆铜梁龙",league:"中超",strength:67};
  G.ensureSchedule(t);
  G.scheduleQualifiers(t,96);
  const q=t.national.wcQual;
  assert.ok(q.rounds<6,`月90才征召只剩 ${q.rounds} 轮`);
  assert.ok(q.threshold<8,`剩 ${q.rounds} 轮的出线线必须同比缩小，实际 ${q.threshold}`);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`G.scheduleQualifiers is not a function`

- [ ] **Step 3: `prepareMatch` 支持球队覆盖**

`app.js:958`，把：
```js
  const club=currentClub(s),opp=opts.opponent||pick(opponentPool(s));
```
改为：
```js
  const club=opts.club||currentClub(s),opp=opts.opponent||pick(opponentPool(s));
```

这一改让世预赛白拿预告、首发判定、时间线、评分、伤病、简报全套，不用平行实现一遍。

- [ ] **Step 4: 拆 `simulateQualifiers`**

删掉 `simulateQualifiers`（原 :1367-1381），换成：

```js
/* 世预赛不再一屏掷完8场——那让玩家整整一年的投入变成一次读秒。
   摊成6轮排进赛程，每轮走完整比赛管线，你的进球真的决定积分。 */
function nationalStrength(s){return 74+(overall(s)-72)*.72+(s.national.adapt||0)*.06+(hasTalent(s,"red_shirt")?2:0)}
function scheduleQualifiers(s,wcMonth,rng=Math.random){
  const months=qualifierMonths(wcMonth).filter(m=>m>=s.totalMonth);
  if(!months.length)return null;
  const bag=shuffled(ASIA_POOL,rng).slice(0,months.length);
  const sc=ensureSchedule(s);
  months.forEach((m,i)=>{
    const f=sc.fixtures.find(x=>x.month===m);
    if(!f)return;
    f.type="wcq";f.wcMonth=wcMonth;f.round=i+1;
    f.opponent=bag[i].name;f.strength=bag[i].strength;f.home=i%2===0;
    f.competition=`世预赛第${i+1}轮`;f.status="upcoming";f.result=null;
  });
  /* 出线线按轮数折算。原来8场需 11+d.threshold 分（11/24≈.458），
     6场18分同比例约8分。中途才被征召的按剩余轮数同比缩——
     否则一个月90才首次入选的国脚必然出局，那不是难度，是无解。 */
  s.national.wcQual={wcMonth,rounds:months.length,played:0,points:0,
    threshold:Math.max(1,Math.round(months.length*3*.458)+diffOf(s).threshold),
    results:[],settled:false};
  return s.national.wcQual;
}
function settleQualifiers(s){
  const q=s.national.wcQual;if(!q||q.settled)return;
  q.settled=true;
  const qualified=q.points>=q.threshold;
  enqueueDecision({title:qualified?"世预赛出线！":"世预赛出局",
    body:`<div class="story-list">${q.results.map(m=>`<div class="story-log"><time>${m.gf>m.ga?"胜":m.gf===m.ga?"平":"负"}</time><div><h3>中国 ${m.gf}-${m.ga} ${esc(m.opp)}</h3><p>你 ${m.goals} 球 ${m.assists} 助</p></div></div>`).join("")}</div>`+
      `<p>${q.rounds}场积 <b>${q.points}</b> 分（出线线 ${q.threshold}）。${qualified?"中国队闯进世界杯决赛圈！":""}</p>`,
    options:[qualified
      ?option("给家里打电话","",()=>enqueueFront({title:"父亲要去看世界杯",portrait:"assets/father.webp",body:`<p>电话响到第七声他才接，背景里有机器的声音——他还在厂里。</p><p>你说队伍出线了。他“嗯”了一声，隔了两秒，问的是：<span class="dialogue">“去那边看一场，要花多少钱。”</span></p><p>你说我给你买票。他说那不用，你比赛要紧。然后又问了一遍：<span class="dialogue">“多少钱。”</span></p>`,options:[option("进入决赛圈抽签","",()=>setupCupFinals(s))]},"世界杯"))
      :option("接受结果","",()=>{s.national.wcQual=null})]},"世界杯预选赛");
}
```

（出局那一屏的分量文案在 Task 18 补。）

- [ ] **Step 5: `startMatchFlow` 分派世预赛**

先放一个 `startCupFinals` 的空壳——它的真身在 Task 17，但 Task 16 就要引用它，缺了会直接抛 `ReferenceError`：

```js
/* 空壳：真身在「决赛圈每场关键时刻」那一步补上。
   现在先让赛程走通——大赛月标记为已打，月末流程正常接回去。 */
function startCupFinals(s,fx,ctx){fx.status="played";if(ctx)finishMonth(ctx)}
```

再把 Task 2 写的 `startMatchFlow` 里的 `s.pendingMatch={...}` 之前插入分派：

```js
function startMatchFlow(s,ctx){
  const fx=fixtureOfMonth(s);
  if(!fx){finishMonth(ctx);return}
  if(fx.type==="cup"){s.pendingMatch=null;modalBusy=false;startCupFinals(s,fx,ctx);return}
  s.pendingMatch={stage:"preview",fixture:fx,ctx,index:0,pending:null};
  modalBusy=false;
  stepMatchPreview(s);
}
```

`beginMatch` 里给世预赛套上国家队球队：

```js
function beginMatch(s,plan){
  const pm=s.pendingMatch,fx=pm.fixture;
  s.matchPlan=plan;
  const club=fx.type==="wcq"?{name:"中国",league:"世预赛",strength:Math.round(nationalStrength(s))}:null;
  pm.pending=prepareMatch(s,Math.random,{opponent:{name:fx.opponent,strength:fx.strength},
    home:fx.home,plan,competition:fx.competition,...(club?{club}:{})});
  pm.stage="moments";
  if(!pm.pending.plays){resolveMatch(s);return}
  stepKeyMoment(s);
}
```

`resolveMatch` 里，回写 fixture 之后追加世预赛积分：

```js
  if(pm.fixture&&pm.fixture.type==="wcq"&&s.national.wcQual){
    const q=s.national.wcQual;
    q.played++;q.points+=report.gf>report.ga?3:report.gf===report.ga?1:0;
    q.results.push({opp:report.opponent,gf:report.gf,ga:report.ga,goals:report.goals,assists:report.assists});
    s.national.caps++;s.statsCareer.nationalCaps++;
    s.national.goals+=report.goals;s.statsCareer.nationalGoals+=report.goals;
    if(q.played>=q.rounds)ctx.settleQual=true;
  }
```

`finishMonth` 里，在月度小结入队**之前**加：
```js
  if(ctx.settleQual)settleQualifiers(S);
```

- [ ] **Step 6: `finishMonth` 挂上排期与大赛触发**

把 `finishMonth` 里原来这一行（现 :1622）：
```js
  const calledNow=nationalSelectionCheck(S);if(calledNow)queueNationalCall(S);else if(S.national.called&&S.totalMonth>=96&&S.totalMonth%48===0)startWorldCup(S);else if(S.national.called&&S.totalMonth%6===0)queueNationalReport(simulateNationalMatch(S));
```
替换为：
```js
  const calledNow=nationalSelectionCheck(S);
  if(calledNow){queueNationalCall(S);ensureSchedule(S)}
  // 世界杯前一年的年初排世预赛
  {const wc=Math.ceil((S.totalMonth+2)/48)*48;
   if(S.national.called&&wc>=96&&!(S.national.wcQual&&S.national.wcQual.wcMonth===wc)&&qualifierMonths(wc).some(m=>m>=S.totalMonth)&&S.totalMonth>=wc-12)scheduleQualifiers(S,wc);}
  if(!calledNow&&S.national.called&&S.totalMonth%6===0&&!fixtureOfMonth(S))queueNationalReport(simulateNationalMatch(S));
```

- [ ] **Step 7: 导出**

`API` 里把 `simulateQualifiers,` 换成 `scheduleQualifiers,settleQualifiers,nationalStrength,`，并同步删掉 `tests/structure.test.mjs` 里对 `G.simulateQualifiers` 的引用（若有）。

- [ ] **Step 8: 跑测试确认通过**

<!-- ⚠️ 陷阱2：世预赛走完整 prepareMatch/finishMatch 管线，关键时刻是异步的（setTimeout）。
     Step 1 测试里只检查排期数据，不驱动比赛，不踩陷阱2。
     但整个测试文件包含的月末 driveMatch 断言会间接跑到世预赛，随机性依然存在。
     必须跑 30 次。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。

- [ ] **Step 9: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "世预赛从一屏幻灯片摊成一整年

原来 simulateQualifiers 在一个月里掷完8场，积分由 overall 推出来的
球队实力算出，玩家不在场上、一次选择都没有——一年的投入变成一次读秒。
拆成 scheduleQualifiers（前一年排6轮）+ settleQualifiers（末轮后判定）。
每轮走完整的 prepareMatch/finishMatch 管线：给 prepareMatch 加一个
opts.club 覆盖就白拿了预告、首发判定、时间线、评分、伤病、简报全套。
你的进球真的进比分、进积分、决定出线。
中途才被征召的按剩余轮数折算阈值——月90首次入选还按6轮的线要求，
那不是难度，是无解。"
```

---

### Task 17: 决赛圈每场关键时刻 + 小组赛基调

**Files:**
- Modify: `app.js`（`cupNext`、`cupPlayMatch`、新增 `startCupFinals`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 决赛圈：进球必须是你选的，不是骰子掷的 =====
{
  assert.ok(!/cupPlayMatch\(s,"均衡"\)/.test(code),
    "小组赛不能再写死均衡——三个阶段都该让玩家定基调");
  assert.ok(/resolveMoments\(s,/.test(code.split("function cupPlayMatch")[1]||""),
    "决赛圈每场必须走 resolveMoments，而不是另写一套近似并入规则");
}
{
  const t=G.createInitialState("决赛圈",allocation,[],"standard","mid");
  ATTR_KEYS.forEach(k=>t.attrs[k]=88);
  t.totalMonth=120;t.flags.pro18=true;t.national.called=true;
  t.national.cupRun={cup:"asian",stage:0,group:G.AC_GROUP_POOL.slice(0,3),
    ko:G.AC_ELITE_POOL.slice(0,4),results:[],groupWins:0,groupPts:0,alive:true,moments:[],choices:[]};
  G.setState(t);G.clearModalQueue();
  G.resumeCup(t);
  const q=G.getModalQueue();
  assert.ok(q.length>0,"决赛圈应该有弹窗");
  const opts=typeof q[0].options==="function"?q[0].options(t):q[0].options;
  assert.equal(opts.length,3,`小组赛第1场也该给基调三选一，实际 ${opts.length} 个选项`);
  // ✅ 陷阱2说明：决赛圈关键时刻通过 enqueueFront 同步推入队列（不是 setTimeout），
  // 所以同步的 driveModals 在这里仍然有效，不需要改 driveMatch。
  // 世预赛才是走 prepareMatch/finishMatch 异步管线的，世预赛测试用 driveMatch。
  // 整届跑完必须终止且不留悬空胜负
  const r=driveModals(t,(m,o)=>Math.floor(Math.random()*o.length),400);
  assert.ok(r.steps<400,"整届亚洲杯必须终止");
  const run=t.national.cupRun;
  if(run)assert.ok(run.results.every(x=>x.won!==null||x.stage<3),"淘汰赛场次不能留 won===null");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`小组赛不能再写死均衡`

- [ ] **Step 3: 小组赛也给基调**

把 `cupNext`（原 :1539）替换为：

```js
function cupNext(s){const run=s.national.cupRun;if(!run||!run.alive)return;
  if(run.stage===6&&!run.eveShown){run.eveShown=true;const d=cupFinalEve(s);return enqueueFront({...d,options:d.options.map(o=>option(o.text,o.effect,()=>{o.apply();cupNext(s)}))},"决赛前夜")}
  cupKnockoutChoice(s);}
```

把 `cupKnockoutChoice` 替换为（小组赛也走这里）：

```js
function cupKnockoutChoice(s){
  const run=s.national.cupRun,i=run.stage,cfg=cupCfg(s);
  const opp=i<3?run.group[i]:run.ko[i-3];
  enqueueFront({title:`${cfg.title} · ${CUP_STAGE_NAMES[i]} · 对阵 ${esc(opp.name)}`,
    body:`<p>对手 <b>${esc(opp.name)}</b>（实力 ${opp.strength}）。选择本场基调：</p><p class="dialogue">稳守：少丢也少进，利于以弱抗强、拖进点球；全力压上：进球更多但后防更险；均衡：居中。</p>`,
    options:[option("稳守反击","降低失球与进球，利于爆冷",()=>cupPlayMatch(s,"稳守")),
      option("攻守均衡","中规中矩",()=>cupPlayMatch(s,"均衡")),
      option("全力压上","多进球，风险更高",()=>cupPlayMatch(s,"强攻"))]},cfg.title);
}
```

- [ ] **Step 4: 每场插一个关键时刻**

把 `cupPlayMatch` 开头改为先出关键时刻：

```js
function cupPlayMatch(s,mentality){
  const run=s.national.cupRun;run.mentality=mentality;
  const slot=(pickMoments(s,Math.random)||[])[0];
  if(!slot){cupResolveMatch(s);return}
  run.moments=[slot];run.choices=[];
  const m=MOMENTS.find(x=>x.id===slot.id);
  if(!m){cupResolveMatch(s);return}
  const opp=run.stage<3?run.group[run.stage]:run.ko[run.stage-3];
  enqueueFront({title:`第${slot.minute}分钟 · ${m.title}`,
    body:`<p>${esc(m.body.replace("{minute}",slot.minute))}</p><p class="dialogue">${CUP_STAGE_NAMES[run.stage]} · 对阵 ${esc(opp.name)}。</p>`,
    options:momentOptions(s,m).map((o,idx)=>{
      const p=Math.round(momentSuccessRate(s,m,o,opp.strength,false)*100);
      return option(o.text,`成功率约 ${p}% · ${o.tip}`,()=>{run.choices=[idx];cupResolveMatch(s)},
        o.risk==="bold"?"danger":o.risk==="safe"?"":"gold")})},"关键时刻");
}
```

新增 `cupResolveMatch`——原 `cupPlayMatch` 的比赛体搬进来，并在掷出基础比分后走 `resolveMoments`：

```js
function cupResolveMatch(s){
  const run=s.national.cupRun,i=run.stage,cfg=cupCfg(s),mentality=run.mentality||"均衡";
  const opp=i<3?run.group[i]:run.ko[i-3];
  const m=cupMatchSim(s,opp,mentality,i,Math.random);
  /* 关键时刻的结果并进比分，用的是 finishMatch 那套 claimGoal/overflow 规则——
     绝不在这里另写一套近似版，两套必然漂移。 */
  const p={gf:m.gf,ga:m.ga,goals:m.goals,assists:m.assists,keyWins:0,failures:0,timeline:[],
    moments:run.moments||[],choices:run.choices||[],oppStrength:opp.strength,plan:s.matchPlan||"box"};
  resolveMoments(s,p,Math.random);
  m.gf=p.gf;m.ga=p.ga;m.goals=p.goals;m.assists=p.assists;m.momentLines=p.timeline;
  if(i>=3&&m.gf===m.ga){m.pen=true;m.won=null}else m.won=m.gf>m.ga;
  run.moments=[];run.choices=[];
  s.national.caps++;s.statsCareer.nationalCaps++;s.national.goals+=m.goals;s.statsCareer.nationalGoals+=m.goals;
  if(m.goals)unlock("national_goal");
  change(s,"fitness",-10);change(s,"fame",m.goals*3+(m.won?2:0));
  run.results.push(m);
  if(m.pen&&m.won===null){run.shootout=newShootout(s,{name:m.opp,strength:m.strength});run.penMatch=m;cupShootoutStep(s);return}
  let advance;
  if(i<3){run.groupWins+=(m.gf>m.ga?1:0);run.groupPts+=(m.gf>m.ga?3:m.gf===m.ga?1:0);advance=i<2?true:(run.groupWins>=1||run.groupPts>=4)}
  else advance=m.won;
  const champion=i===6&&m.won;
  const resultLine=i>=3?(m.won?"晋级下一轮！":"止步于此。"):(i===2?(advance?"小组出线！":"小组赛出局。"):(m.gf>m.ga?"拿下三分。":m.gf===m.ga?"逼平对手。":"惜败。"));
  enqueueFront({title:`${CUP_STAGE_NAMES[i]} · 中国 ${m.gf}-${m.ga} ${esc(m.opp)}${m.pen?"（点球）":""}`,
    body:`<div class="match-score"><span class="match-team">中国</span><strong>${m.gf} : ${m.ga}</strong><span class="match-team">${esc(m.opp)}</span></div>`+
      `<div class="timeline-list">${(m.momentLines||[]).map(t=>`<div class="timeline-row"><b>${t.minute}'</b><span>${esc(t.text)}</span></div>`).join("")}</div>`+
      `<p>你贡献 <b>${m.goals}</b> 球 ${m.assists} 助。${resultLine}</p><p class="dialogue">本场基调：${mentality}。</p>`,
    options:[option(champion?"捧起奖杯":advance?"继续":"接受结果","",()=>{
      run.stage++;if(champion)cupFinish(s,true);else if(!advance){run.alive=false;cupFinish(s,false)}else cupNext(s);})]},cfg.title);
}
```

- [ ] **Step 5: 亚洲杯入口**

新增（放在 `setupCupFinals` 旁边）：

```js
/* 亚洲杯：不设预选赛，赛程到月直接开打。 */
function startCupFinals(s,fx,ctx){
  s.cupCtx=ctx;
  const cup=fx.cup||"world",cfg=CUP_CONFIG[cup],d=cupDraw(Math.random,cfg);
  s.national[cfg.counter]=(s.national[cfg.counter]||0)+1;
  if(cup==="world")s.flags.worldcup_qualified=true;
  s.national.cupRun={cup,stage:0,group:d.group,ko:d.ko,results:[],groupWins:0,groupPts:0,alive:true,moments:[],choices:[]};
  fx.status="played";fx.result={gf:0,ga:0,goals:0,assists:0,rating:0};
  enqueueFront({title:`${cfg.title}抽签`,
    body:`<p>小组赛对手：<b>${d.group.map(o=>esc(o.name)).join("、")}</b></p><p>若能出线，淘汰赛之路可能是：${d.ko.map(o=>esc(o.name)).join(" → ")}</p><p class="dialogue">小组赛至少赢一场（或积满4分）即可出线；淘汰赛单场定胜负，平局进点球。</p>`,
    options:[option("开始小组赛","",()=>cupNext(s))]},cfg.title);
}
```

`setupCupFinals` 改成复用它（世界杯出线后调用）：把原函数体替换为
```js
function setupCupFinals(s){startCupFinals(s,{cup:"world",status:"upcoming",result:null},null)}
```

`cupFinish` 末尾清 run 之后，要把月末流程接回去：在两处 `s.national.cupRun=null` 之后各加一行 `if(s.cupCtx){const c=s.cupCtx;s.cupCtx=null;finishMonth(c)}`。

- [ ] **Step 6: 导出**

`API` 里加 `startCupFinals,cupResolveMatch,`。

- [ ] **Step 7: 跑测试确认通过**

<!-- ⚠️ 陷阱4：一次绿不算绿。决赛圈结果有随机性（进球/失球判定）。必须跑 30 次。 -->
<!-- ✅ 陷阱2确认：driveModals 在这里仍然有效（决赛圈关键时刻是 enqueueFront 同步推入）。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。

- [ ] **Step 8: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "决赛圈每场加关键时刻，小组赛也让玩家定基调

原来7场比赛没有一次关键时刻，你的进球是 cupMatchSim 里
rng()<clamp(.30+...) 掷出来的；小组赛连基调都写死「均衡」。
现在每场先出一个关键时刻，结果经 resolveMoments 并进比分——
用的是 finishMatch 那套 claimGoal/overflow 规则，不另写近似版。
亚洲杯走同一条路径，赛程到月直接开打（不设预选赛）。"
```

---

# D 段 · 收尾

### Task 18: 分量文案

spec 明确要求：世预赛出局、亚洲杯夺冠与淘汰，都不能是空收尾。

**Files:**
- Modify: `app.js`（`settleQualifiers`、`cupOutroScene`、`cupFinish`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 出局同样要有分量 =====
{
  // 世预赛出局：一年的投入不能用一句「差一口气」打发
  assert.ok(!/差一口气，四年后再来/.test(code),"世预赛出局的旧敷衍文案要换掉");
  assert.ok(/世预赛出局/.test(code),"出局这一屏还在");
  // 亚洲杯不能复用世界杯的大力神杯文案
  const t=G.createInitialState("亚洲杯收尾",allocation,[],"standard","mid");
  t.national.cupRun={cup:"asian",stage:7,group:[],ko:[],results:[
    {opp:"日本",gf:2,ga:1,goals:1,assists:0,won:true,stage:6}],groupWins:2,groupPts:6,alive:true};
  G.setState(t);G.clearModalQueue();
  G.cupFinish(t,true);
  const seen=G.getModalQueue().map(m=>m.title+String(typeof m.body==="function"?m.body(t):m.body)).join(" ");
  assert.ok(!/大力神杯/.test(seen),"亚洲杯夺冠不能出现大力神杯——那是世界杯的东西");
  assert.ok(/亚洲杯/.test(seen),"亚洲杯夺冠要说清是哪个冠军");
  G.clearModalQueue();
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`世预赛出局的旧敷衍文案要换掉`

- [ ] **Step 3: 世预赛出局场景**

`settleQualifiers` 里出局那个分支，把 `option("接受结果","",()=>{s.national.wcQual=null})` 换成：

```js
      :option("走出球场","",()=>enqueueFront({title:"最后一轮的终场哨",portrait:"assets/father.webp",
        body:`<p>积分榜挂在更衣室走廊的电视上，没有人去关。差的那几分是哪一场丢的，每个人心里都有一本账，但谁都没说。</p>`+
          `<p>你换衣服的时候手机震了一下。是你爸：<span class="dialogue">“看完了。”</span>隔了很久又来一条：<span class="dialogue">“下次还有。”</span></p>`+
          `<p>你想回一句什么，打了两遍都删了。四年后你${ageInfo(s).age+4}岁——这个数字你不用算，它一直在那儿。</p>`,
        options:[option("把这一年收起来","国家队数据已归档",()=>{s.national.wcQual=null})]},"世界杯预选赛"))
```

- [ ] **Step 4: 亚洲杯的夺冠与淘汰文案**

`cupFinish` 里那段写死大力神杯的分支，按赛事分派。把 `if(champion)return enqueueFront({title:"大力神杯",...})` 那一块替换为：

```js
  if(champion){
    const worldTrophy={title:"大力神杯",portrait:"assets/player.webp",
      body:`<p>队长把奖杯递过来的时候你没有马上接。你先把手在球衣上擦了两下——手心全是汗，你怕滑。</p><p>金属是凉的。比你想象中重。</p>`};
    const asiaTrophy={title:"亚洲杯",portrait:"assets/player.webp",
      body:`<p>颁奖台比想象中矮，你上去的时候还差点踩空。奖杯递到手里，你才发现自己一直在笑，从终场哨响就没停过。</p>`+
        `<p>有人在你耳边喊了句什么，你没听清。你只想着一件事：这是中国队的。这是我们的。</p>`};
    return enqueueFront({...(run.cup==="asian"?asiaTrophy:worldTrophy),options:[option("找看台","",()=>{
      enqueueFront({title:"看台",portrait:"assets/father.webp",body:`<p>你在人群里一排排地找。找到的时候他正把眼镜摘下来擦，擦了很久。</p><p>旁边那个位置上的人一直在挥手，从终场哨响到现在，没停过。</p>`,
        options:[option("记住这一届","这一届会写进你的生涯",()=>{s.national.cupRun=null;if(s.cupCtx){const c=s.cupCtx;s.cupCtx=null;finishMonth(c)}})]},`${cfg.title}冠军`);
    })]},`${cfg.title}冠军`);
  }
```

`cupOutroScene` 里那句写死「四年后」的尾巴，亚洲杯要换成「两年后还有世界杯」：

```js
function cupOutroScene(s){
  const run=s.national.cupRun,age=ageInfo(s).age,asian=run&&run.cup==="asian";
  const gap=asian?2:4,again=age+gap<=diffOf(s).retireAge;
  const tail=asian
    ?(again?`两年后就是世界杯。你${age+2}岁，还来得及。`:`两年后就是世界杯。你${age+2}岁，你知道那意味着什么。`)
    :(again?`四年后你${age+4}岁。还来得及。`:`四年后你${age+4}岁。你知道那意味着什么。`);
  if(run&&run.missedDecisivePenalty)return {title:"那一脚",portrait:"assets/father.webp",
    body:`<p>更衣室里没有人说话。有人在解鞋带，解了很久也没解开。</p><p>你父亲的消息进来得很晚：<span class="dialogue">“我在电视上看见你走回去了。”</span>就这一句，没有别的。</p><p>${tail}</p>`};
  return {title:"回家的航班",portrait:"assets/lin-xiaoman.webp",
    body:`<p>行李在传送带上转了两圈你才认出自己那个。出口外面人不多，她举着的牌子上什么也没写。</p><p>她说：<span class="dialogue">“我看完了。全部。”</span></p><p>${tail}</p>`};
}
```

- [ ] **Step 5: 跑测试确认通过**

<!-- ⚠️ 陷阱4：一次绿不算绿。必须跑 30 次。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。

- [ ] **Step 6: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "出局与夺冠都要有分量：世预赛出局场景、亚洲杯专属文案

世预赛现在是一整年6轮，出局还用一句「差一口气，四年后再来」打发，
等于把玩家一年的投入一笔带过。
亚洲杯不能复用「大力神杯」——那是世界杯的东西；
cupOutroScene 的尾巴也要分开：亚洲杯之后是两年后的世界杯，不是四年。"
```

---

### Task 19: 文档与单文件版

**Files:**
- Modify: `README.md`
- Regenerate: `模拟球员-单文件版.html`

- [ ] **Step 1: 更新 README「已实现」**

在 `README.md` 的「已实现」列表里，把与本次改动相关的条目更新：

- 新增一条：`- 赛季赛程在赛季初排定：主界面常显下一场对手与时间，「赛程」页可查一整季日程（含国家队大赛），比赛前先出预告并在那一屏选定本场职责`
- 新增一条：`- 家庭状况影响状态长期基线，与小满关系并列显示在侧栏`
- 把原来的 `- 国家队征召、陌生位置、国家队伤病、世界杯模拟` 改为：
  `- 国家队大赛日历：20/24/28岁亚洲杯、21/25/29岁世预赛六轮、22/26/30岁世界杯；世预赛摊在整年逐场可玩，决赛圈每场有关键时刻与临场基调，平局进点球`

- [ ] **Step 2: 跑完整测试（30次稳定性验证）**

<!-- ⚠️ 陷阱4：最终交付必须通过30次连续验证。一次绿不算绿。 -->
Run:
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30个点（全绿）。出现任何 `F` 都不能提交最终成果，先排查。
再单跑一次拿完整输出确认结束行：
```bash
node tests/structure.test.mjs
```
Expected: 输出 `模拟球员 architecture test passed`（末尾）。

- [ ] **Step 3: 重建单文件版**

```bash
python3 build_single.py
```
Expected: 输出含 `残留未内联 src=assets: 0 (应0)` 与 `PLAYER_ASSETS: True`

- [ ] **Step 4: 浏览器整体验收**

```bash
python3 -m http.server 8766
```

开 `http://127.0.0.1:8766/`，用浏览器 devtools 的 console 执行下面这段把档快进到22岁世界杯年，验证大赛链路：

```js
const S=window.PlayerLife.getState();
S.totalMonth=83;S.national.called=true;S.flags.pro18=true;S.route="pro";
S.club={name:"上海海港",league:"中超",strength:79};
Object.keys(S.attrs).forEach(k=>S.attrs[k]=88);
window.PlayerLife.setState(S);location.reload();
```

逐项确认：
1. 赛程页能看到世预赛六轮，标着对手
2. 结束本月 → 世预赛预告 → 关键时刻 → 简报 → 月度小结
3. 六轮打完出「世预赛出线/出局」，出局那屏有分量、不是一句话
4. 出线后进抽签，小组赛第1场就有基调三选一
5. 每场决赛圈比赛有一个关键时刻，且简报里能看到它的时间线
6. 打到22岁往后，24岁那年赛程页出现「亚洲杯决赛圈」

- [ ] **Step 5: 提交**

```bash
git add README.md 模拟球员-单文件版.html
git commit -m "README 与单文件版：赛程、家庭、国家队大赛日历"
```

---

## 自查

**Spec 覆盖：**

| Spec 要求 | 任务 |
|---|---|
| `s.schedule` 结构与生成 | T1 |
| `ensureSchedule` 只重建未来 | T1 |
| `shouldPlayMatch` 改查表 | T2 |
| 主界面下一场 | T5 |
| 日程页放进「比赛」tab | T6 |
| 赛前预告 = 职责三选一 | T4 |
| `startChance` 抽公用 | T4 |
| 月度小结移队尾 + 惰性 body | T3 |
| 不升 VERSION，`normalizeSave` 兜底 | T1/T8/T9/T13/T14 |
| `study`/`risks.health`/`risks.media` 删除映射 | T9 |
| `scout` → `fame` + 门槛标定 | T7/T8 |
| 老档 scout 搬运 | T8 |
| `language` 收窄 | T10 |
| `familySupport` 抬基线 + 侧栏 | T11 |
| 孤儿配色护栏 | T11 Step 6 |
| `resolveMoments` 抽取 | T12 |
| `wc*` → `cup*` + `cupRun` 迁移 | T13 |
| 亚洲杯池分层、无难度补正 | T14 |
| 大赛日历不撞车 | T15 |
| 世预赛6轮 + 中途征召折算 | T16 |
| 决赛圈关键时刻 + 小组赛基调 | T17 |
| 出局/夺冠文案 | T18 |
| 重建单文件版 | T19 |

**类型一致性检查（跨任务的名字必须对得上）：**
- `ensureSchedule` / `fixtureOfMonth` / `nextFixture`（T1 定义 → T2/T5/T6/T15/T16 使用）✓
- `stepMatchPreview` / `beginMatch`（T2 放直通版 → T4 实现 → T16 改造）✓
- `startChance`（T4 Step 3 定义 → T4 Step 4 把 `prepareMatch:961` 也改成调它，公式只留一份）✓
- `resolveMoments`（T12 定义 → T17 使用）✓
- `cupCfg` / `CUP_CONFIG`（T14 定义 → T17/T18 使用）✓
- `cupMonthOf` / `qualifierMonths` / `qualifierRoundAt`（T15 定义 → T16 使用）✓
- `nationalStrength`（T16 定义 → T16 内部使用）✓
- `startCupFinals`（T16 Step 5 放空壳 → T17 Step 5 补真身）✓
- `s.cupCtx`（T17 设置 → T17/T18 清理）✓

**占位符扫描：** 全文无 TBD/TODO；每个改代码的步骤都带完整代码块；T7 的两个待定数（`NN`/`MM`）是**实测产出**而非占位符——T7 会跑出具体值，T8 明确写了「把 `NN`/`MM` 换成 Task 7 量出来的数」。这是 spec 要求的标定流程，不是没想清楚。

