# 世界杯仪式感 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把世界杯从「七个只显示比分的弹窗」变成有紧张感与人物在场的高潮段落——点球大战交互化、人物三节点、夺冠与淘汰仪式，并让进行中的世界杯不再因刷新而消失。

**Architecture:** 全部状态挂在既有的 `s.national.wcRun` 下（纯 JSON，沿用 `pendingMatch` 已验证过的可序列化纪律）。点球是一台以 `wcRun.shootout` 为状态的小状态机，逐轮推进、在玩家主罚那轮停下来等选择。判定一律经 `eff()`，不新增平衡表。不触碰俱乐部比赛引擎。

**Tech Stack:** 单文件 vanilla JS（`app.js` 约 1570 行 / 233KB，**极长行风格**），无构建步骤、无依赖。测试为 `node:vm` 沙箱加载 `app.js` 读 `window.PlayerLife`。

---

## 阅读本计划前必读

`app.js` 单行常超过 2000 字符。**不要整读、不要读大段行范围**，会耗尽上下文。用 `grep -n` 定位，用 `Read` 读 ≤3 行，用 `Edit` 配唯一 `old_string` 改。看某一长行：

```bash
python3 -c "print(open('app.js',encoding='utf-8').read().split(chr(10))[1399][:600])"
```

**保持极长行风格，不要重新格式化、不要换行。**

## 既有基础设施（直接复用，不要重造）

| 名字 | 签名 / 说明 |
|---|---|
| `option(text,effect,apply,tone="")` | 弹窗选项 |
| `enqueueFront(d,kicker)` | 插队弹窗；`d={title,body,options,portrait?}` |
| `enqueueDecision(d,kicker)` | 排队弹窗 |
| `eff(s,key)` | **所有判定唯一取值入口**，已含状态与体能 |
| `clamp(n,min=0,max=100)` | |
| `WC_STAGE_NAMES` | 7 项，索引 6 是决赛 |
| `s.national.wcRun` | `{stage,group,ko,results,groupWins,groupPts,alive}` |
| `saveGame()` | 每次弹窗选项 apply 之后自动调用，无需手动加 |

`d.portrait` 走 `modalPortrait`，传 `"assets/father.webp"` 这类路径即可。

**判定纪律：任何成功率不得直接读 `s.attrs[k]`，一律 `eff(s,k)`。** 已有测试禁止在判定公式里另掺 `form`/`fitness`。

---

## 文件结构

| 文件 | 本次职责 |
|---|---|
| `app.js` | 新增 `PENALTY_OPTIONS` 常量、`penaltyKickerRound` / `penaltyRate` / `teamPenaltyRate` 三个纯函数、`wcShootout*` 状态机、`WC_STORY_BEATS` 人物节点、改写 `wcFinish`；修改 `init()` 的读档分支与 `wcMatchSim` 的点球分支 |
| `tests/structure.test.mjs` | 追加断言（当前 247 条） |

不新增文件——本项目是单文件分发，`build_single.py` 依赖这一结构。

---

## Task 1: `wcRun` 可恢复

进行中的世界杯目前刷新即消失。点球是多步交互，不先修这个，玩家在第四轮刷新会丢掉整届世界杯。

**Files:**
- Modify: `app.js`（`init()` 里的 `menuContinue` 分支）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 `tests/structure.test.mjs` 末尾 `for(const file of [...])` 之前插入：

```js
// ===== 世界杯必须能跨刷新续上 =====
// 点球是多步交互；若 wcRun 不持久化，第四轮刷一下页面整届世界杯就没了。
assert.ok(!/wcRun=null;\$\("menu"\)/.test(code),"读档不再无条件清空进行中的世界杯");
assert.equal(typeof G.resumeWorldCup,"function","有一个恢复入口");
const wcSave=G.createInitialState("世界杯",allocation,[],"standard","mid");
wcSave.national.wcRun={stage:4,group:[{name:"A",strength:70}],ko:[{name:"B",strength:80}],
  results:[{opp:"A",gf:1,ga:0,goals:1,assists:0,won:true,pen:false,stage:0}],
  groupWins:1,groupPts:3,alive:true};
const wcRound=JSON.parse(JSON.stringify(wcSave));
assert.equal(JSON.stringify(wcRound.national.wcRun),JSON.stringify(wcSave.national.wcRun),
  "wcRun 是纯 JSON，round-trip 不丢字段");
assert.equal(G.normalizeSave(wcRound).national.wcRun.stage,4,"normalizeSave 保留进行中的世界杯");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `读档不再无条件清空进行中的世界杯`

- [ ] **Step 3: 删掉清空、加恢复入口**

`init()` 里找到这一段（在 `menuContinue` 的点击处理里）：

```js
$("menuContinue").addEventListener("click",()=>{if(!loadGame())return;S=saved;if(S.national&&S.national.wcRun)S.national.wcRun=null;$("menu").classList.add("hidden");showGame()})
```

替换为：

```js
$("menuContinue").addEventListener("click",()=>{if(!loadGame())return;S=saved;$("menu").classList.add("hidden");showGame();resumeWorldCup(S)})
```

- [ ] **Step 4: 实现 `resumeWorldCup`**

紧挨在 `function wcNext(s)` 之前插入：

```js
/* 读档后把进行中的世界杯接回来。点球打到一半就回点球，否则回到下一场。
   stage>6 或 alive 为假说明这届已经结束，直接清掉。 */
function resumeWorldCup(s){
  const run=s&&s.national&&s.national.wcRun;
  if(!run||!run.alive){if(run)s.national.wcRun=null;return}
  if(run.stage>6){s.national.wcRun=null;return}
  if(run.shootout&&!run.shootout.done){wcShootoutStep(s);return}
  wcNext(s);
}
```

`wcShootoutStep` 在 Task 3 实现；本任务先加一个空壳，紧跟在 `resumeWorldCup` 之后：

```js
function wcShootoutStep(s){wcNext(s)}   // Task 3 接入真正的点球流程
```

- [ ] **Step 5: 导出**

在 `API` 对象里 `prepareMatch,` 之后插入 `resumeWorldCup,`。

- [ ] **Step 6: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: `模拟球员 architecture test passed`

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "世界杯可恢复：读档不再清空进行中的赛事

原先 init() 里无条件 wcRun=null，进行中的世界杯刷新即消失。俱乐部
比赛早已用可序列化的 pendingMatch 解决过同一问题，世界杯照抄该纪律。
点球是多步交互，不先修这个，玩家在第四轮刷新会丢掉整届世界杯。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: 点球的三个纯函数

先落地可独立测试的判定逻辑，不碰流程。

**Files:**
- Modify: `app.js`（`wcMatchSim` 之前插入常量与三个函数）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 Task 1 的测试块之后插入：

```js
// ===== 点球：主罚轮次由意志决定 =====
// 赛前的教练决策，取决于你是什么样的球员，而不是此刻多累——所以读裸 WIL。
const pk=(wil)=>{const t=G.createInitialState("点球",allocation,[],"standard","mid");t.attrs.WIL=wil;return G.penaltyKickerRound(t)};
assert.equal(pk(80),5,"意志强的人被交付最后一脚");
assert.equal(pk(65),3,"中段");
assert.equal(pk(40),1,"先踢，卸掉压力");

// ===== 点球：三个选项的真实关系 =====
assert.equal(G.PENALTY_OPTIONS.length,3,"三选一");
G.PENALTY_OPTIONS.forEach(o=>{
  assert.ok(["SHO","WIL"].includes(o.stat),`${o.text} 指向真实属性`);
  assert.ok(o.text&&o.tip,`${o.stat} 选项有文案`);
});
function pRate(sho,wil,fitness,id){
  const t=G.createInitialState("罚球",allocation,[],"standard","mid");
  t.attrs.SHO=sho;t.attrs.WIL=wil;t.form=55;t.fitness=fitness;
  return G.penaltyRate(t,G.PENALTY_OPTIONS.find(o=>o.id===id));
}
// 均衡 build：正常体能稳推最优，体能见底等门将反超
assert.ok(pRate(75,75,72,"steady")>pRate(75,75,72,"wait"),"均衡 build 正常体能下稳推更优");
assert.ok(pRate(75,75,0,"wait")>pRate(75,75,0,"steady"),"均衡 build 体能见底时等门将反超");
// 射手型：两种体能下稳推都最优——极端 build 不该被体能翻盘
assert.ok(pRate(85,60,72,"steady")>pRate(85,60,72,"wait"),"射手型正常体能下稳推最优");
assert.ok(pRate(85,60,0,"steady")>pRate(85,60,0,"wait"),"射手型体能见底时稳推仍最优");
// 抽死角靠附加价值立足，成功率本就低于稳推
assert.ok(pRate(75,75,72,"bold")<pRate(75,75,72,"steady"),"抽死角成功率低于稳推，靠附加取胜");
assert.equal(G.PENALTY_OPTIONS.find(o=>o.id==="bold").bold,true,"抽死角被标为高风险");
// 成功率永远夹在 35%~92%
[[1,1,0],[99,99,100]].forEach(([a,b,f])=>G.PENALTY_OPTIONS.forEach(o=>{
  const r=pRate(a,b,f,o.id);
  assert.ok(r>=.35&&r<=.92,`${o.id} 在极端属性下仍被夹住，实际 ${r}`);
}));

// ===== 点球：队友与对手的成功率 =====
const tp=G.createInitialState("队友",allocation,[],"standard","mid");
assert.ok(G.teamPenaltyRate(60)<G.teamPenaltyRate(90),"球队越强罚得越准");
assert.ok(G.teamPenaltyRate(1)>=.55&&G.teamPenaltyRate(200)<=.88,"队友成功率被夹在 55%~88%");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `TypeError: G.penaltyKickerRound is not a function`

- [ ] **Step 3: 实现常量与三个函数**

紧挨在 `function wcMatchSim(` 之前插入：

```js
/* ========== 点球大战 ==========
   三个选项刻意不是「同一条曲线上的三个点」：稳推对 SHO 最敏感、等门将
   走 WIL 且对体能最钝、抽死角成功率最低但成功会被记住。因为点球是二元
   结果，若三者只差成功率，最高的那个必然严格占优、另外两个就是死选项。 */
const PENALTY_OPTIONS=[
  {id:"bold",text:"抽死角",tip:"成功率最低，但这一脚会被记住",stat:"SHO",bold:true},
  {id:"steady",text:"稳稳推一侧",tip:"最不容易出洋相",stat:"SHO"},
  {id:"wait",text:"等门将先动",tip:"最不受体能影响",stat:"WIL"}
];
/* 主罚轮次是赛前的教练决策：取决于你是什么样的球员，而不是此刻多累，
   所以读裸 WIL 而非 eff()。 */
function penaltyKickerRound(s){const w=s.attrs.WIL;return w>=75?5:w>=55?3:1}
function penaltyRate(s,o){
  const e=eff(s,o.stat);
  const base=o.id==="bold"?.58+(e-70)/200:o.id==="steady"?.68+(e-70)/160:.66+(e-70)/300;
  return clamp(base+(hasTalent(s,"big_heart")?.06:0),.35,.92);
}
function teamPenaltyRate(strength){return clamp(.72+(strength-74)/200,.55,.88)}
```

- [ ] **Step 4: 导出**

在 `API` 对象里 `resumeWorldCup,` 之后插入 `PENALTY_OPTIONS,penaltyKickerRound,penaltyRate,teamPenaltyRate,`。

- [ ] **Step 5: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: PASS

若「均衡 build 体能见底时等门将反超」失败，**不要直接改测试期望**——回到 spec §2.4 的验算表核对三条公式的除数（200 / 160 / 300），那三个数就是为了让这个交叉点存在而选的。

- [ ] **Step 6: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "点球判定：主罚轮次由意志决定，三选一各有所长

三个选项不在同一条曲线上：稳推对 SHO 最敏感、等门将走 WIL 且对体能
最钝、抽死角成功率最低但成功会被记住。点球是二元结果，若三者只差
成功率，最高的那个必然严格占优。

由此长出一条不是单独配的性质：均衡 build 在体能见底时最优解从稳推
翻转到等门将，而射手型 build 两种体能下都该相信自己那一脚——这是
COND_SENS(SHO .7 / WIL .4) 自己算出来的。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: 点球流程接入

**Files:**
- Modify: `app.js`（`wcMatchSim` 的点球分支、`wcPlayMatch`、Task 1 留下的 `wcShootoutStep` 空壳）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 Task 2 的测试块之后插入：

```js
// ===== 点球状态机 =====
const soState=G.createInitialState("大战",allocation,[],"standard","mid");
soState.attrs.WIL=80;
const so=G.newShootout(soState,{name:"对手",strength:78});
assert.equal(so.round,1,"从第一轮开始");
assert.equal(so.myRound,5,"WIL 80 主罚第五轮");
assert.equal(so.myScore,0);assert.equal(so.oppScore,0);
assert.equal(so.done,false);
assert.equal(JSON.stringify(JSON.parse(JSON.stringify(so))),JSON.stringify(so),
  "点球状态是纯 JSON，刷新不丢");
// 推进到玩家主罚那轮时必须停下来
let guard=0;
while(!so.done&&so.round<so.myRound&&guard++<20)G.shootoutAdvance(soState,so,()=>.5);
assert.equal(so.round,so.myRound,"推进到玩家那轮就停");
assert.ok(so.kicks.length>0,"前面几轮留下了记录");
so.kicks.forEach(k=>assert.ok(["me","opp"].includes(k.side)&&typeof k.scored==="boolean",
  "每一脚都记了是谁踢的、进没进"));

// 玩家主罚后必须推进，且比分随结果变化
const soBefore={my:so.myScore,opp:so.oppScore,round:so.round};
const scored=G.shootoutPlayerKick(soState,so,"steady",()=>0);   // rng=0 必进
assert.equal(scored,true,"rng=0 时必进");
assert.equal(so.myScore,soBefore.my+1,"进了就加分");
assert.equal(so.round,soBefore.round+1,"主罚后推进一轮");
const soMiss=G.newShootout(soState,{name:"对手",strength:78});
G.shootoutPlayerKick(soState,soMiss,"steady",()=>0.999);        // rng≈1 必丢
assert.equal(soMiss.myMiss,true,"罚丢会被记下来");

// ===== 点球结果必须写回那场比赛的胜负 =====
// wcMatchSim 平局时 won=null（待点球决出），不能让 null 漏进赛果。
const drawn=G.createInitialState("平局",allocation,[],"standard","mid");
ATTR_KEYS.forEach(k=>drawn.attrs[k]=80);drawn.totalMonth=96;drawn.flags.pro18=true;
let sawNull=false;
for(let i=1;i<=200;i++){
  let x=i>>>0;const rng=()=>((x=(x*1664525+1013904223)>>>0)/4294967296);
  const mm=G.wcMatchSim(drawn,{name:"对手",strength:80},"均衡",4,rng);
  if(mm.gf===mm.ga&&mm.won===null&&mm.pen)sawNull=true;
  else assert.ok(typeof mm.won==="boolean",`非平局场次 won 必须是布尔，实际 ${mm.won}`);
}
assert.ok(sawNull,"淘汰赛平局时 won 置 null 交给点球决出");
```

- [ ] **Step 2: 运行测试确认失败**

Expected: FAIL — `TypeError: G.newShootout is not a function`

- [ ] **Step 3: 实现状态机**

紧挨在 `PENALTY_OPTIONS` 之后插入：

```js
function newShootout(s,opp){
  return {round:1,myScore:0,oppScore:0,kicks:[],myRound:penaltyKickerRound(s),
    oppStrength:opp.strength,oppName:opp.name,sudden:false,done:false,won:null,myMiss:false};
}
/* 推进一轮：先中国后对手。轮到玩家主罚就原地返回，等 UI 拿选择回来。
   五轮打平进突然死亡（单次判定，不做多轮，避免流程冗长）。 */
function shootoutAdvance(s,so,rng){
  if(so.done||so.round===so.myRound)return so;
  const teamStrength=74+(overall(s)-72)*.72+(s.national.adapt||0)*.06;
  const mine=rng()<teamPenaltyRate(teamStrength);
  so.kicks.push({side:"me",round:so.round,scored:mine});
  if(mine)so.myScore++;
  const theirs=rng()<teamPenaltyRate(so.oppStrength);
  so.kicks.push({side:"opp",round:so.round,scored:theirs});
  if(theirs)so.oppScore++;
  so.round++;
  shootoutSettle(so);
  return so;
}
/* 五轮结束后判定；平局则进突然死亡标记，由 wcShootoutStep 收尾。 */
function shootoutSettle(so){
  if(so.round<=5)return;
  if(so.myScore!==so.oppScore){so.done=true;so.won=so.myScore>so.oppScore}
  else so.sudden=true;
}
function shootoutPlayerKick(s,so,optId,rng){
  const o=PENALTY_OPTIONS.find(x=>x.id===optId)||PENALTY_OPTIONS[1];
  const scored=rng()<penaltyRate(s,o);
  so.kicks.push({side:"me",round:so.round,scored,mine:true,opt:o.id});
  if(scored){so.myScore++;if(o.bold)change(s,"fame",4)}else so.myMiss=true;
  const theirs=rng()<teamPenaltyRate(so.oppStrength);
  so.kicks.push({side:"opp",round:so.round,scored:theirs});
  if(theirs)so.oppScore++;
  so.round++;
  shootoutSettle(so);
  return scored;
}
```

- [ ] **Step 4: 接进 `wcMatchSim`**

`wcMatchSim` 里这一行：

```js
if(i>=3&&gf===ga){pen=true;won=rng()<clamp(.42+edge/40+(hasTalent(s,"big_heart")?.08:0),.18,.82)}
```

替换为：

```js
if(i>=3&&gf===ga){pen=true;won=null}
```

`won=null` 表示「待点球决出」，由 `wcPlayMatch` 处理。

- [ ] **Step 5: 改 `wcPlayMatch` 分流**

`wcPlayMatch` 里 `run.results.push(m);` 之后、`let advance;` 之前插入：

```js
  if(m.pen&&m.won===null){run.shootout=newShootout(s,{name:m.opp,strength:m.strength});run.penMatch=m;wcShootoutStep(s);return}
```

- [ ] **Step 6: 实现 `wcShootoutStep`**

把 Task 1 的空壳 `function wcShootoutStep(s){wcNext(s)}` 整行替换为：

```js
/* 点球流程的唯一入口，也是刷新后的恢复点：每次都从当前 shootout 状态
   重新算该显示什么，因此中途刷新能原地接上。 */
function wcShootoutStep(s){
  const run=s.national.wcRun,so=run&&run.shootout;
  if(!so)return wcNext(s);
  const board=()=>`<div class="match-score"><span class="match-team">中国</span><strong>${so.myScore} : ${so.oppScore}</strong><span class="match-team">${esc(so.oppName)}</span></div><p class="dialogue">第 ${Math.min(so.round,5)} 轮${so.sudden?" · 突然死亡":""}</p>`;
  if(so.done)return wcShootoutFinish(s);
  if(so.sudden){
    const p=clamp(.46+(eff(s,"WIL")-70)/260+(hasTalent(s,"big_heart")?.06:0),.25,.75);
    return enqueueFront({title:"突然死亡",body:`${board()}<p>五轮罚完，比分还是平的。谁先罚丢谁出局。</p>`,
      options:[option("走上点球点","一脚定生死",()=>{so.won=Math.random()<p;so.done=true;if(!so.won)so.myMiss=true;wcShootoutFinish(s)})]},"点球大战");
  }
  if(so.round===so.myRound){
    return enqueueFront({title:`第 ${so.round} 轮 · 轮到你了`,
      body:`${board()}<p>裁判把球摆在点球点上，然后退开。走过去的这十几米，看台的声音忽然离你很远。</p>`,
      options:PENALTY_OPTIONS.map(o=>option(o.text,`成功率约 ${Math.round(penaltyRate(s,o)*100)}% · ${o.tip}`,()=>{
        shootoutPlayerKick(s,so,o.id,Math.random);wcShootoutStep(s);
      },o.bold?"danger":""))},"点球大战");
  }
  shootoutAdvance(s,so,Math.random);
  const last=so.kicks.slice(-2);
  enqueueFront({title:`点球大战 · 第 ${Math.min(so.round-1,5)} 轮`,
    body:`${board()}<p>${last[0]&&last[0].scored?"队友稳稳罚进。":"队友那一脚被扑了出来。"}${last[1]&&last[1].scored?"对方也进了。":"对方罚丢了！"}</p>`,
    options:[option("继续","",()=>wcShootoutStep(s))]},"点球大战");
}
/* 点球结束：把胜负写回那场比赛，然后交还给正常的世界杯流程。 */
function wcShootoutFinish(s){
  const run=s.national.wcRun,so=run.shootout,m=run.penMatch;
  m.won=so.won;m.penScore=`${so.myScore}-${so.oppScore}`;
  if(so.myMiss)run.missedDecisivePenalty=!so.won;
  run.shootout=null;run.penMatch=null;
  const champion=m.stage===6&&m.won;
  enqueueFront({title:so.won?`点球 ${so.myScore}-${so.oppScore}，晋级！`:`点球 ${so.myScore}-${so.oppScore}，出局`,
    body:`<p>${so.won?"最后一个球进网的瞬间，替补席上的人全冲了进来。":"你站在中圈没有动。有人过来拍你的背，你没有抬头。"}</p>`,
    options:[option(champion?"捧起大力神杯":so.won?"继续":"接受结果","",()=>{
      run.stage++;
      if(champion)wcFinish(s,true);
      else if(!so.won){run.alive=false;wcFinish(s,false)}
      else wcNext(s);
    })]},"点球大战");
}
```

- [ ] **Step 7: 导出**

在 `API` 里 `teamPenaltyRate,` 之后插入 `newShootout,shootoutAdvance,shootoutPlayerKick,`。

- [ ] **Step 8: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: PASS

- [ ] **Step 9: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "点球大战接入世界杯：逐轮推进，轮到你时停下来

wcMatchSim 里那行隐藏掷骰被真实点球取代——足球里最有仪式感的场面，
原先玩家看都看不见。状态挂在 wcRun.shootout 上且是纯 JSON，
wcShootoutStep 每次都从当前状态重算该显示什么，因此中途刷新能原地接上。

主罚未进且球队出局时记 missedDecisivePenalty，淘汰收尾会专门写它。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: 人物三节点

世界杯全程没有任何人物出场，而这款游戏最强的就是人物。

**Files:**
- Modify: `app.js`（`startWorldCup` 出线分支、`wcNext` 决赛前分支）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 Task 3 的测试块之后插入：

```js
// ===== 人物必须出现在世界杯 =====
assert.ok(/assets\/father\.webp/.test(code)&&/世预赛|出线/.test(code),"出线后父亲出场");
assert.equal(typeof G.wcFinalEve,"function","决赛前夜有专属场景");
const eveLove=G.createInitialState("热恋",allocation,[],"standard","mid");
const eveA=G.wcFinalEve(eveLove);
assert.match(eveA.portrait,/lin-xiaoman/,"恋爱中是小满");
assert.equal(eveA.options.length,2,"决赛前夜是一个带取舍的二选一");
const eveGone=G.createInitialState("分手",allocation,[],"standard","mid");
eveGone.relationship.status="分手";eveGone.relationship.love=0;
assert.match(G.wcFinalEve(eveGone).portrait,/coach-zhou/,"分手后换成周骁，避免尴尬");
// 取舍必须是真的：一个给状态、一个给体能，不是白送
const t1=G.createInitialState("选A",allocation,[],"standard","mid");t1.form=50;t1.fitness=50;
G.wcFinalEve(t1).options[0].apply();
assert.ok(t1.form>50&&t1.fitness<50,"给她打电话：状态↑ 体能↓");
const t2=G.createInitialState("选B",allocation,[],"standard","mid");t2.form=50;t2.fitness=50;
G.wcFinalEve(t2).options[1].apply();
assert.ok(t2.fitness>50&&t2.form===50,"早点睡：体能↑ 状态不变");
```

- [ ] **Step 2: 运行测试确认失败**

Expected: FAIL — `TypeError: G.wcFinalEve is not a function`

- [ ] **Step 3: 实现决赛前夜**

紧挨在 `function wcShootoutStep` 之前插入：

```js
/* 决赛前夜。两个选择都对——这正是本作一贯的取舍手感，落在最该纠结的时刻。
   分手状态下换成周骁，避免出现一个不该出现的人。 */
function wcFinalEve(s){
  const together=["恋人","异地"].includes(s.relationship.status)||s.flags.married;
  if(together)return {title:"决赛前夜",portrait:"assets/lin-xiaoman.webp",
    body:`<p>酒店的窗帘拉不严，走廊的灯从缝里透进来一条。你把手机拿起来又放下，放下又拿起来。</p><p>屏幕上是她三个小时前发的：<span class="dialogue">“睡了吗。”</span>你没回。</p>`,
    options:[
      option("给她打电话","状态+5，体能-6",()=>{change(s,"form",5);change(s,"fitness",-6)}),
      option("明天再说，先睡","体能+6",()=>{change(s,"fitness",6)})]};
  return {title:"决赛前夜",portrait:"assets/coach-zhou.webp",
    body:`<p>周骁的消息在凌晨一点进来，只有一句：<span class="dialogue">“十四岁那年你在铁丝网外面站了多久，还记得吗。”</span></p><p>你记得。你还记得那天他连头都没回。</p>`,
    options:[
      option("回他一条长的","状态+5，体能-6",()=>{change(s,"form",5);change(s,"fitness",-6)}),
      option("放下手机，先睡","体能+6",()=>{change(s,"fitness",6)})]};
}
```

- [ ] **Step 4: 在打进决赛时插入**

`wcNext` 现在是：

```js
function wcNext(s){const run=s.national.wcRun;if(!run||!run.alive)return;if(run.stage<3)wcPlayMatch(s,"均衡");else wcKnockoutChoice(s);}
```

替换为：

```js
function wcNext(s){const run=s.national.wcRun;if(!run||!run.alive)return;
  if(run.stage===6&&!run.eveShown){run.eveShown=true;const d=wcFinalEve(s);return enqueueFront({...d,options:d.options.map(o=>option(o.text,o.effect,()=>{o.apply();wcNext(s)}))},"决赛前夜")}
  if(run.stage<3)wcPlayMatch(s,"均衡");else wcKnockoutChoice(s);}
```

- [ ] **Step 5: 出线后父亲出场**

`startWorldCup` 里出线分支的选项：

```js
options:[q.qualified?option("进入决赛圈抽签","",()=>setupWcFinals(s)):option("接受结果","",()=>{})]
```

替换为：

```js
options:[q.qualified?option("给家里打电话","",()=>enqueueFront({title:"父亲要去看世界杯",portrait:"assets/father.webp",body:`<p>电话响到第七声他才接，背景里有机器的声音——他还在厂里。</p><p>你说队伍出线了。他"嗯"了一声，隔了两秒，问的是：<span class="dialogue">“去那边看一场，要花多少钱。”</span></p><p>你说我给你买票。他说那不用，你比赛要紧。然后又问了一遍：<span class="dialogue">“多少钱。”</span></p>`,options:[option("进入决赛圈抽签","",()=>setupWcFinals(s))]},"世界杯")):option("接受结果","",()=>{})]
```

- [ ] **Step 6: 导出**

在 `API` 里 `newShootout,` 之前插入 `wcFinalEve,`。

- [ ] **Step 7: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

- [ ] **Step 8: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "世界杯人物三节点：父亲、小满/周骁

世界杯全程没有任何人物出场，而这款游戏最强的就是人物。出线后父亲
问的是"要花多少钱"；决赛前夜给一个带真实取舍的二选一——打电话给
状态但熬夜掉体能，早睡给体能。分手状态下换成周骁。

两个选择都对，落在最该纠结的时刻。不占执行点。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 夺冠三段式与淘汰两段式

大多数玩家的世界杯记忆会是**输**。把它写好，比把夺冠写好更影响整体观感。

**Files:**
- Modify: `app.js`（`wcFinish`、`buildEnding`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 Task 4 的测试块之后插入：

```js
// ===== 夺冠与淘汰都要有分量 =====
const champ=G.createInitialState("冠军",allocation,[],"standard","mid");
champ.national.wcRun={stage:7,group:[],ko:[],results:[{opp:"巴西",gf:1,ga:0,goals:1,assists:0,won:true,pen:false,stage:6}],groupWins:2,groupPts:6,alive:true};
G.wcFinish(champ,true);
assert.equal(champ.flags.worldChampion,true,"夺冠置位生涯标记");
assert.ok(champ.honours.some(h=>/世界杯/.test(h.title)),"荣誉里有世界杯");
// buildEnding 返回 {grade,line,loveEnd,coda,age,peak,score,metrics,honours,difficulty}
// 收尾段落是 coda，世界杯那一段进这里
const endChamp=G.buildEnding(champ);
assert.match(endChamp.coda,/世界杯|奖杯/,"退役结局为夺冠单独写一段");
const endPlain=G.buildEnding(G.createInitialState("普通",allocation,[],"standard","mid"));
assert.ok(!/大力神|那只奖杯/.test(endPlain.coda),"没夺过冠的结局不该出现夺冠文案");
// 淘汰路径不能是空收尾
const out=G.createInitialState("止步",allocation,[],"standard","mid");
out.national.wcRun={stage:5,group:[],ko:[],results:[{opp:"法国",gf:0,ga:1,goals:0,assists:0,won:false,pen:false,stage:4}],groupWins:1,groupPts:4,alive:false,missedDecisivePenalty:false};
const outScene=G.wcOutroScene(out);
assert.ok(outScene.portrait,"淘汰收尾带立绘，不是空收尾");
// 踢飞那一脚必须被记住
const missed={...out,national:{...out.national,wcRun:{...out.national.wcRun,missedDecisivePenalty:true}}};
assert.notEqual(G.wcOutroScene(missed).body,outScene.body,"踢飞与常规淘汰的收尾文案不同");
```

- [ ] **Step 2: 运行测试确认失败**

Expected: FAIL — `TypeError: G.wcOutroScene is not a function`

- [ ] **Step 3: 实现淘汰收尾**

紧挨在 `function wcFinish` 之前插入：

```js
/* 淘汰收尾。大多数玩家的世界杯记忆是输，所以这条线不能是空收尾。
   踢飞那一脚会被专门写出来。 */
function wcOutroScene(s){
  const run=s.national.wcRun,age=ageInfo(s).age,again=age+4<=diffOf(s).retireAge;
  if(run&&run.missedDecisivePenalty)return {title:"那一脚",portrait:"assets/father.webp",
    body:`<p>更衣室里没有人说话。有人在解鞋带，解了很久也没解开。</p><p>你父亲的消息进来得很晚：<span class="dialogue">“我在电视上看见你走回去了。”</span>就这一句，没有别的。</p><p>${again?`四年后你${age+4}岁。还来得及。`:`四年后你${age+4}岁。你知道那意味着什么。`}</p>`};
  return {title:"回家的航班",portrait:"assets/lin-xiaoman.webp",
    body:`<p>行李在传送带上转了两圈你才认出自己那个。出口外面人不多，她举着的牌子上什么也没写。</p><p>她说：<span class="dialogue">“我看完了。全部。”</span></p><p>${again?`四年后你${age+4}岁。还来得及。`:`四年后你${age+4}岁。你知道那意味着什么。`}</p>`};
}
```

- [ ] **Step 4: 改写 `wcFinish`**

`wcFinish` 里 `if(champion){...}` 那一段的末尾加上生涯标记，即把：

```js
if(champion){s.honours.unshift({title:"世界杯冠军",season:ageInfo(s).season,icon:"世",detail:"中国国家队"});s.seasonStats.trophies++;unlock("world_cup");change(s,"fame",25);}
```

替换为：

```js
if(champion){s.honours.unshift({title:"世界杯冠军",season:ageInfo(s).season,icon:"世",detail:"中国国家队"});s.seasonStats.trophies++;unlock("world_cup");change(s,"fame",25);s.flags.worldChampion=true;}
```

然后把 `wcFinish` 末尾那个 `enqueueFront({...},"世界杯")` 的 `options` 从：

```js
options:[option("记住这一届","国家队数据已更新",()=>{s.national.wcRun=null;})]
```

替换为（夺冠走三段式，淘汰走两段式）：

```js
options:[option(champion?"走上领奖台":"离开球场","",()=>{
  if(champion)return enqueueFront({title:"大力神杯",portrait:"assets/player.webp",body:`<p>队长把奖杯递过来的时候你没有马上接。你先把手在球衣上擦了两下——手心全是汗，你怕滑。</p><p>金属是凉的。比你想象中重。</p>`,options:[option("找看台","",()=>{
    enqueueFront({title:"看台",portrait:"assets/father.webp",body:`<p>你在人群里一排排地找。找到的时候他正把眼镜摘下来擦，擦了很久。</p><p>旁边那个位置上的人一直在挥手，从终场哨响到现在，没停过。</p>`,options:[option("记住这一届","这一届会写进你的生涯",()=>{s.national.wcRun=null})]},"世界杯冠军");
  })]},"世界杯冠军");
  const sc=wcOutroScene(s);
  enqueueFront({...sc,options:[option("记住这一届","国家队数据已更新",()=>{s.national.wcRun=null})]},"世界杯");
})]
```

- [ ] **Step 5: 结局文本加一段**

`buildEnding` 返回 `{grade,line,loveEnd,coda,...}`，收尾段落是 `coda`，它当前由一个三元构造：

```js
const coda=s.national.called?`退役后你把那封征召信从包底翻出来过一次，…`:"";
```

那段长文案很长，**不要把它抄进 `Edit`**。用两处短小且唯一的替换：

**替换 A** —— `const` 改 `let`：

| old_string | `const coda=s.national.called?` |
|---|---|
| new_string | `let coda=s.national.called?` |

**替换 B** —— 在 `return` 之前插入一行：

| old_string | `return{grade:g.tier,line:g.line,loveEnd,coda,` |
|---|---|
| new_string | `if(s.flags&&s.flags.worldChampion)coda=\`有一年夏天，你们赢到了最后一场。那只奖杯你只抱了很短的时间就要交回去，但那天晚上它的重量，后来很多年你都还记得。<br><br>\`+coda;\n  return{grade:g.tier,line:g.line,loveEnd,coda,` |

两个 `old_string` 在文件里都唯一。**不要试图用「在前面拼一个三元」的写法**——原句结尾是 `:"";`，在前面加左括号会导致括号不配对，直接语法错误。

- [ ] **Step 6: 导出**

在 `API` 里 `wcFinalEve,` 之后插入 `wcOutroScene,wcFinish,`。

- [ ] **Step 7: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

- [ ] **Step 8: 目视确认**

```bash
python3 -m http.server 8766
```

开一局，用浏览器控制台把玩家推到世界杯决赛并打平（`PlayerLife.getState()` 可拿到状态），确认：点球比分板逐轮更新、轮到自己时三个选项的成功率显示合理、夺冠三段式能连续走完、中途刷新页面能回到点球原处。

- [ ] **Step 9: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "夺冠三段式与淘汰两段式

夺冠：终场哨跪在草皮上 → 手碰到奖杯前的那半秒 → 在看台上找那两张脸。
置位 flags.worldChampion，退役结局为它单独写一段。不动金球奖平衡。

淘汰同样给足分量——大多数玩家的世界杯记忆是输，把它写好比把夺冠
写好更影响整体观感。踢飞决定性点球的那一局，收尾会专门写那一脚。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 完工检查表

```bash
node --check app.js && node tests/structure.test.mjs
python3 build_single.py
```

- [ ] 断言数从 247 涨到 280 以上
- [ ] `grep -c "wcRun=null;\$(\"menu\")" app.js` → 0
- [ ] 点球三个选项的成功率在极端属性下仍夹在 35%–92%
- [ ] 中途刷新能回到点球原处
- [ ] 单文件版重建后体积正常（约 1316 KB），`node --check` 通过
