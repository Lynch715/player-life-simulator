# 七维属性重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `stats`(5) + `skills`(4) 两层九个数扁平化为唯一七项主属性（PAC/SHO/PAS/DRI/DEF/PHY/WIL），所有行动与比赛判定统一经由 `eff()` 取值，状态与体能作为分属性敏感度的动态系数介入。

**Architecture:** 单文件 vanilla JS 游戏，无构建步骤、无依赖。`app.js` 顶部是数据常量（`ATTRS`/`TALENTS`/`ACTIONS`/`EVENTS`/`MOMENTS`），中部是纯函数引擎，底部是 DOM 渲染与 `API` 导出。测试通过 `node:vm` 沙箱加载 `app.js` 并读取 `window.PlayerLife`。本次重构不新增文件，改动集中在 `app.js` 的常量区与引擎区。

**Tech Stack:** Vanilla JS (ES2020)、`node:test` 风格的 `node:assert/strict` 脚本、`node:vm`、localStorage 存档。无 npm 依赖，无 transpile。

**Spec:** `docs/superpowers/specs/2026-08-01-七维属性重构-design.md`

---

## 阅读须知

**`app.js` 是超长行风格**（1460 行 / 232KB，单行常达 2000+ 字符）。用 `Edit` 工具做精确字符串替换，**不要**尝试重排格式或加换行——那会产生无法审阅的巨型 diff。

**每个任务结束后必跑：**

```bash
node --check app.js && node tests/structure.test.mjs
```

`node --check` 只验语法；真正的回归保护来自 `tests/structure.test.mjs`（178 行，覆盖创建、成长、流派、比赛、世界杯、存档规范化）。

---

## File Structure

| 文件 | 职责 | 本次改动 |
|---|---|---|
| `app.js` | 全部游戏逻辑与渲染 | 常量区新增 `ATTRS`；引擎区新增 `gain`/`cond`/`eff`/`atk`/`def`/`migrateV2toV3`；删除 `CORE_STATS`/`gainStat`/`gainSkill`/`weightedStatScore`；改写 ~120 处属性调用与 4 层判定；渲染区改写 3 个面板 |
| `tests/structure.test.mjs` | 架构回归测试 | 改写依赖 `state.stats`/`state.skills` 的断言；新增 7 组断言 |
| `index.html` | 骨架 DOM | 侧栏面板改名、删心情行、创建页加身高选择器 |
| `style.css` | 全部样式 | `.plan-*` 换成 `.plan-seg-*`；新增 `.radar-*`、`.height-picker` |
| `README.md` | 玩法说明 | 属性描述同步 |

**不新增文件。** 项目刻意保持「双击 index.html 即可游玩」的零依赖形态。

---

## 属性映射总表（贯穿全程，反复查阅）

**写入侧**（`gainStat`/`gainSkill` → `gain`）：

| 旧调用 | 新调用 |
|---|---|
| `gainStat(s,"speed",n,t)` | `gain(s,"PAC",n,t)` |
| `gainStat(s,"burst",n,t)` | `gain(s,"PAC",n,t)` |
| `gainStat(s,"height",n,t)` | `gain(s,"PHY",n,t)` |
| `gainStat(s,"stamina",n,t)` | `gain(s,"PHY",n,t)` |
| `gainStat(s,"will",n,t)` | `gain(s,"WIL",n,t)` |
| `gainSkill(s,"finishing",n,t)` | `gain(s,"SHO",n,t)` |
| `gainSkill(s,"dribble",n,t)` | `gain(s,"DRI",n,t)` |
| `gainSkill(s,"vision",n,t)` | `gain(s,"PAS",n,t)` |
| `gainSkill(s,"setPiece",n,t)` | `gain(s,"PAS",n,t)` |

**读取侧**（`s.stats.X` / `s.skills.X` → `s.attrs.Y`，判定处再包一层 `eff()`）：

| 旧读取 | 新读取 |
|---|---|
| `s.stats.speed` / `s.stats.burst` | `s.attrs.PAC` |
| `s.stats.height` / `s.stats.stamina` | `s.attrs.PHY` |
| `s.stats.will` | `s.attrs.WIL` |
| `s.skills.finishing` | `s.attrs.SHO` |
| `s.skills.dribble` | `s.attrs.DRI` |
| `s.skills.vision` / `s.skills.setPiece` | `s.attrs.PAS` |

**`MOMENTS` 的 `o.stat` 字段**（关键时刻选项的关键属性）：`finishing`→`SHO`、`dribble`→`DRI`、`vision`→`PAS`、`setPiece`→`PAS`、`height`→`PHY`、`burst`→`PAC`。

---

## 阶段总览

| 阶段 | 任务 | 交付物 |
|---|---|---|
| 1 数据模型 | Task 1–3 | `ATTRS`、`gain()`、存档迁移 |
| 2 判定内核 | Task 4–6 | `cond()`/`eff()`/`atk()`/`def()`、四层判定 |
| 3 行动与流派 | Task 7–9 | 五条训练、小满互动、流派软上限 |
| 4 删除废弃变量 | Task 10–11 | 删心情、删 `teamFit`（各自独立提交） |
| 5 UI | Task 12–15 | 雷达图、职责分段控件、创建页、生涯页 |

**Task 1 未完成前不得开始 Task 4** —— 判定内核依赖 `s.attrs` 已经存在。

---

## Task 1: 引入 `ATTRS` 与 `gain()`，切换全部属性调用

这是最大的一个任务：新模型落地 + ~120 处调用点改写 + 测试同步，**必须在同一个提交内完成**，否则中间状态的游戏跑不起来。

**Files:**
- Modify: `app.js:13-19`（`CORE_STATS` → `ATTRS`）
- Modify: `app.js:211-213`（`gainStat`/`gainSkill`/`overall`）
- Modify: `app.js:220-226`（`createInitialState`）
- Modify: `app.js:1458`（`API` 导出）
- Modify: `app.js` 全文约 120 处 `gainStat`/`gainSkill`/`s.stats.`/`s.skills.` 调用
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 `tests/structure.test.mjs` 中，把 `const allocation=...` 那一行之后的旧断言块替换掉。找到这段：

```js
const allocation={height:4,speed:4,burst:4,stamina:4,will:4};
const state=G.createInitialState("测试前锋",allocation,G.TALENTS.slice(0,3).map(x=>x.id));
assert.equal(Object.values(state.allocation).reduce((a,b)=>a+b,0),20);
```

替换为：

```js
const ATTR_KEYS=["PAC","SHO","PAS","DRI","DEF","PHY","WIL"];
const allocation={PAC:4,SHO:4,PAS:3,DRI:3,DEF:3,PHY:3,WIL:4};
const state=G.createInitialState("测试前锋",allocation,G.TALENTS.slice(0,3).map(x=>x.id),"standard","mid");

// 七项属性是唯一一层，旧的两层结构必须消失
assert.equal(G.ATTRS.length,7,"exactly seven core attributes");
assert.deepEqual(G.ATTRS.map(x=>x.key),ATTR_KEYS,"attribute order drives the radar chart vertices");
assert.equal(Math.abs(G.ATTRS.reduce((t,x)=>t+x.w,0)-1)<1e-9,true,"OVR weights sum to exactly 1");
assert.equal("stats" in state,false,"the old stats layer is gone");
assert.equal("skills" in state,false,"the old skills layer is gone");
ATTR_KEYS.forEach(k=>assert.ok(Number.isFinite(state.attrs[k]),`${k} is a real number`));
assert.equal(Object.keys(state.attrs).length,7,"no stray attribute keys");
assert.equal(Object.values(state.allocation).reduce((a,b)=>a+b,0),24,"creation spends 24 points across seven attributes");
```

再把下面这四行旧断言：

```js
assert.ok(Number.isFinite(state.skills.finishing),"shooting is a real technical attribute");
assert.ok(Number.isFinite(state.skills.setPiece),"set pieces are a real technical attribute");
assert.equal("clutch" in state.skills,false,"key moments are derived rather than a separate attribute");
const finishingBefore=state.skills.finishing;
```

替换为：

```js
assert.equal("clutch" in state.attrs,false,"key moments are derived rather than a separate attribute");
const shoBefore=state.attrs.SHO;
```

**并在同一处插入防漏改断言**（spec §6.2 第 4 条，被标为「重点」）。约 120 处调用点手改必然有漏网的，靠这一组兜底：

```js
// ===== 防漏改：没有任何代码路径还在碰旧属性 =====
// 跑一遍每个行动，断言它只动了七个合法 key，且没有凭空冒出新 key。
G.ACTIONS.forEach(a=>{
  const t=G.createInitialState("漏网探针",allocation,[],"standard","mid");
  t.flags.intimateUnlocked=true;t.money=999;
  const keysBefore=Object.keys(t.attrs).sort().join(",");
  try{a.run(t)}catch(e){assert.fail(`ACTION ${a.id} threw: ${e.message}`)}
  assert.equal(Object.keys(t.attrs).sort().join(","),keysBefore,
    `${a.id} invented or dropped an attribute key`);
  ATTR_KEYS.forEach(k=>assert.ok(Number.isFinite(t.attrs[k]),
    `${a.id} left ${k} as ${t.attrs[k]}`));
  assert.equal("stats" in t,false,`${a.id} resurrected the stats layer`);
  assert.equal("skills" in t,false,`${a.id} resurrected the skills layer`);
});
// 每个关键时刻选项的 stat 都必须是七个合法 key 之一
G.MOMENTS.forEach(m=>m.options.forEach(o=>
  assert.ok(ATTR_KEYS.includes(o.stat),
    `MOMENT ${m.id} option "${o.text}" points at stale stat "${o.stat}"`)));
// 源码里不该再有旧标识符
assert.ok(!/\bCORE_STATS\b/.test(code),"CORE_STATS is retired");
assert.ok(!/\bgainStat\b|\bgainSkill\b/.test(code),"the two growth functions merged into gain()");
assert.ok(!/s\.stats\.|s\.skills\./.test(code),"no source path still reads the old two-tier model");
```

并把紧随其后的两组训练断言：

```js
G.ACTIONS.find(x=>x.id==="train_box").run(state);
assert.ok(state.skills.finishing>finishingBefore,"technical training increases finishing");
const setPieceBefore=state.skills.setPiece;
G.ACTIONS.find(x=>x.id==="train_box").run(state);
assert.ok(state.skills.setPiece>setPieceBefore,"technical training increases set-piece ability");
```

替换为：

```js
G.ACTIONS.find(x=>x.id==="train_box").run(state);
assert.ok(state.attrs.SHO>shoBefore,"finishing training raises SHO");
const pasBefore=state.attrs.PAS;
G.ACTIONS.find(x=>x.id==="train_box").run(state);
assert.ok(state.attrs.PAS>pasBefore,"finishing training also feeds PAS via set pieces");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `AssertionError: exactly seven core attributes`（`G.ATTRS` 是 `undefined`，`.length` 抛 TypeError 也算预期失败）

- [ ] **Step 3: 用 `ATTRS` 替换 `CORE_STATS`**

`app.js:13-19`，把整个 `const CORE_STATS=[...]` 块替换为：

```js
const ATTRS=[
  {key:"PAC",name:"速度",sub:"冲刺与启动",icon:"»",w:.21},
  {key:"SHO",name:"射门",sub:"终结与打门",icon:"◎",w:.25},
  {key:"PAS",name:"传球",sub:"视野与定位球",icon:"▣",w:.08},
  {key:"DRI",name:"盘带",sub:"控球与过人",icon:"✦",w:.14},
  {key:"DEF",name:"防守",sub:"逆抢与协防",icon:"▰",w:.05},
  {key:"PHY",name:"身体",sub:"对抗与续航",icon:"▲",w:.13},
  {key:"WIL",name:"意志",sub:"抗压与临场",icon:"◆",w:.14}
];
const ATTR_KEYS=ATTRS.map(a=>a.key);
```

- [ ] **Step 4: 合并成长函数**

`app.js:211-212`，把 `gainStat` 与 `gainSkill` 两个函数整体替换为单个 `gain`：

```js
function gain(s,key,n,tag){
  const d=diffOf(s);let mult=1;
  if(hasTalent(s,"explosive_start")&&(key==="PAC"||key==="DRI"))mult+=.25;
  if(hasTalent(s,"engine")&&key==="PHY")mult+=.25;
  if(hasTalent(s,"aerial_king")&&key==="PHY")mult+=.2;
  if(hasTalent(s,"big_heart")&&key==="WIL")mult+=.18;
  if(hasTalent(s,"football_iq")&&(key==="PAS"||tag==="tactics"))mult+=.25;
  if(hasTalent(s,"free_kick")&&key==="PAS")mult+=.3;
  if(hasTalent(s,"box_instinct")&&key==="SHO")mult+=.14;
  if(hasTalent(s,"ambidextrous")&&(key==="SHO"||key==="DRI"))mult+=.12;
  if(tag&&hasTalent(s,"training_rat")&&chance(.08))mult+=.5;
  if(n>0){
    mult*=d.growth*softFactor(s.attrs[key],d,styleCapLevel(s,key))*trainMult(s)*actionMult;
    if(key==="SHO"&&s.flags&&s.flags.outOfPosition)mult*=.7;
  }
  s.attrs[key]=clamp(s.attrs[key]+n*mult,1,99);
}
```

`styleCapLevel` 在 Task 9 才实现真正逻辑；本任务先加一个返回 0 的桩函数，紧跟在 `gain` 之后：

```js
function styleCapLevel(){return 0}   // Task 9 接入流派软上限
```

`softFactor`（`app.js:12`）加第三个参数，默认 0，保证本任务行为不变：

```js
function softFactor(cur,d,lv=0){const t=lv*5;let f=cur>=88+t?.25:cur>=82+t?.4:cur>=75+t?.6:cur>=68+t?.8:1;if(cur>=68+t)f/=d.soft;return f}
```

- [ ] **Step 5: 改写 `overall()`**

`app.js:213` 整行替换：

```js
function overall(s){return Math.round(ATTRS.reduce((t,a)=>t+s.attrs[a.key]*a.w,0))}
```

- [ ] **Step 6: 改写 `createInitialState()`**

`app.js:220-222`，把函数签名与前两行替换为：

```js
function createInitialState(name="陈逐风",allocation={PAC:4,SHO:4,PAS:3,DRI:3,DEF:3,PHY:3,WIL:4},talents=[],difficulty="standard",heightTier="mid"){
  const attrs={};ATTRS.forEach(x=>attrs[x.key]=42+(allocation[x.key]||0)*4);
  const tierAdj={short:{PHY:-4,PAC:3,DRI:2},mid:{},tall:{PHY:5,PAC:-3,DRI:-2}}[heightTier]||{};
  Object.entries(tierAdj).forEach(([k,v])=>attrs[k]=clamp(attrs[k]+v,1,99));
  const heightCm={short:174,mid:182,tall:191}[heightTier]||182;
```

然后在 `app.js:223` 的巨型 `const state={...}` 里做三处替换：

- `heightCm:Math.round(heightCm),` → `heightCm,heightTier,`
- `talents:[...talents],stats,skills:{finishing:48+(allocation.will||0)*.5,dribble:46+(allocation.burst||0)*.5,vision:43,setPiece:41},` → `talents:[...talents],attrs,`
- `version:VERSION,` 保持不变（`VERSION` 在 Task 3 升到 3）

- [ ] **Step 7: 批量改写全部调用点**

按「属性映射总表」逐个替换。用这条命令拿到完整清单（每处都要人工确认语义，**不要盲目 sed**）：

```bash
grep -n "gainStat(\|gainSkill(\|s\.stats\.\|s\.skills\.\|S\.stats\.\|S\.skills\.\|elite\.stats\|elite\.skills\|weak\.stats\|weak\.skills" app.js
```

需要特别留意的 5 处**非机械**改写：

| 位置 | 原代码 | 改成 |
|---|---|---|
| `app.js:283` | `s.stats.speed=clamp(s.stats.speed-1)` | `s.attrs.PAC=clamp(s.attrs.PAC-1,1,99)` |
| `app.js:289` | `s.stats.will=clamp(s.stats.will-1)` | `s.attrs.WIL=clamp(s.attrs.WIL-1,1,99)` |
| `app.js:780` | `cond:s=>s.stats.height>=60\|\|styleOf(s,"target")>=1` | `cond:s=>s.attrs.PHY>=60\|\|styleOf(s,"target")>=1` |
| `app.js:908` | `s.skills.setPiece>68` | `s.attrs.PAS>68` |
| `app.js:910` | `type==="dribble"?((s.stats.burst+s.skills.dribble)/2)` | `type==="dribble"?s.attrs.DRI`（不再取平均，DRI 已经合并了这两者） |

`MOMENTS` 里每个选项的 `stat:"..."` 按总表改。改完自查：

```bash
grep -o 'stat:"[a-zA-Z]*"' app.js | sort -u
```

Expected: 只剩 `stat:"PAC"` `stat:"SHO"` `stat:"PAS"` `stat:"DRI"` `stat:"DEF"` `stat:"PHY"` `stat:"WIL"` 七种。

- [ ] **Step 8: 更新 `API` 导出**

`app.js:1458`，在 `const API={VERSION,TALENTS,` 之后插入 `ATTRS,ATTR_KEYS,gain,softFactor,`。

- [ ] **Step 9: 清理测试里剩余的旧引用**

`tests/structure.test.mjs` 中还有三处：

```js
Object.keys(elite.stats).forEach(k=>elite.stats[k]=88);
Object.keys(elite.skills).forEach(k=>elite.skills[k]=88);
```
→
```js
ATTR_KEYS.forEach(k=>elite.attrs[k]=88);
```

```js
Object.keys(weak.stats).forEach(k=>weak.stats[k]=1);
Object.keys(weak.skills).forEach(k=>weak.skills[k]=1);
```
→
```js
ATTR_KEYS.forEach(k=>weak.attrs[k]=1);
```

以及 `const elite=G.createInitialState("精英",allocation,[...])` 与 `const late=G.createInitialState("校园前锋",allocation,[...])` 两处——`allocation` 已在 Step 1 改成七项，无需再动。

- [ ] **Step 10: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: `模拟球员 architecture test passed`

- [ ] **Step 11: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "重构：属性扁平化为七项 PAC/SHO/PAS/DRI/DEF/PHY/WIL

删除 CORE_STATS/gainStat/gainSkill 两层结构，合并为单个 gain()。
overall() 改为 ATTRS 的 w 加权和（权重合计 1.00，按前锋定：
SHO+PAC 占 46%，DEF 仅 5% 但通过球队贡献分影响胜负）。
createInitialState 接受 heightTier，身高从主属性降级为创建时的
独立选择，影响 PHY/PAC/DRI 初值。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: 天赋 tag 重映射与文案同步

`gain()` 里已经按新 key 匹配天赋，但天赋自身的 `tags` 数组和描述文案还停在旧词汇上。

**Files:**
- Modify: `app.js:20-35`（`TALENTS`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 `tests/structure.test.mjs` 的 `assert.equal(G.TALENTS.length,20,...)` 之后插入：

```js
// 天赋 tag 必须指向真实属性，否则 gain() 里的加成永远不会触发
const TAG_WHITELIST=new Set([...ATTR_KEYS,"injury","fitness","goal","clutch","team","tactics","language","overseas","love","header","dribble","finish","training","scout","money","media"]);
G.TALENTS.forEach(t=>t.tags.forEach(tag=>
  assert.ok(TAG_WHITELIST.has(tag),`talent ${t.id} has unknown tag "${tag}"`)));
const attrTagged=G.TALENTS.filter(t=>t.tags.some(x=>ATTR_KEYS.includes(x)));
assert.ok(attrTagged.length>=8,"most talents should hang off a real attribute");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `talent explosive_start has unknown tag "burst"`

- [ ] **Step 3: 改写 tags**

`app.js:21-33`，按下表改每个天赋的 `tags` 首项：

| id | 旧 tags | 新 tags |
|---|---|---|
| `explosive_start` | `["burst","dribble"]` | `["PAC","DRI"]` |
| `box_instinct` | `["finish","goal"]` | `["SHO","goal"]` |
| `big_heart` | `["will","clutch"]` | `["WIL","clutch"]` |
| `iron_man` | `["injury","stamina"]` | `["injury","PHY"]` |
| `aerial_king` | `["height","header"]` | `["PHY","header"]` |
| `free_kick` | `["setpiece","goal"]` | `["PAS","goal"]` |
| `ambidextrous` | `["finish","dribble"]` | `["SHO","DRI"]` |
| `captain` | `["will","team"]` | `["WIL","team"]` |
| `football_iq` | `["vision","tactics"]` | `["PAS","tactics"]` |
| `engine` | `["stamina","fitness"]` | `["PHY","fitness"]` |

同时改三条描述文案（`desc` 字段），让玩家看到的和实际生效的一致：

- `explosive_start`：`"爆发训练收益+25%，突破第一步成功率提高。"` → `"速度与盘带训练收益+25%，突破第一步成功率提高。"`
- `engine`：`"耐力训练收益+25%，比赛后体能消耗降低。"` → `"身体训练收益+25%，比赛后体能消耗降低。"`
- `big_heart`：`"意志类训练收益+18%..."` → `"意志成长+18%..."`（意志已不属于任何训练，见 spec §3.4）

- [ ] **Step 4: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "天赋 tag 重映射到七项属性，同步三条描述文案

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: 存档迁移 v2 → v3

**关键前提**：`loadGame()` 现在是 `if(data.version!==VERSION)return null` —— 旧档被**静默丢弃**。要迁移就必须先改这个分支。

**Files:**
- Modify: `app.js:3`（`VERSION`）
- Modify: `app.js` `normalizeSave` 与 `loadGame`
- Modify: `app.js:1458`（`API` 导出）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 `tests/structure.test.mjs` 末尾 `for(const file of [...])` 之前插入：

```js
// ===== 存档迁移 v2 → v3 =====
function v2Save(o){
  return {version:2,stats:{height:o.height,speed:o.speed,burst:o.burst,stamina:o.stamina,will:o.will},
    skills:{finishing:o.finishing,dribble:o.dribble,vision:o.vision,setPiece:o.setPiece},
    form:60,morale:76,teamFit:63,matchPlan:"box"};
}
const oldOVR=o=>Math.round(o.height*.1+o.speed*.15+o.burst*.18+o.stamina*.12+o.will*.15
  +o.finishing*.16+o.dribble*.08+o.vision*.04+o.setPiece*.02);
const BUILDS={
  "新手14岁":{height:58,speed:58,burst:58,stamina:58,will:58,finishing:50,dribble:48,vision:43,setPiece:41},
  "均衡型":{height:58,speed:70,burst:72,stamina:65,will:68,finishing:72,dribble:64,vision:55,setPiece:48},
  "支点型":{height:82,speed:55,burst:58,stamina:78,will:75,finishing:76,dribble:52,vision:50,setPiece:45},
  "速度型":{height:45,speed:85,burst:88,stamina:62,will:60,finishing:70,dribble:80,vision:52,setPiece:40},
  "巅峰32岁":{height:70,speed:88,burst:90,stamina:84,will:92,finishing:93,dribble:86,vision:78,setPiece:70}
};
for(const [label,o] of Object.entries(BUILDS)){
  const m=G.migrateV2toV3(v2Save(o));
  assert.equal(m.version,3,`${label}: version bumped`);
  ATTR_KEYS.forEach(k=>{
    assert.ok(Number.isFinite(m.attrs[k]),`${label}: ${k} is finite`);
    assert.ok(m.attrs[k]>=1&&m.attrs[k]<=99,`${label}: ${k} in range`);
  });
  assert.equal("stats" in m,false,`${label}: old stats dropped`);
  assert.equal("skills" in m,false,`${label}: old skills dropped`);
  assert.equal("morale" in m,false,`${label}: morale folded into form`);
  assert.equal("teamFit" in m,false,`${label}: teamFit discarded`);
  assert.ok(Math.abs(G.overall(m)-oldOVR(o))<=2,
    `${label}: OVR drift ${G.overall(m)-oldOVR(o)} exceeds ±2`);
}
// 损坏的档不能带着 NaN 进游戏
assert.equal(G.migrateV2toV3({version:2,stats:{height:"坏"},skills:{}}),null,"corrupt save migrates to null");
assert.equal(G.migrateV2toV3(null),null,"null in, null out");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `TypeError: G.migrateV2toV3 is not a function`

- [ ] **Step 3: 升 `VERSION`**

`app.js:3`：`VERSION=2` → `VERSION=3`

- [ ] **Step 4: 实现 `migrateV2toV3`**

紧挨在 `function normalizeSave(d){` 之前插入：

```js
/* v2(stats+skills 九个数) → v3(attrs 七项)。任何异常或 NaN 一律返回 null，
   由 loadGame 回退到「提示重开」，绝不让损坏的档进入游戏。 */
function migrateV2toV3(d){
  try{
    if(!d||typeof d!=="object"||!d.stats||!d.skills)return null;
    const st=d.stats,sk=d.skills,num=v=>typeof v==="number"&&Number.isFinite(v)?v:NaN;
    const attrs={
      PAC:(num(st.speed)+num(st.burst))/2,
      SHO:num(sk.finishing),
      PAS:num(sk.vision)*.65+num(sk.setPiece)*.35,
      DRI:num(sk.dribble),
      PHY:(num(st.height)+num(st.stamina))/2,
      WIL:num(st.will),
      DEF:20+num(st.stamina)*.35+num(st.will)*.15
    };
    for(const k of ATTR_KEYS){
      if(!Number.isFinite(attrs[k]))return null;
      attrs[k]=clamp(attrs[k],1,99);
    }
    const out={...d,version:3,attrs};
    out.form=clamp(Math.round(num(d.form)*.65+num(d.morale)*.35));
    if(!Number.isFinite(out.form))return null;
    delete out.stats;delete out.skills;delete out.morale;delete out.teamFit;delete out.allocation;
    if(!out.heightTier)out.heightTier="mid";
    if(!Number.isFinite(out.heightCm))out.heightCm=182;
    return out;
  }catch(e){return null}
}
```

- [ ] **Step 5: 改 `loadGame` 的版本分支**

整行替换 `function loadGame(){...}`：

```js
function loadGame(){try{const raw=localStorage.getItem(SAVE_KEY);if(!raw)return null;let data=JSON.parse(raw);
  if(data.version===2){data=migrateV2toV3(data);if(!data){localStorage.removeItem(SAVE_KEY);return null}}
  if(data.version!==VERSION)return null;return normalizeSave(data)}catch(e){return null}}
```

- [ ] **Step 6: 导出**

`app.js:1458`，在 `normalizeSave,` 之后插入 `migrateV2toV3,`。

- [ ] **Step 7: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: PASS，五种 build 的 OVR 漂移全在 ±2 内

- [ ] **Step 8: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "存档 v2→v3 自动迁移，五种 build 的 OVR 漂移经测试卡在 ±2 内

loadGame 原先对旧版本直接返回 null（静默弃档），现在先走 migrate。
迁移失败或产出 NaN 时清档回退，不让损坏数据进入游戏。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: 判定内核 `cond()` / `eff()` / `atk()` / `def()`

只加函数，不改任何调用点——保证这一步单独提交后游戏行为完全不变，便于二分定位。

**Files:**
- Modify: `app.js`（`softFactor` 之后插入四个函数）
- Modify: `app.js:1458`（`API` 导出）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 `tests/structure.test.mjs` 的存档迁移块之前插入：

```js
// ===== 判定内核：有效属性 =====
const probe=G.createInitialState("探针",allocation,[],"standard","mid");
ATTR_KEYS.forEach(k=>probe.attrs[k]=70);

// 基准点：form 55 / fitness 72 时 cond 恰为 1，有效属性等于裸值
probe.form=55;probe.fitness=72;
assert.ok(Math.abs(G.cond(probe)-1)<1e-9,"cond is exactly 1 at the calibration point");
ATTR_KEYS.forEach(k=>assert.ok(Math.abs(G.eff(probe,k)-70)<1e-9,`${k} unmodified at cond=1`));

// 巅峰与崩盘都被 clamp 住
probe.form=100;probe.fitness=100;assert.equal(G.cond(probe),1.08,"cond ceiling");
probe.form=0;probe.fitness=0;assert.equal(G.cond(probe),.78,"cond floor");

// 敏感度方向：体能崩盘时身体掉得比意志狠得多
const phyLow=G.eff(probe,"PHY"),wilLow=G.eff(probe,"WIL");
assert.ok(phyLow<wilLow,"PHY collapses harder than WIL when exhausted");
assert.ok(phyLow<50&&phyLow>45,`PHY 70 should land near 48 at rock bottom, got ${phyLow.toFixed(1)}`);
assert.ok(wilLow>62&&wilLow<66,`WIL 70 should hold near 64 at rock bottom, got ${wilLow.toFixed(1)}`);

// 单调性：状态与体能上升，任何属性的有效值都不下降
ATTR_KEYS.forEach(k=>{
  probe.form=20;probe.fitness=30;const lo=G.eff(probe,k);
  probe.form=90;probe.fitness=95;const hi=G.eff(probe,k);
  assert.ok(hi>lo,`${k} improves with better condition`);
});

// 球队贡献分：权重合计为 1，全 70 时两个分值都回到 70
probe.form=55;probe.fitness=72;
assert.ok(Math.abs(G.atk(probe)-70)<1e-9,"atk weights sum to 1");
assert.ok(Math.abs(G.def(probe)-70)<1e-9,"def weights sum to 1");
// DEF 是防守分的主导项，SHO 完全不参与
probe.attrs.DEF=90;assert.ok(G.def(probe)>70,"DEF drives the defensive contribution");
probe.attrs.DEF=70;probe.attrs.SHO=99;
assert.ok(Math.abs(G.def(probe)-70)<1e-9,"shooting never inflates the defensive score");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `TypeError: G.cond is not a function`

- [ ] **Step 3: 实现四个函数**

紧跟在 `softFactor` 定义之后插入：

```js
/* ========== 判定内核 ==========
   cond() 把状态与体能压成一个 0.78~1.08 的系数；eff() 是所有判定读取属性的
   唯一入口。任何判定公式都不得再单独掺 form/fitness——那会双重计数。      */
function cond(s){return clamp(1+(s.form-55)/500+(s.fitness-72)/600,.78,1.08)}
const COND_SENS={PHY:1.4,PAC:1.3,DEF:1.2,DRI:1.0,SHO:.7,PAS:.6,WIL:.4};
function eff(s,k){return s.attrs[k]*(1+(cond(s)-1)*COND_SENS[k])}
function atk(s){return eff(s,"SHO")*.35+eff(s,"PAC")*.25+eff(s,"DRI")*.20+eff(s,"PAS")*.20}
function def(s){return eff(s,"DEF")*.60+eff(s,"PHY")*.25+eff(s,"WIL")*.15}
```

注意 `clamp` 的签名是 `clamp(n,min=0,max=100)`，这里显式传了 `.78,1.08` 两个边界。

- [ ] **Step 4: 导出**

`app.js:1458`，在 `overall,` 之后插入 `cond,eff,atk,def,COND_SENS,`。

- [ ] **Step 5: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "判定内核：cond()/eff()/atk()/def()

状态与体能压成单一系数，按 COND_SENS 分属性作用：
体能见底时 PHY 70→48、WIL 70→64——累垮的是身体不是意志。
本次只加函数不改调用点，游戏行为保持不变。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 四层判定改吃 `eff()`，删干净双重计数

**本次重构最大的坑**：`eff()` 已经把状态体能吃进去了，旧公式里每一处 `form`/`fitness` 散项必须删干净，否则状态好时强度失控。

**Files:**
- Modify: `app.js:856-868`（`momentSuccessRate`）
- Modify: `app.js:884-922`（`prepareMatch`）
- Modify: `app.js:718`（`weightedStatScore` 删除）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 Task 4 的测试块之后插入：

```js
// ===== 无双重计数 =====
// eff() 已经吃掉状态与体能。若旧公式的 form/fitness 散项没删干净，
// 状态从 30 拉到 90 会产生远超预期的涨幅。
const dc=G.createInitialState("双计探针",allocation,[],"standard","mid");
ATTR_KEYS.forEach(k=>dc.attrs[k]=70);
const m0=G.MOMENTS.find(x=>x.id==="counter_break"),o0=m0.options[0];
dc.form=30;dc.fitness=60;const rLo=G.momentSuccessRate(dc,m0,o0,60,false);
dc.form=90;dc.fitness=60;const rHi=G.momentSuccessRate(dc,m0,o0,60,false);
const lift=rHi-rLo;
assert.ok(lift>0,"better form still helps");
assert.ok(lift<0.06,`form 30→90 should lift success by <6pp via eff() alone, got ${(lift*100).toFixed(1)}pp`);

// weightedStatScore 已被 atk()/def() 取代
assert.equal(G.weightedStatScore,undefined,"weightedStatScore is gone");

// 关键时刻仍夹在 15%~85%
assert.ok(G.momentSuccessRate(dc,m0,o0,99,true)>=0.15);
assert.ok(G.momentSuccessRate(dc,m0,o0,1,false)<=0.85);
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `form 30→90 should lift success by <6pp via eff() alone, got 6.0pp`（旧公式的 `s.form/1000` 散项还在）

- [ ] **Step 3: 改写 `momentSuccessRate`**

`app.js:856-868`，整个函数替换为：

```js
function momentSuccessRate(s,m,o,opp,behind){
  let p=.35+(eff(s,o.stat)-opp)*.008+MOMENT_RISK[o.risk];
  if(hasTalent(s,"box_instinct")&&o.stat==="SHO")p+=.06;
  if(hasTalent(s,"explosive_start")&&(o.stat==="DRI"||o.stat==="PAC"))p+=.06;
  if(hasTalent(s,"aerial_king")&&o.stat==="PHY")p+=.08;
  if(hasTalent(s,"free_kick")&&o.stat==="PAS")p+=.1;
  if(hasTalent(s,"football_iq")&&o.stat==="PAS")p+=.05;
  if(hasTalent(s,"big_heart")&&behind)p+=.05;
  if(styleOf(s,"box")>=1&&o.stat==="SHO")p+=.05;
  if(styleOf(s,"burst")>=1&&(m.id==="counter_break"||m.id==="press_trigger"))p+=.06;
  if(styleOf(s,"box")>=3&&m.id==="late_chase"&&o.stat==="SHO")p+=.08;
  return clamp(p,.15,.85);
}
```

第一行删掉了 `+s.form/1000+s.fitness/2000`，并把 `o.stat in s.skills?...` 的取值分支换成 `eff(s,o.stat)`。

- [ ] **Step 4: 删除 `weightedStatScore`**

`app.js:718` 整行删除：

```js
function weightedStatScore(s){return overall(s)*.58+s.form*.18+s.fitness*.12+s.morale*.08+s.coachFavor*.04+loveSupport(s)}
```

- [ ] **Step 5: 改写 `prepareMatch` 的球队强弱计算**

`app.js:888-896`，把从 `const rawStart=` 到 `const myXg=` 之间的片段替换为：

```js
  const rawStart=.42+(overall(s)-club.strength)/50+(s.coachFavor-50)/170+(hasTalent(s,"super_sub")?-.04:0);
  const starts=!injured&&rng()<clamp(rawStart,.15,.9),plays=!injured&&(starts||rng()<.74+(hasTalent(s,"super_sub")?.15:0));
  const role=starts?"首发":plays?"替补":"未出场";
  const talentBonus=(hasTalent(s,"big_heart")&&opts.important?4:0)+(hasTalent(s,"home_favorite")&&home?3:0)+(hasTalent(s,"super_sub")&&!starts&&plays?4:0)+(hasTalent(s,"final_master")&&opts.final?5:0);
  const myAtk=atk(s)+talentBonus+rndFloat(rng,-12,12),myDef=def(s);
  let clubEdge=(club.strength-opp.strength)+(home?3:-2);
  if(plays)clubEdge+=(myAtk-club.strength)*.14+(myDef-club.strength)*.06;
  if(plan==="press")clubEdge+=(myDef-55)*.10;
  if(styleOf(s,"play")>=3)clubEdge+=2;
```

三处变化：`rawStart` 删掉 `(s.form-50)/220` 和 `((s.teamFit||50)-50)/300`（`teamFit` 由 Task 11 收尾，此处直接不再引用）；`playerLevel`/`weightedStatScore` 换成 `myAtk`/`myDef`；逼抢从固定 `+2.5` 改成 `(myDef-55)*.10`。

- [ ] **Step 6: 改写射门尝试循环**

`app.js:910-915`，把 `const type=...` 到 `const isAssist=...` 四行替换为：

```js
      const type=types[Math.floor(rng()*types.length)];
      const ATTR_OF={finish:"SHO",dribble:"DRI",header:"PHY",setpiece:"PAS",pass:"PAS"};
      const stat=eff(s,ATTR_OF[type]||"SHO");
      let p=.25+(stat-45)/90+talentBonus/100;
      if(type==="finish"&&hasTalent(s,"box_instinct"))p+=.09;if(type==="header"&&hasTalent(s,"aerial_king"))p+=.13;if(type==="setpiece"&&hasTalent(s,"free_kick"))p+=.16;if(type==="dribble"&&hasTalent(s,"explosive_start"))p+=.08;
      const success=rng()<clamp(p,.16,.88);if(success)keyWins++;else failures++;
      const isGoal=success&&["finish","header","setpiece"].includes(type)&&goals<gf&&rng()<clamp(.38+(eff(s,"SHO")-52)/100+goalBonus,.28,.82);
      const isAssist=success&&["pass","dribble"].includes(type)&&assists+goals<gf&&rng()<clamp(.26+(eff(s,"PAS")-45)/150+assistBonus,.18,.63);
```

`p` 的公式删掉了 `+(s.form-50)/260`。三处 `clamp` 区间与常数（45/90、52/100、45/150）刻意保持原值——平衡漂移只来自 `eff()` 一个变量。

- [ ] **Step 7: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: PASS

- [ ] **Step 8: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "四层判定改吃 eff()，删干净 form/fitness 双重计数

momentSuccessRate 删 form/1000+fitness/2000；射门循环删 (form-50)/260；
weightedStatScore 整体删除，由 atk()/def() 取代；rawStart 删 form 项。
后三层刻意沿用原有常数（45/90、52/100、45/150），平衡漂移只来自 eff()。
逼抢职责从固定 +2.5 改为 (def-55)*.10——DEF 低于 55 时是净负收益。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 逼抢条件性回归测试

Task 5 已经改完逻辑，这一步补上专门的行为断言——逼抢从「无脑加成」变成「有条件选择」是本次玩法上最实质的改动，值得单独锁住。

**Files:**
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写测试**

在 Task 5 的测试块之后插入：

```js
// ===== 逼抢是有条件的选择 =====
// 同一名球员、同一个随机种子，只改 DEF 与职责，比较进球期望。
function xgUnder(attrsPatch,plan){
  const p=G.createInitialState("逼抢探针",allocation,[],"standard","mid");
  ATTR_KEYS.forEach(k=>p.attrs[k]=70);Object.assign(p.attrs,attrsPatch);
  p.totalMonth=48;p.flags.pro18=true;p.coachFavor=95;p.form=55;p.fitness=72;
  let mine=0,theirs=0;
  for(let i=0;i<60;i++){
    const r=G.prepareMatch(p,seeded(i+100),{plan,opponent:{name:"测试队",strength:70},home:true});
    mine+=r.gf;theirs+=r.ga;
  }
  return mine-theirs;
}
const strongPress=xgUnder({DEF:90},"press"), strongBox=xgUnder({DEF:90},"box");
const weakPress=xgUnder({DEF:30},"press"),   weakBox=xgUnder({DEF:30},"box");
assert.ok(strongPress>strongBox,"a high-DEF player gains from pressing");
assert.ok(weakPress<weakBox,"a low-DEF player is punished for pressing");
```

注意：`seeded` 已在测试文件下方定义（`function seeded(seed){...}`），本块必须放在它**之后**。若位置靠前，把 `seeded` 的定义整体上移到文件顶部的 `const G=...` 之后。

- [ ] **Step 2: 运行测试**

```bash
node tests/structure.test.mjs
```

Expected: PASS（Task 5 已实现逻辑）。若 FAIL，说明 `(myDef-55)*.10` 的斜率不足以在 60 场里拉开差距——把系数提到 `.14` 重跑，并在 spec §2.5 同步该数字。

- [ ] **Step 3: 提交**

```bash
git add tests/structure.test.mjs
git commit -m "回归测试：DEF 高选逼抢得利、DEF 低选逼抢受罚

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: 五条训练映射 + 新增「前场逆抢」

DEF 目前没有任何成长入口。补上第五条训练，计入既有的支点中锋流派——不新增第五条流派。

**Files:**
- Modify: `app.js:128-131`（四条训练的 `run`/`effects`）
- Modify: `app.js:131` 之后（插入 `train_press`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 `assert.equal(G.STYLES.length,4,...)` 那一段之后插入：

```js
// ===== 每条训练都必须真的推动它宣称的属性 =====
const TRAIN_EXPECT={
  train_box:{main:"SHO",also:["PAS","WIL"]},
  train_burst:{main:"PAC",also:["DRI"]},
  train_target:{main:"PHY",also:["WIL"]},
  train_play:{main:"PAS",also:["DRI"]},
  train_press:{main:"DEF",also:["PHY"]}
};
for(const [id,exp] of Object.entries(TRAIN_EXPECT)){
  const a=G.ACTIONS.find(x=>x.id===id);
  assert.ok(a,`${id} exists`);
  const t=G.createInitialState("训练探针",allocation,[],"standard","mid");
  ATTR_KEYS.forEach(k=>t.attrs[k]=50);
  const before={...t.attrs};
  a.run(t);
  assert.ok(t.attrs[exp.main]>before[exp.main]+0.1,`${id} clearly raises ${exp.main}`);
  exp.also.forEach(k=>assert.ok(t.attrs[k]>=before[k],`${id} does not hurt ${k}`));
  assert.ok(t.fitness<90,`${id} costs stamina`);
}
// DEF 有且只有一个专属训练入口
const defTrainers=G.ACTIONS.filter(a=>{
  const t=G.createInitialState("d",allocation,[],"standard","mid");
  ATTR_KEYS.forEach(k=>t.attrs[k]=50);const b=t.attrs.DEF;
  try{a.run(t)}catch(e){return false}
  return t.attrs.DEF>b+0.1;
});
assert.equal(defTrainers.length,1,"exactly one action is the DEF growth path");
assert.equal(defTrainers[0].id,"train_press");
// 前场逆抢计入支点中锋，流派总数不变
assert.equal(G.ACTIONS.find(x=>x.id==="train_press").style,"target");
assert.equal(G.STYLES.length,4,"no fifth style route was added");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `AssertionError: train_press exists`

- [ ] **Step 3: 改写四条现有训练**

`app.js:128-131`，把每条的 `run` 与 `effects` 按下表改。逐条用 `Edit` 精确替换：

`train_box`（抢点终结）：
```js
effects:["射门↑↑","传球↑","意志↑","体能↓"],max:2,run:s=>{gain(s,"SHO",.75,"finish");gain(s,"PAS",.3,"setpiece");gain(s,"WIL",.22,"will");change(s,"fitness",-10)}
```

`train_burst`（速度突破）：
```js
effects:["速度↑↑","盘带↑","体能↓"],max:2,run:s=>{gain(s,"PAC",.8,"speed");gain(s,"DRI",.4,"dribble");change(s,"fitness",-13);fatigueInjuryCheck(s,.022)}
```

`train_target`（对抗支点）：
```js
effects:["身体↑↑","意志↑","体能↓"],max:2,run:s=>{gain(s,"PHY",.8,"physical");gain(s,"WIL",.3,"will");change(s,"fitness",-13);fatigueInjuryCheck(s,.022)}
```

`train_play`（回撤组织）：
```js
effects:["传球↑↑","盘带↑","教练信任↑","状态↑"],max:2,run:s=>{gain(s,"PAS",.8,"vision");gain(s,"DRI",.35,"tactics");change(s,"coachFavor",hasTalent(s,"training_rat")?5:3);change(s,"form",3);change(s,"fitness",-6);if(hasTalent(s,"training_rat")&&chance(.18)){gain(s,"PAS",.5);log(s,"good","训练模范发动：你留下加练的细节被教练看见。")}}
```

- [ ] **Step 4: 插入「前场逆抢」**

在 `train_play` 那条对象的 `},` 之后、`{id:"love_time",` 之前插入：

```js
  {id:"train_press",style:"target",phases:["academy","firstteam","overseas","campus","pro"],icon:"▰",name:"前场逆抢",desc:"丢球后的三秒最值钱。反复练回追角度、封传球线、把对方后卫逼到边线。",effects:["防守↑↑","身体↑","教练信任↑","体能↓↓"],max:2,run:s=>{gain(s,"DEF",.8,"defend");gain(s,"PHY",.3,"physical");change(s,"coachFavor",2);change(s,"fitness",-14);fatigueInjuryCheck(s,.024)}},
```

- [ ] **Step 5: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "五条训练直连属性，新增「前场逆抢」补上 DEF 成长入口

前场逆抢计入既有的支点中锋流派，流派总数保持 4 条不变。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: 其余行动与小满互动

**Files:**
- Modify: `app.js:132-140`（`love_time`/`gift`/`together`/`home`/`english`/`street`/`campus_match`/`coach_talk`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 Task 7 的测试块之后插入：

```js
// ===== 小满互动：陪伴回血、独处消耗 =====
function runAction(id,patch){
  const t=G.createInitialState("小满探针",allocation,[],"standard","mid");
  ATTR_KEYS.forEach(k=>t.attrs[k]=50);
  t.form=50;t.fitness=50;t.flags.intimateUnlocked=true;Object.assign(t,patch||{});
  const before={form:t.form,fitness:t.fitness,WIL:t.attrs.WIL,love:t.relationship.love};
  G.ACTIONS.find(x=>x.id===id).run(t);
  return {form:t.form-before.form,fitness:t.fitness-before.fitness,
          WIL:t.attrs.WIL-before.WIL,love:t.relationship.love-before.love};
}
const love=runAction("love_time");
assert.ok(love.form>0,"陪小满 lifts form");
assert.ok(love.fitness>0,"陪小满 restores stamina — she makes you rest");
assert.ok(love.WIL>0,"陪小满 grows will");
assert.ok(love.love>0,"陪小满 deepens the relationship");

const alone=runAction("together");
assert.ok(alone.form>love.form,"独处 is the bigger form swing");
assert.ok(alone.fitness<0,"独处 costs stamina");
assert.ok(alone.WIL>0,"独处 grows will");

const gift=runAction("gift");
assert.ok(gift.form>0&&gift.love>love.love,"礼物 buys more affection than time does");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `AssertionError: 陪小满 restores stamina — she makes you rest`

- [ ] **Step 3: 改写小满三条**

`love_time`（陪小满）—— `effects` 与 `run` 整体替换：
```js
effects:["感情↑","状态↑","意志↑","体能↑"],max:1,show:s=>["恋人","暧昧","异地"].includes(s.relationship.status),run:s=>{const away=s.relationship.status==="异地";changeLove(s,hasTalent(s,"childhood_bond")?10:7);change(s,"form",4);change(s,"fitness",4);gain(s,"WIL",.3,"love");if(!away&&chance(.25)){change(s,"coachFavor",-3);log(s,"warn","你迟到了七分钟。周骁没有骂你，只在名单上画了一个圈。")}else if(away)change(s,"coachFavor",-1)}
```

`gift`（给小满买礼物）：
```js
effects:["感情↑↑","状态↑","花钱3万"],max:1,cost:3,show:s=>["恋人","异地","暧昧"].includes(s.relationship.status),run:s=>{addMoney(s,-3);changeLove(s,hasTalent(s,"childhood_bond")?12:9);change(s,"form",4)}
```

`together`（和小满独处）：
```js
effects:["状态↑↑","意志↑","体力↓"],max:1,show:s=>s.flags&&s.flags.intimateUnlocked&&["恋人","异地"].includes(s.relationship.status),run:s=>{change(s,"form",12);gain(s,"WIL",.3,"love");change(s,"fitness",-12);changeLove(s,3)}
```

`together` 的 `+12` 是 spec §1.6 合并规则的直接结果（原本同时给 `form+8` 与 `morale+6`），不是单独调的数。

- [ ] **Step 4: 改写其余五条**

| 行动 | `run` 中需替换的片段 |
|---|---|
| `home`（顾家） | `gainStat(s,"will",.28,"will")` → `gain(s,"WIL",.28,"will")`；`change(s,"morale",6)` → `change(s,"form",4)` |
| `english`（学英语） | `gainStat(s,"will",.28,"language")` → `gain(s,"WIL",.28,"language")` |
| `street`（踢野球） | `gainSkill(s,"dribble",1,"dribble")` → `gain(s,"DRI",1,"dribble")`；`gainSkill(s,"vision",.35,"vision")` → `gain(s,"PAS",.35,"vision")` |
| `campus_match`（校队赛） | `gainSkill(s,"finishing",.55,"finish")` → `gain(s,"SHO",.55,"finish")`；`gainSkill(s,"dribble",.45,"dribble")` → `gain(s,"DRI",.45,"dribble")` |
| `coach_talk`（找教练谈） | `chance(.48+s.stats.will/220+...)` → `chance(.48+s.attrs.WIL/220+...)`；`gainStat(s,"will",.25,"pressure")` → `gain(s,"WIL",.25,"pressure")` |

同时把这五条的 `effects` 里的「心情↑」改成「状态↑」（`home` 一条）。

- [ ] **Step 5: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "行动全面直连七项属性；小满互动分化为陪伴回血与独处消耗

陪小满：意志↑ 状态+4 体能+4；独处：状态+12 意志↑ 体能-12。
两个行动手感分开，玩家按体能条决定选哪个。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 9: 流派抬高对应属性的成长软上限

Task 1 埋的 `styleCapLevel(){return 0}` 桩函数，在这里接上真实逻辑。

**Files:**
- Modify: `app.js`（`styleCapLevel` 桩函数）
- Modify: `app.js:162-168`（`STYLES` 加 `attrs` 字段与卡面文案）
- Modify: `app.js:1458`（`API` 导出）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 Task 8 的测试块之后插入：

```js
// ===== 流派抬高对应属性的成长天花板 =====
// 没有流派兜着的属性练到 88 左右就爬不动；Ⅲ 级禁区杀手才能把 SHO 推过 95。
function grindTo(styleExp,key,startAt){
  const t=G.createInitialState("磨练",allocation,[],"standard","mid");
  ATTR_KEYS.forEach(k=>t.attrs[k]=50);
  t.attrs[key]=startAt;t.styles={box:0,burst:0,target:0,play:0,...styleExp};
  for(let i=0;i<400;i++)G.gain(t,key,1,"grind");
  return t.attrs[key];
}
const noStyle=grindTo({},"SHO",85);
const maxStyle=grindTo({box:120},"SHO",85);
assert.ok(maxStyle>noStyle+2,`style III should push SHO meaningfully higher (${noStyle.toFixed(1)} → ${maxStyle.toFixed(1)})`);
assert.ok(maxStyle>93,`style III should break past 93, got ${maxStyle.toFixed(1)}`);
assert.ok(noStyle<93,`no style should stall below 93, got ${noStyle.toFixed(1)}`);

// 每条流派声明自己抬高哪些属性，且全部是合法 key
assert.deepEqual(G.STYLES.map(s=>s.key),["box","burst","target","play"]);
G.STYLES.forEach(st=>{
  assert.ok(Array.isArray(st.attrs)&&st.attrs.length>0,`${st.key} declares its attributes`);
  st.attrs.forEach(k=>assert.ok(ATTR_KEYS.includes(k),`${st.key} attr ${k} is a real key`));
});
// 七项里只有 WIL 不归任何流派
const covered=new Set(G.STYLES.flatMap(s=>s.attrs));
assert.deepEqual(ATTR_KEYS.filter(k=>!covered.has(k)),["WIL"],"only WIL has no style route");
// 流派等级正确映射到 cap level
const t9=G.createInitialState("流派",allocation,[],"standard","mid");
t9.styles={box:120,burst:0,target:60,play:20};
assert.equal(G.styleCapLevel(t9,"SHO"),3,"box III caps SHO at level 3");
assert.equal(G.styleCapLevel(t9,"PHY"),2,"target II caps PHY at level 2");
assert.equal(G.styleCapLevel(t9,"DEF"),2,"target II also caps DEF");
assert.equal(G.styleCapLevel(t9,"PAS"),1,"play I caps PAS at level 1");
assert.equal(G.styleCapLevel(t9,"PAC"),0,"untrained burst leaves PAC uncapped");
assert.equal(G.styleCapLevel(t9,"WIL"),0,"WIL never gets a style cap");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `AssertionError: style III should push SHO meaningfully higher (88.0 → 88.0)`（桩函数恒返回 0）

- [ ] **Step 3: 给 `STYLES` 加 `attrs` 字段**

`app.js:162-168`，四条流派各加一个 `attrs` 字段并改 `desc`：

```js
  {key:"box",name:"禁区杀手",icon:"◎",desc:"抬高射门的成长上限",attrs:["SHO"],
   levels:["射门类关键时刻成功率 +5%","解锁比赛选项「抢前点」","落后时的绝杀机会成功率再 +8%，进球评分更高"]},
  {key:"burst",name:"爆点前锋",icon:"»",desc:"抬高速度与盘带的成长上限",attrs:["PAC","DRI"],
```
（`burst` 与 `play` 两条的 `levels` 数组原样保留，只加 `attrs` 与改 `desc`）
```js
  {key:"target",name:"支点中锋",icon:"▲",desc:"抬高身体与防守的成长上限",attrs:["PHY","DEF"],
  {key:"play",name:"组织型前锋",icon:"▣",desc:"抬高传球的成长上限",attrs:["PAS"],
```

- [ ] **Step 4: 实现 `styleCapLevel`**

把 Task 1 埋的桩函数整行替换：

```js
/* 流派等级抬高对应属性的成长软上限，每级 +5。WIL 不归任何流派——
   它只从剧情选择、压力事件和小满那里长，练不出来。 */
function styleCapLevel(s,key){
  if(!s||!s.styles)return 0;
  let lv=0;
  for(const st of STYLES)if(st.attrs&&st.attrs.includes(key))lv=Math.max(lv,styleOf(s,st.key));
  return lv;
}
```

**注意声明顺序**：`styleCapLevel` 引用了 `STYLES` 与 `styleOf`。`STYLES` 是 `const`（TDZ），`styleOf` 是函数声明（提升）。由于 `styleCapLevel` 只在 `gain()` 运行时被调用，而那时模块已完全求值，此处无 TDZ 问题。**但不要**把 `styleCapLevel` 的调用提到模块顶层。

- [ ] **Step 5: 导出**

`app.js:1458`，在 `styleLevel,` 之后插入 `styleCapLevel,styleOf,`。

- [ ] **Step 6: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "流派等级抬高对应属性的成长软上限，每级 +5

禁区杀手→SHO、爆点前锋→PAC+DRI、支点中锋→PHY+DEF、组织型→PAS。
WIL 不归任何流派，与它在 COND_SENS 里最抗跌（.4）互为呼应：
意志是唯一不被身体状况打折、也练不出来的东西。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 10: 删除「心情」，并入「状态」

25 处调用点。**必须连同 `option()` 的说明文案一起改**——玩家看到「心情+8」而实际动的是状态，是最容易漏的部分。

**Files:**
- Modify: `app.js`（25 处 `change(*,"morale",n)` + `morale:76` 初值 + 月度漂移 + 渲染）
- Modify: `index.html:135-136`（心情行）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在测试文件末尾 `for(const file of [...])` 之前插入：

```js
// ===== 心情已并入状态 =====
assert.equal("morale" in state,false,"morale is gone from fresh states");
assert.ok(!code.includes('"morale"'),"no morale key survives in source");
assert.ok(!/\bmorale\b/.test(code),"the morale identifier is fully retired");
// 玩家看到的效果描述不能提到一个已经不存在的变量
const moraleWords=G.ACTIONS.flatMap(a=>a.effects||[]).filter(e=>e.includes("心情"));
assert.equal(moraleWords.length,0,`actions still advertise 心情: ${moraleWords.join(",")}`);
// 小满关系抬高状态基线：关系满的球员，月度漂移把状态拉得更高
function driftTo(loveVal){
  const t=G.createInitialState("漂移",allocation,[],"standard","mid");
  t.relationship.love=loveVal;t.form=20;
  for(let i=0;i<40;i++)t.form=G.monthlyFormDrift(t);
  return t.form;
}
assert.ok(driftTo(90)>driftTo(30)+3,"a strong relationship raises the long-run form baseline");
```

并在 `index.html` 断言块里加一行：

```js
assert.ok(!html.includes("moraleText"),"the morale row is removed from the sidebar");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `AssertionError: morale is gone from fresh states`

- [ ] **Step 3: 抽出月度状态漂移**

先把散在 `advanceMonth`（`app.js:1323`）里的漂移逻辑抽成一个可测函数。在 `loveSupport` 定义之后插入：

```js
/* 月度状态回归基线；基线由小满关系抬高（分手 52，关系满 59）。
   关系好不再是「+7 战力」，而是「长期状态更稳」——分手的代价看得见。 */
function monthlyFormDrift(s){return clamp(s.form+Math.round((52+loveSupport(s)-s.form)*.22))}
```

然后把 `app.js:1323` 里的这段：

```js
change(S,"morale",1);change(S,"form",Math.round((52-S.form)*0.22));
```

替换为：

```js
S.form=monthlyFormDrift(S);
```

- [ ] **Step 4: 转换 25 处 morale 调用**

拿到完整清单：

```bash
grep -n 'morale' app.js
```

按 **`change(*,"morale",n)` → `change(*,"form",Math.round(n*0.6))`** 逐个替换，直接把算好的整数写进去（不要留 `Math.round` 运行时计算）：

| 原幅度 | 新幅度 | 出现次数 |
|---|---|---|
| `+5` | `+3` | 2 |
| `+6` | `+4` | 7 |
| `+7` | `+4` | 1 |
| `+8` | `+5` | 1 |
| `+10` | `+6` | 3 |
| `+12` | `+7` | 3 |
| `+18` | `+11` | 1 |
| `-4` | `-2` | 2 |

系数 0.6 的理由：原心情幅度最大到 +18，而 `form` 每月有 22% 回归基线，1:1 搬运会让状态过山车。

**同一个 `run` 里若同时改了 `form` 和 `morale`，两者相加**（例如 `together` 已在 Task 8 处理成 `form+12`）。

删掉 `createInitialState` 里的 `morale:76,`。

- [ ] **Step 5: 改文案**

搜出所有面向玩家、提到「心情」的字符串：

```bash
grep -n '心情' app.js index.html
```

全部改成「状态」。包括 `ACTIONS` 的 `effects` 数组、`option()` 的第二个参数（效果描述）、事件正文里的提示。

- [ ] **Step 6: 删渲染与 DOM**

`app.js:1401`（`renderAll`）里删掉这段：

```js
const mEl=$("moraleText"),mBar=$("moraleBar");if(mEl)mEl.textContent=Math.round(S.morale);if(mBar)mBar.style.width=`${S.morale}%`;
```

`index.html:135-136` 删掉整两行：

```html
<div class="pulse-row"><span title="影响比赛发挥，太低会掉状态">心情</span><b id="moraleText">76</b></div>
<div class="mini-track violet"><i id="moraleBar"></i></div>
```

- [ ] **Step 7: 导出并验证**

`app.js:1458` 加 `monthlyFormDrift,`，然后：

```bash
node --check app.js && node tests/structure.test.mjs && grep -c morale app.js
```

Expected: PASS，且 `grep -c morale app.js` 输出 `0`

- [ ] **Step 8: 提交**

```bash
git add app.js index.html tests/structure.test.mjs
git commit -m "删除「心情」，并入「状态」

25 处 change(morale,n) 按 ×0.6 折算成 form（原幅度最大 +18，而 form
每月有 22% 回归，1:1 搬运会让状态过山车）。月度漂移抽成
monthlyFormDrift()，基线由小满关系抬高：分手 52、关系满 59——
关系好不再是隐形战力加成，而是长期状态更稳。
面向玩家的「心情」文案同步改为「状态」。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 11: 删除 `teamFit`，9 处事件效果拆到已有变量

`teamFit` 影响首发概率 ±16.7pp，却从未初始化、有三个不一致的默认值（`||45` / `||50` / `||55`，触发顺序决定基准）、且零 UI。

**Files:**
- Modify: `app.js`（9 处事件选项 + `rawStart` 已在 Task 5 处理）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 Task 10 的测试块之后插入：

```js
// ===== teamFit 已彻底移除 =====
assert.ok(!/\bteamFit\b/.test(code),"the teamFit identifier is fully retired");
assert.equal("teamFit" in state,false,"fresh states carry no teamFit");
// 玩家看到的效果描述不能再提到一个不存在的机制
assert.ok(!code.includes("队内适应"),"no option still advertises 队内适应");
assert.ok(!code.includes("更衣室-")&&!code.includes("更衣室+"),"no option still advertises a 更衣室 meter");
// 首发概率只剩「你有多强」与「教练信不信你」两个输入
const fitProbe=G.createInitialState("首发",allocation,[],"standard","mid");
ATTR_KEYS.forEach(k=>fitProbe.attrs[k]=70);
fitProbe.totalMonth=48;fitProbe.flags.pro18=true;
function startRate(coachFavor,form){
  fitProbe.coachFavor=coachFavor;fitProbe.form=form;
  let n=0;for(let i=0;i<200;i++)if(G.prepareMatch(fitProbe,seeded(i+7)).role==="首发")n++;
  return n/200;
}
assert.ok(startRate(90,55)>startRate(20,55)+.15,"coach trust drives selection");
assert.ok(Math.abs(startRate(50,90)-startRate(50,20))<.06,"form no longer decides whether you start");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `AssertionError: the teamFit identifier is fully retired`

- [ ] **Step 3: 改写 9 处事件选项**

拿到清单：

```bash
grep -n 'teamFit' app.js
```

逐处按下表替换。**每处都要同时改 `option()` 的第二个参数（玩家看到的效果描述）**：

| 行 | 事件 · 选项 | 原代码片段 | 改成 |
|---|---|---|---|
| 289 | 捡球 ·「先做一个月，再靠表现说话」 | `s.teamFit=clamp((s.teamFit\|\|45)+8);` | `change(s,"coachFavor",6);` |
| | 描述 | `"队内适应+8，体能-8；意志-1"` | `"教练信任+6，体能-8；意志-1"` |
| 300 | 听错指令 ·「让翻译解释是指令不清」 | `change(s,"coachFavor",-8);` + `s.teamFit=clamp((s.teamFit\|\|45)-6)` | `change(s,"coachFavor",-10);`（删掉 teamFit 那半句） |
| | 描述 | `"教练信任-8，状态+4；队内适应下降"` | `"教练信任-10，状态+4"` |
| 305 | 夜店 ·「去，但设好离场时间」 | `s.teamFit=clamp((s.teamFit\|\|45)+8);` | `change(s,"form",5);` |
| | 描述 | `"队内适应+8；体能-8，夜生活累积隐患"` | `"状态+5；体能-8，夜生活累积隐患"` |
| 306 | 夜店 ·「拒绝，独自留在基地」 | `s.teamFit=clamp((s.teamFit\|\|45)-7);` | `change(s,"form",-4);` |
| | 描述 | `"体能+10；队内适应-7，意志+1"` | `"体能+10；状态-4，意志+1"` |
| 337 | 酒驾 ·「拒绝背书，只谈球队纪律」 | `s.teamFit=clamp((s.teamFit\|\|55)-8);` | `change(s,"coachFavor",-6);` |
| | 描述 | `"短期更衣室-8；公众信任+10"` | `"教练信任-6；公众信任+10"` |
| 338 | 酒驾 ·「按俱乐部稿件发言」 | `s.teamFit=clamp((s.teamFit\|\|55)+8);` | `change(s,"coachFavor",6);` |
| | 描述 | `"队内适应+8；若后续反转，媒体风险+18"` | `"教练信任+6；若后续反转，媒体风险+18"` |
| 344 | 酒驾 ·「替队友强硬出头」 | `s.teamFit=clamp((s.teamFit\|\|50)+10);` | `gain(s,"WIL",1,"pressure");change(s,"coachFavor",-4);` |
| | 描述 | `"队内适应+10；40%停赛1个月"` | `"意志+1，教练信任-4；40%停赛1个月"` |
| 364 | 玩笑 ·「硬着头皮融进去」 | `s.teamFit=clamp((s.teamFit\|\|45)+9);` | `change(s,"form",5);` |
| | 描述 | `"队内适应+9，语言+6；体能-6"` | `"状态+5，语言+6；体能-6"` |
| 365 | 玩笑 ·「专注训练，少社交」 | `s.teamFit=clamp((s.teamFit\|\|45)-6)` | `change(s,"form",-4)` |
| | 描述 | `"射术+1，体能+6；队内适应-6"` | `"射门+1，体能+6；状态-4"` |

第 344 条刻意拆成一正一负：原本 `teamFit+10` 把「队友敬你」和「教练高兴」混成一个数；拆开后这个选择才有真正的取舍——40% 停赛加教练不满，换威望与意志。

- [ ] **Step 4: 确认 `rawStart` 已清理**

Task 5 Step 5 已经把 `((s.teamFit||50)-50)/300` 从 `rawStart` 里删掉了。核对一遍：

```bash
grep -c teamFit app.js
```

Expected: `0`

- [ ] **Step 5: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "删除 teamFit，9 处事件效果按语义拆到已有变量

teamFit 影响首发概率 ±16.7pp，却从未初始化、有三个不一致的默认值
（45/50/55，触发顺序决定基准）、且零 UI——玩家的首发概率被一个
看不见且有 bug 的变量左右。

拆解去向：对俱乐部的顺从与表现→教练信任；融入或孤立的心理后果→
状态；顶住压力→意志。「替队友强硬出头」拆成一正一负，让该选择
第一次有真正的取舍。9 处 option() 的效果描述同步改写。

rawStart 现在只剩 overall 与 coachFavor 两个输入。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 12: 首页七边形雷达图

FIFA 卡面式排布：缩写与数字挂在七个顶点外侧，数字统一白色（不按数值分色）。

**Files:**
- Modify: `app.js:1402`（`renderAll` 里的 `statBars` 渲染）
- Modify: `index.html:122-125`（侧栏面板）
- Modify: `style.css`（新增 `.radar-*`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

在 `index.html` 断言块附近插入：

```js
// ===== 七边形雷达图 =====
const rs=G.createInitialState("雷达",allocation,[],"standard","mid");
ATTR_KEYS.forEach((k,i)=>rs.attrs[k]=40+i*8);   // 40,48,56,64,72,80,88
const svg=G.radarSVG(rs);
assert.match(svg,/^<svg /,"radarSVG returns an svg element");
assert.match(svg,/viewBox="0 0 240 215"/,"fixed viewBox keeps the sidebar layout stable");
ATTR_KEYS.forEach(k=>assert.ok(svg.includes(">"+k+"<"),`${k} label is rendered`));
ATTR_KEYS.forEach(k=>assert.ok(svg.includes(">"+Math.round(rs.attrs[k])+"<"),`${k} value is rendered`));
// 七个顶点，四层网格环
assert.equal((svg.match(/<polygon/g)||[]).length,5,"four grid rings plus one data polygon");
assert.equal((svg.match(/<circle/g)||[]).length,7,"one dot per vertex");
// 所有坐标都落在 viewBox 内，标签不会被裁掉
const coords=[...svg.matchAll(/(?:x|cx)="([-\d.]+)"/g)].map(m=>+m[1]);
assert.ok(Math.min(...coords)>=0&&Math.max(...coords)<=240,"x coords stay inside the viewBox");
const ys=[...svg.matchAll(/(?:y|cy)="([-\d.]+)"/g)].map(m=>+m[1]);
assert.ok(Math.min(...ys)>=0&&Math.max(...ys)<=215,"y coords stay inside the viewBox");
assert.match(html,/id="radarPanel"/,"sidebar hosts the radar");
assert.ok(!html.includes('id="heightCm"'),"height is no longer a sidebar headline");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `TypeError: G.radarSVG is not a function`

- [ ] **Step 3: 实现 `radarSVG`**

在 `renderAll` 定义之前插入：

```js
/* 七边形能力雷达。顶点顺序即 ATTRS 顺序，从正上方(-90°)顺时针每 360/7 度一个。
   R=64 时标签落在 x 36~204、y 22~193，均在 240×215 的 viewBox 内。 */
function radarSVG(s){
  const R=64,cx=120,cy=108,N=ATTRS.length;
  const pt=(i,k)=>{const t=(-90+i*360/N)*Math.PI/180;return[cx+R*k*Math.cos(t),cy+R*k*Math.sin(t)]};
  const ring=k=>ATTRS.map((_,i)=>pt(i,k).map(v=>v.toFixed(1)).join(",")).join(" ");
  let out=`<svg viewBox="0 0 240 215" width="100%" role="img" aria-label="能力雷达">`;
  [.25,.5,.75,1].forEach(k=>{out+=`<polygon points="${ring(k)}" fill="none" stroke="#ffffff${k===1?"28":"12"}" stroke-width="1"/>`});
  ATTRS.forEach((_,i)=>{const[x,y]=pt(i,1);out+=`<line x1="${cx}" y1="${cy}" x2="${x.toFixed(1)}" y2="${y.toFixed(1)}" stroke="#ffffff12" stroke-width="1"/>`});
  const data=ATTRS.map((a,i)=>pt(i,clamp(s.attrs[a.key],1,99)/100));
  out+=`<polygon points="${data.map(p=>p.map(v=>v.toFixed(1)).join(",")).join(" ")}" fill="#28d27d33" stroke="#28d27d" stroke-width="2" stroke-linejoin="round"/>`;
  data.forEach(([x,y])=>{out+=`<circle cx="${x.toFixed(1)}" cy="${y.toFixed(1)}" r="2.6" fill="#28d27d"/>`});
  ATTRS.forEach((a,i)=>{
    const[x,y]=pt(i,1.34),mid=Math.abs(x-cx)<4,anchor=mid?"middle":(x>cx?"start":"end"),dx=mid?0:(x>cx?2:-2);
    out+=`<text x="${(x+dx).toFixed(1)}" y="${(y-2).toFixed(1)}" fill="#9bb0a5" font-size="8.5" font-weight="800" text-anchor="${anchor}">${a.key}</text>`;
    out+=`<text x="${(x+dx).toFixed(1)}" y="${(y+8).toFixed(1)}" fill="#f5f2e9" font-size="13" font-weight="900" text-anchor="${anchor}">${Math.round(s.attrs[a.key])}</text>`;
  });
  return out+`</svg>`;
}
```

- [ ] **Step 4: 接进 `renderAll`**

`app.js:1402`，把这段：

```js
$("statBars").innerHTML=CORE_STATS.map(x=>`<div class="stat-row"><span>${x.name}</span><div class="track"><i style="width:${S.stats[x.key]}%"></i></div><b>${Math.round(S.stats[x.key])}</b></div>`).join("");
```

替换为：

```js
$("radarPanel").innerHTML=radarSVG(S);
```

同时把 `renderAll` 里的 `$("heightCm").textContent=`${S.heightCm}cm`;` 改成：

```js
$("radarOvr").textContent=overall(S);
```

- [ ] **Step 5: 改 `index.html`**

`index.html:122-125`，整个 `<section class="side-panel glass">` 块替换为：

```html
        <section class="side-panel glass">
          <div class="section-title"><h3>能力雷达</h3><span id="radarOvr">58</span></div>
          <div id="radarPanel" class="radar-panel"></div>
        </section>
```

- [ ] **Step 6: 加样式**

`style.css` 末尾追加：

```css
.radar-panel{margin-top:6px}
.radar-panel svg{display:block}
@media (max-width:760px){.radar-panel{margin-top:2px}}
```

- [ ] **Step 7: 导出并验证**

`app.js:1458` 加 `radarSVG,`，然后：

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: PASS

- [ ] **Step 8: 目视确认**

```bash
python3 -m http.server 8766
```

打开 `http://127.0.0.1:8766/`，开一局新游戏。确认：七边形完整不被裁切、七个数字都在、移动端（DevTools 390px 宽）雷达那一行不比左边的立绘卡片高太多。若移动端过高，把 `.radar-panel svg` 在 `@media (max-width:760px)` 里加 `max-height:150px`。

- [ ] **Step 9: 提交**

```bash
git add app.js index.html style.css tests/structure.test.mjs
git commit -m "首页七边形能力雷达图（FIFA 卡面式）

缩写与数字挂在七个顶点外侧，数字统一白色。侧栏标题从「五维雷达」
改为「能力雷达」，右上角由身高改显 OVR——身高降级为创建时的
一次性选择，移到生涯页当背景信息。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 13: 「本场职责」改分段控件

三张大卡在移动端会塌成单列竖排，高度翻三倍。改成一条 segmented + 一行当前项说明。

**Files:**
- Modify: `app.js:1413`（`renderActions` 里的 `plan-grid` 片段）
- Modify: `style.css:28-34, 55`（`.plan-*` → `.plan-seg-*`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 本场职责：分段控件 =====
assert.equal(G.MATCH_PLANS.length,3,"still exactly three match roles");
G.MATCH_PLANS.forEach(p=>{
  assert.ok(p.name&&p.desc&&Array.isArray(p.effects),`${p.id} keeps its copy for the description row`);
});
const css=fs.readFileSync(path.join(root,"style.css"),"utf8");
assert.ok(!css.includes(".plan-grid"),"the three-card grid is gone");
assert.ok(css.includes(".plan-seg"),"the segmented control is styled");
assert.ok(!/\.plan-card/.test(css),"no stale plan-card rules remain");
assert.match(code,/data-plan=/,"role buttons still carry data-plan for the click handler");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `AssertionError: the three-card grid is gone`

- [ ] **Step 3: 改渲染**

`app.js:1413`，把这段：

```js
<div class="plan-grid">${MATCH_PLANS.map(p=>`<button class="plan-card ${S.matchPlan===p.id?"active":""}" data-plan="${p.id}"><div class="action-icon">${p.icon}</div><h3>${esc(p.name)}</h3><p>${esc(p.desc)}</p><div class="effect-line">${p.effects.map(e=>`<span>${esc(e)}</span>`).join("")}</div></button>`).join("")}</div>
```

替换为：

```js
<div class="plan-seg">${MATCH_PLANS.map(p=>`<button class="${S.matchPlan===p.id?"on":""}" data-plan="${p.id}">${p.icon} ${esc(p.name)}</button>`).join("")}</div><div class="plan-seg-desc"><p>${esc(planOf(S).desc)}</p><div class="effect-line">${planOf(S).effects.map(e=>`<span>${esc(e)}</span>`).join("")}</div></div>
```

- [ ] **Step 4: 换样式**

`style.css:28-34`，把 `.plan-grid` / `.plan-card` 及其五条派生规则整体替换为：

```css
.plan-seg{display:flex;border:1px solid var(--line);border-radius:12px;overflow:hidden;background:#0b1b15}
.plan-seg button{flex:1;text-align:center;padding:9px 6px;font:inherit;font-size:12px;font-weight:800;color:var(--muted);background:none;border:0;border-right:1px solid var(--line);cursor:pointer;min-height:40px}
.plan-seg button:last-child{border-right:0}
.plan-seg button:hover{color:#dce6df}
.plan-seg button.on{background:#f0bd5e14;color:var(--gold);box-shadow:inset 0 -2px 0 var(--gold)}
.plan-seg-desc{display:flex;align-items:center;gap:8px;margin-top:7px;padding:0 2px;flex-wrap:wrap}
.plan-seg-desc p{margin:0;font-size:10px;line-height:1.5;color:var(--muted);flex:1;min-width:180px}
.plan-seg-desc .effect-line{margin:0;flex:0 0 auto}
```

`style.css:55` 的移动端规则里删掉 `.plan-grid{grid-template-columns:1fr}.plan-card{padding:12px}`，改成 `.plan-seg button{font-size:11px;padding:9px 3px}`。

- [ ] **Step 5: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: PASS

- [ ] **Step 6: 目视确认**

启动本地服务器，切到行动页。点三个职责，确认：选中项金色下划线、下方说明行跟着换、移动端不塌成竖排。

- [ ] **Step 7: 提交**

```bash
git add app.js style.css tests/structure.test.mjs
git commit -m "「本场职责」从三张大卡改为分段控件 + 一行说明

移动端不再塌成单列竖排三张卡。三张卡的描述文案保留在
MATCH_PLANS 数据里，只是不再同时全渲染。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 14: 创建页身高选择器与 24 点分配

**Files:**
- Modify: `app.js:1174`（`creatorAllocation` 初值）、`app.js:1431`（`renderCreator`）、`app.js:1443/1445`（两处重开逻辑）
- Modify: `index.html:50-57`（分配区）
- Modify: `style.css`（`.height-picker`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 创建页：身高独立选择，24 点分配七项 =====
const short=G.createInitialState("矮个",allocation,[],"standard","short");
const tall=G.createInitialState("高个",allocation,[],"standard","tall");
assert.ok(tall.attrs.PHY>short.attrs.PHY,"tall players start stronger physically");
assert.ok(short.attrs.PAC>tall.attrs.PAC,"short players start quicker");
assert.ok(short.attrs.DRI>tall.attrs.DRI,"short players start more agile");
assert.ok(tall.heightCm>short.heightCm,"heightCm follows the tier");
assert.equal(tall.heightTier,"tall");
assert.match(html,/id="heightPicker"/,"creator hosts a height picker");
assert.match(html,/24/,"creator advertises 24 points");
```

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `AssertionError: creator hosts a height picker`（Task 1 已实现 `heightTier` 逻辑，只缺 UI）

- [ ] **Step 3: 改 `index.html`**

`index.html:50-57`，把 `<div class="allocation-head">` 到 `<div id="attributeAllocator" ...>` 那段替换为：

```html
      <div class="talent-head">
        <div>
          <span class="field-label">身高</span>
          <p>出生就定下的东西。高个吃对抗，矮个吃灵活，选完不能改。</p>
        </div>
      </div>
      <div id="heightPicker" class="height-picker"></div>

      <div class="allocation-head">
        <div>
          <span class="field-label">初始属性</span>
          <p>把24点分给七项能力。你不可能样样精通。</p>
        </div>
        <div class="points-orb"><b id="pointsLeft">24</b><span>剩余</span></div>
      </div>
      <div id="attributeAllocator" class="attribute-allocator"></div>
```

- [ ] **Step 4: 改 `renderCreator`**

`app.js:1174`，把 `creatorAllocation={height:4,speed:4,burst:4,stamina:4,will:4}` 改成：

```js
creatorAllocation={PAC:4,SHO:4,PAS:3,DRI:3,DEF:3,PHY:3,WIL:4},creatorHeight="mid"
```

`app.js:1431` 的 `renderCreator`，把开头的 `const left=20-...` 与 `$("attributeAllocator").innerHTML=CORE_STATS.map(...)` 改成：

```js
function renderCreator(){const left=24-Object.values(creatorAllocation).reduce((a,b)=>a+b,0);$("pointsLeft").textContent=left;
  const HEIGHT_TIERS=[{key:"short",name:"矮个",cm:"174cm",note:"PAC/DRI 起步高，PHY 偏低"},{key:"mid",name:"中等",cm:"182cm",note:"没有偏科"},{key:"tall",name:"高个",cm:"191cm",note:"PHY 起步高，PAC/DRI 偏低"}];
  $("heightPicker").innerHTML=HEIGHT_TIERS.map(h=>`<button class="height-card ${creatorHeight===h.key?"active":""}" data-height="${h.key}"><b>${h.name}</b><span>${h.cm}</span><p>${h.note}</p></button>`).join("");
  $("heightPicker").querySelectorAll("[data-height]").forEach(b=>b.addEventListener("click",()=>{creatorHeight=b.dataset.height;renderCreator()}));
  $("attributeAllocator").innerHTML=ATTRS.map(x=>`<div class="allocate-row"><div class="allocate-name"><b>${x.icon} ${x.key} ${x.name}</b><span>${x.sub}</span></div><div class="allocate-track"><i style="width:${creatorAllocation[x.key]*5}%"></i></div><button class="step-btn" data-stat="${x.key}" data-delta="-1" ${creatorAllocation[x.key]<=0?"disabled":""}>−</button><button class="step-btn" data-stat="${x.key}" data-delta="1" ${left<=0?"disabled":""}>＋</button><div class="allocate-value">${creatorAllocation[x.key]}</div></div>`).join("");
```

（该函数其余部分——天赋抽取、难度选择器——原样保留）

- [ ] **Step 5: 传 `heightTier` 到 `createInitialState`**

找到 `startGame` 的点击处理里调用 `createInitialState` 的地方，补上第五个参数 `creatorHeight`。同时 `app.js:1443` 与 `1445` 两处重开逻辑里的 `creatorAllocation={height:4,...}` 都改成新的七项默认值，并加 `creatorHeight="mid"`。

```bash
grep -n 'creatorAllocation={' app.js
```

Expected: 3 处，全部改完。

- [ ] **Step 6: 加样式**

`style.css` 末尾追加：

```css
.height-picker{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:10px}
.height-card{text-align:left;border:1px solid var(--line);background:#06130e80;border-radius:13px;padding:11px;color:inherit;font:inherit;cursor:pointer}
.height-card:hover{border-color:#31d98555}
.height-card.active{border-color:var(--gold);background:#f0bd5e0e}
.height-card b{display:block;font-size:13px}
.height-card span{display:block;font-size:11px;color:var(--gold);margin:2px 0 4px}
.height-card p{margin:0;font-size:10px;line-height:1.45;color:var(--muted)}
```

- [ ] **Step 7: 运行测试确认通过**

```bash
node --check app.js && node tests/structure.test.mjs
```

Expected: PASS

- [ ] **Step 8: 目视确认**

启动服务器，走一遍创建流程：三个身高档位能切换、24 点能分完、「踏上绿茵场」按钮在点数用尽前保持禁用。

- [ ] **Step 9: 提交**

```bash
git add app.js index.html style.css tests/structure.test.mjs
git commit -m "创建页：身高档位独立选择 + 七项 24 点分配

身高不占分配点，高个 PHY+5/PAC-3/DRI-2，矮个相反。
分配点从 5 项 20 点提到 7 项 24 点（平均 3.4，接近原先的 4.0 手感）。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 15: 生涯页与侧栏收尾，同步 README

**Files:**
- Modify: `app.js:1417`（`renderCareer`）
- Modify: `README.md`
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 生涯页展示七维与流派属性标注 =====
assert.match(code,/七项主属性/,"career page explains the flat attribute model");
assert.ok(!code.includes("技术属性（训练成长）"),"the old two-tier wording is gone");
assert.match(code,/heightCm/,"height still shows somewhere as background info");
assert.match(code,/体能见底时/,"the career page explains the condition coefficient");
// 流派卡标注属性缩写
G.STYLES.forEach(st=>assert.ok(code.includes(st.attrs.join("·"))||code.includes(st.attrs[0]),
  `${st.key} shows its attribute abbreviations`));
```

并把测试文件里那条旧断言：

```js
assert.match(code,/关键球”不设单独数值/,"derived key-moment mechanic should be explained");
```

保留不动——生涯页文案改写时必须留住这句。

- [ ] **Step 2: 运行测试确认失败**

```bash
node tests/structure.test.mjs
```

Expected: FAIL — `AssertionError: career page explains the flat attribute model`

- [ ] **Step 3: 改 `renderCareer` 的属性卡**

`app.js:1417`，把 `<article class="info-card"><h3>技术属性（训练成长）</h3>...` 整个 article 替换为：

```js
<article class="info-card"><h3>七项主属性</h3><div class="effect-line">${ATTRS.map(a=>`<span>${a.key} ${esc(a.name)} ${Math.round(S.attrs[a.key])}</span>`).join("")}<span>语言 ${Math.round(S.language)}</span><span>声望 ${Math.round(S.fame)}</span><span>身高 ${S.heightCm}cm</span></div><p>七项都是真实数值，行动、比赛结果与进球判定全部由它们决定；状态与体能作为动态系数同时介入——体能见底时身体和速度掉得最狠，意志几乎不受影响。“关键球”不设单独数值，由意志、状态、赛事阶段和相关天赋共同影响。当前生涯积分 <b>${careerScore(S)}</b>。</p></article>
```

- [ ] **Step 4: 流派卡标注属性**

同一行里，把流派卡的 `<b>${st.icon} ${esc(st.name)}</b>` 改成：

```js
<b>${st.icon} ${esc(st.name)} <small style="color:var(--gold);font-size:9px">${st.attrs.join("·")}</small></b>
```

并把流派卡的引导文案 `<p>天赋是出生带来的，流派是练出来的...` 改成：

```js
<p>天赋是出生带来的，流派是练出来的。流派等级抬高对应属性的成长天花板——没有流派兜着的属性练到 88 左右就基本爬不动了。意志不归任何流派，它只从剧情和压力里长。</p>
```

- [ ] **Step 5: 同步 README**

`README.md` 的「已实现」列表，把这两条：

```
- 20点五维初始属性、20个足球天赋随机抽3个、玩家命名
```

改成：

```
- 七项主属性 PAC/SHO/PAS/DRI/DEF/PHY/WIL，首页七边形雷达图展示
- 创建时独立选择身高档位（影响 PHY/PAC/DRI 初值），24点分配七项属性
- 20个足球天赋随机抽3个、玩家命名
```

并把 `- 能力、状态、体能、对手和随机共同决定的比赛简报与伤病` 改成：

```
- 所有行动与比赛判定统一经由「有效属性」= 属性 × 状态体能系数；体能见底时身体和速度掉得最狠，意志几乎不受影响
- 四条训练流派抬高对应属性的成长天花板；「前场逆抢」训练支撑防守成长，逼抢职责在 DEF 不足时是净负收益
```

- [ ] **Step 6: 运行完整验证**

```bash
node --check app.js && node tests/structure.test.mjs
grep -c 'morale\|teamFit\|CORE_STATS\|gainStat\|gainSkill\|weightedStatScore' app.js
```

Expected: 测试 PASS，`grep -c` 输出 `0`

- [ ] **Step 7: 全流程目视确认**

启动服务器，完整走一遍：新建球员（选身高、分 24 点）→ 首页看雷达图 → 行动页切职责 → 做训练看属性涨 → 结束本月打比赛 → 生涯页看七维与流派。确认没有任何页面显示 `undefined` 或 `NaN`。

- [ ] **Step 8: 提交**

```bash
git add app.js README.md tests/structure.test.mjs
git commit -m "生涯页展示七维与流派属性标注，同步 README

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 完工检查表

全部 15 个任务完成后逐条核对：

- [ ] `node --check app.js && node tests/structure.test.mjs` 通过
- [ ] `grep -c 'morale\|teamFit\|CORE_STATS\|gainStat\|gainSkill\|weightedStatScore\|s\.stats\.\|s\.skills\.' app.js` 输出 `0`
- [ ] `grep -o 'stat:"[a-zA-Z]*"' app.js | sort -u` 只有七个合法 key
- [ ] 旧存档（v2）能读进来，OVR 掉不超过 2 点
- [ ] 首页雷达图七个角完整、桌面与 390px 移动端都不裁切
- [ ] 行动页职责是一条分段控件，移动端不塌成竖排
- [ ] 侧栏没有「心情」行
- [ ] 没有任何 `option()` 的效果描述提到「心情」「队内适应」「更衣室」
- [ ] 完整玩一个月：创建 → 训练 → 比赛 → 生涯页，无 `undefined` / `NaN`
