# 联赛积分榜 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给每个联赛（梯队/校园/中超/英超）加一张真实的积分榜，并把「联赛冠军」从掷骰子改成榜首判定。

**Architecture:** 新增 `s.league`，与 `s.schedule` 平行：赛季初建表，每一轮把玩家的真实比分直接写进榜、其余球队按「赛季+轮次」派生的种子两两配对模拟。轮次数直接取赛程里 `type==="club"` 的场次数，两边永远对齐。

**Tech Stack:** 纯 ES5+ 浏览器 JS，无框架无构建。`app.js`（单文件，约2000行）+ `index.html` + `style.css`。

**Spec:** [docs/superpowers/specs/2026-08-07-联赛积分榜-design.md](../specs/2026-08-07-联赛积分榜-design.md)

---

## 读这份计划之前必须知道的

**跑测试：**
```bash
cd /Users/ruis/Documents/模拟/模拟球员
node --check app.js && node tests/structure.test.mjs
```
`tests/structure.test.mjs` 是**顶层裸断言**，从上往下顺序执行，任一 `assert` 失败就整个脚本抛错。通过时打印 `模拟球员 architecture test passed`。新测试**追加到文件末尾**，前面的 `G`/`allocation`/`ATTR_KEYS`/`code`/`html`/`css`/`driveMatch` 都在作用域里。

### 五个已知陷阱（上一轮全踩过，别再踩一遍）

**1. 禁止对 `app.js` 做正则批量替换。**
文件里有 **243 个中文弯引号 `“”`** 嵌在 `"..."` 字符串里。任何碰引号字符的批量操作都会把文件搞崩，而且崩得很隐蔽。**只用 Edit 工具做精确字符串替换，每改一处跑一次 `node --check app.js`。** 改完核对 `grep -o "“" app.js | wc -l` 仍为 243。

**2. vm realm：跨沙箱 `deepEqual` 必然失败。**
`app.js` 跑在 `node:vm` 沙箱里，沙箱内数组的原型与外部不同。比较数组前要 `[...]` 展开。（`Object.keys()` 在外部调用，返回外部数组，不需要展开。）

**3. 测试有随机性，一次绿不算绿。**
`prepareMatch` 约 22% 概率球员未出场。**每个任务的验证都要跑 30 次**：
```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
出现任何一个 `F` 都不算完成。

**4. `advanceMonth` 先 `S.totalMonth++` 再判定比赛。** 设 `totalMonth=48` 跑一次，踢的是第 **49** 月那场。

**5. 不许为了让测试变绿去改生产行为、改用户可见文案、或写混淆代码。** 断言拦到不该拦的，报告出来让计划作者改断言。上一轮这条犯过三次（把同步 `resolveMatch` 改成 `setTimeout`、把 `delete d.risks.media` 写成 `["med"+"ia"]`、把平衡档位调去迁就一个测试常数）。

---

## 文件结构

不新建源文件（`build_single.py` 只内联单个 `app.js`，README 承诺双击 `index.html` 即可玩，`file://` 下 ES module 会被 CORS 拦掉）。

| 文件 | 本次改动 |
|---|---|
| `app.js` | 新增联赛模块（放在赛程模块 `nextFixture` 之后）；接进 `resolveMatch`、`advanceMonth` 的 missed 分支、`seasonAwardCheck`、`evaluateSeasonGoal`、`renderMatches`、`normalizeSave` |
| `style.css` | 新增 `.league-table` 变体 |
| `tests/structure.test.mjs` | 追加断言 |
| `模拟球员-单文件版.html` | 最后一步重建 |

---

## 任务依赖

```
T1 建表 → T2 推进一轮 → T3 接进两条比赛路径 → T4 渲染
                                              ↘ T5 冠军与保级接线
                                                          ↘ T6 存档兼容 + 回归 + 分发版
```

---

### Task 1: `s.league` 建表与排序

**Files:**
- Modify: `app.js`（在 `nextFixture` 之后插入新段）
- Modify: `app.js` 的 `API` 导出
- Test: `tests/structure.test.mjs`（追加末尾）

- [ ] **Step 1: 写失败的测试**

```js
// ===== 联赛积分榜：赛季初建表 =====
// 联赛冠军原本是掷骰子判的（wins/matches>=.7 之后再 rng()<.48），
// 两个赛季表现一模一样，一个拿冠军一个没拿，玩家无从理解。
{
  const mk=(club,month,route,called)=>{
    const t=G.createInitialState("榜",allocation,[],"standard","mid");
    t.totalMonth=month;t.club=club;t.route=route;
    if(month>=48)t.flags.pro18=true;
    if(called)t.national.called=true;
    G.ensureSchedule(t);return t;
  };
  const cases=[
    ["中超",   {name:"上海海港",league:"中超",strength:79}, 60,"pro",  16],
    ["英超",   {name:"Manchester United",league:"英超",strength:85}, 60,"pro",  20],
    ["中超梯队",{name:"重庆铜梁龙 U16",league:"中超梯队",strength:58}, 0,"academy",16],
    ["校园联赛",{name:"重庆市第七中学校队",league:"校园联赛",strength:55},24,"campus",11],
  ];
  cases.forEach(([label,club,month,route,expectTeams])=>{
    const t=mk(club,month,route,false);
    const lg=G.ensureLeague(t);
    assert.ok(lg&&Array.isArray(lg.teams),`${label} 应该建出积分榜`);
    assert.equal(lg.teams.length,expectTeams,
      `${label} 参赛队应为 ${expectTeams}，实际 ${lg.teams.length}`);
    assert.ok(lg.teams.some(x=>x.name===club.name),`${label} 榜上必须有玩家自己的球队`);
    assert.equal(new Set(lg.teams.map(x=>x.name)).size,lg.teams.length,`${label} 队名不能重复`);
    // 轮次必须与赛程里的 club 场次数一致
    const clubFx=t.schedule.fixtures.filter(f=>f.type==="club").length;
    assert.equal(lg.rounds,clubFx,`${label} 轮次(${lg.rounds})必须等于赛程里的联赛场次(${clubFx})`);
    // 新建的表是空的
    lg.teams.forEach(x=>assert.equal(x.p+x.w+x.d+x.l+x.gf+x.ga+x.pts,0,`${label} 新表所有数据应为0`));
  });
}
// 幂等：签名没变就不该重建
{
  const t=G.createInitialState("幂等",allocation,[],"standard","mid");
  t.totalMonth=60;t.flags.pro18=true;t.route="pro";
  t.club={name:"上海海港",league:"中超",strength:79};
  G.ensureSchedule(t);
  const a1=G.ensureLeague(t),a2=G.ensureLeague(t);
  assert.equal(a1,a2,"签名未变时必须返回同一个对象，不能每次重建（否则比分会被抹掉）");
}
// 排序：积分 → 净胜球 → 进球 → 队名
{
  const rows=[
    {name:"乙",p:3,w:2,d:0,l:1,gf:5,ga:4,pts:6},
    {name:"甲",p:3,w:2,d:0,l:1,gf:7,ga:6,pts:6},
    {name:"丙",p:3,w:3,d:0,l:0,gf:3,ga:0,pts:9},
    {name:"丁",p:3,w:2,d:0,l:1,gf:7,ga:6,pts:6},
  ];
  /* 甲和丁三项全同（6分、净胜+1、进7球），只能靠队名分先后。
     队名按升序（足球惯例的字母序）：localeCompare 下「丁」在「甲」前。 */
  const out=G.leagueStandings({teams:rows}).map(x=>x.name);
  assert.deepEqual([...out],["丙","丁","甲","乙"],
    `排序应为 积分→净胜球→进球→队名(升序)，实际 ${[...out].join(",")}`);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`TypeError: G.ensureLeague is not a function`

**必须真的看到红再往下。**

- [ ] **Step 3: 实现建表**

用 Edit 工具，在 `app.js` 的 `function nextFixture(...)` 那一行**之后**插入：

```js
/* ========== 联赛积分榜 ==========
   压缩赛制：全联盟就打玩家那么多轮（梯队3 / 校园5 / 英超梯队11 / 职业12），
   不模拟真实的 30/38 轮。按真实赛制个人贡献会被稀释到 1/3，
   会出现「我进了两球但排名没动」的脱节感。
   轮次直接取赛程里 type==="club" 的场次数，两边永远对齐。 */
function leagueSig(s){return{season:ageInfo(s).season,key:`${s.club.league}|${s.club.name}`}}
function leagueTeams(s){
  const me=currentClub(s);
  const rows=[{name:s.club.name,strength:me.strength}];
  opponentPool(s).forEach(o=>{if(!rows.some(r=>r.name===o.name))rows.push({name:o.name,strength:o.strength})});
  return rows.map(r=>({name:r.name,strength:r.strength,p:0,w:0,d:0,l:0,gf:0,ga:0,pts:0}));
}
function buildLeague(s){
  const sc=ensureSchedule(s);
  return {...leagueSig(s),rounds:sc.fixtures.filter(f=>f.type==="club").length,played:0,teams:leagueTeams(s)};
}
/* 与 ensureSchedule 同样的懒重建：签名没变就原样返回，
   否则每次渲染都会把已经打完的比分抹掉。 */
function ensureLeague(s){
  const sig=leagueSig(s),lg=s.league;
  if(lg&&lg.season===sig.season&&lg.key===sig.key)return lg;
  s.league=buildLeague(s);return s.league;
}
/* 足球惯例：积分 → 净胜球 → 进球 → 队名。 */
function leagueStandings(lg){
  return [...(lg&&lg.teams||[])].sort((a,b)=>
    b.pts-a.pts || (b.gf-b.ga)-(a.gf-a.ga) || b.gf-a.gf || a.name.localeCompare(b.name));
}
```

- [ ] **Step 4: 加进 `API` 导出**

用 Edit 工具，在 `API` 里紧跟 `ensureSchedule,buildSchedule,` 之后加：

```js
ensureLeague,buildLeague,leagueStandings,opponentPool,
```

（`opponentPool` 目前没被导出，测试和后续任务都要用。）

- [ ] **Step 5: 跑 30 次确认稳定**

```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30 个点。出现任何 `F` 都不算完成。

- [ ] **Step 6: 核对弯引号没被破坏**

```bash
grep -o "“" app.js | wc -l
```
Expected: `243`

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "联赛积分榜：赛季初建表

压缩赛制——全联盟就打玩家那么多轮（梯队3/校园5/英超梯队11/职业12），
不模拟真实的 30/38 轮：按真实赛制个人贡献会被稀释到 1/3，
玩家会遇到「进了两球但排名没动」的脱节感。

轮次直接取赛程里 type===\"club\" 的场次数，两边永远对齐。
ensureLeague 照 ensureSchedule 的懒重建写法，签名没变就原样返回，
否则每次渲染都会把已经打完的比分抹掉。
排序按足球惯例：积分→净胜球→进球→队名。"
```

---

### Task 2: 推进一轮（配对、种子、轮空）

**Files:**
- Modify: `app.js`（接在 Task 1 那段之后）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 每轮推进：全联盟必须内部自洽 =====
// 「逐队独立掷 W/D/L」的写法算术上会崩：全联盟总胜场不等于总负场。
// 玩家一加总就会发现这榜是假的。必须真实两两配对。
{
  const mk=(club,route,month)=>{
    const t=G.createInitialState("推进",allocation,[],"standard","mid");
    t.totalMonth=month;t.club=club;t.route=route;if(month>=48)t.flags.pro18=true;
    G.ensureSchedule(t);G.ensureLeague(t);return t;
  };
  [["中超",{name:"上海海港",league:"中超",strength:79},"pro",60],
   ["校园联赛",{name:"重庆市第七中学校队",league:"校园联赛",strength:55},"campus",24]]
  .forEach(([label,club,route,month])=>{
    const t=mk(club,route,month);
    const N=t.league.teams.length;
    for(let r=1;r<=3;r++)G.advanceLeagueRound(t,{opponent:null,result:null},r);
    const T=t.league.teams;
    const W=T.reduce((n,x)=>n+x.w,0), L=T.reduce((n,x)=>n+x.l,0);
    const D=T.reduce((n,x)=>n+x.d,0);
    const GF=T.reduce((n,x)=>n+x.gf,0), GA=T.reduce((n,x)=>n+x.ga,0);
    assert.equal(W,L,`${label} 全联盟总胜场(${W})必须等于总负场(${L})`);
    assert.equal(D%2,0,`${label} 总平局数(${D})必须是偶数`);
    assert.equal(GF,GA,`${label} 总进球(${GF})必须等于总失球(${GA})`);
    T.forEach(x=>assert.equal(x.p,x.w+x.d+x.l,`${label} ${x.name} 场次与胜平负对不上`));
    T.forEach(x=>assert.equal(x.pts,x.w*3+x.d,`${label} ${x.name} 积分算错`));
    // 奇数队每轮恰好一队轮空 → 3轮后应有 3 支队少打一场
    const maxP=Math.max.apply(null,T.map(x=>x.p));
    const behind=T.filter(x=>x.p<maxP).length;
    if(N%2===1)assert.equal(behind,3,`${label} 是${N}队（奇数），3轮后该有3支队各轮空一次，实际 ${behind}`);
    else assert.equal(behind,0,`${label} 是${N}队（偶数），不该有人轮空`);
  });
}
// 种子确定性：同一赛季同一轮，跑多少次结果都一样
{
  const mk=()=>{
    const t=G.createInitialState("种子",allocation,[],"standard","mid");
    t.totalMonth=60;t.flags.pro18=true;t.route="pro";
    t.club={name:"上海海港",league:"中超",strength:79};
    G.ensureSchedule(t);G.ensureLeague(t);return t;
  };
  const snap=t=>[...G.leagueStandings(t.league).map(x=>`${x.name}:${x.pts}:${x.gf}-${x.ga}`)].join("|");
  const a=mk(),b=mk();
  for(let r=1;r<=5;r++){G.advanceLeagueRound(a,{opponent:null,result:null},r);G.advanceLeagueRound(b,{opponent:null,result:null},r)}
  assert.equal(snap(a),snap(b),"同赛季同轮次必须产出完全相同的结果——否则读档或重渲染会让历史比分变样");
}
// 玩家的比分直接进榜，不重算
{
  const t=G.createInitialState("直接进榜",allocation,[],"standard","mid");
  t.totalMonth=60;t.flags.pro18=true;t.route="pro";
  t.club={name:"上海海港",league:"中超",strength:79};
  G.ensureSchedule(t);G.ensureLeague(t);
  const opp=t.league.teams.find(x=>x.name!==t.club.name).name;
  G.advanceLeagueRound(t,{opponent:opp,result:{gf:4,ga:1}},1);
  const me=t.league.teams.find(x=>x.name===t.club.name);
  const foe=t.league.teams.find(x=>x.name===opp);
  assert.equal(me.gf,4,"玩家进的球必须原样进榜，不能重新模拟");
  assert.equal(me.ga,1,"玩家丢的球同理");
  assert.equal(me.w,1,"4-1 是一场胜利");
  assert.equal(foe.gf,1,"对手那边必须是镜像：进1");
  assert.equal(foe.ga,4,"对手丢4");
  assert.equal(foe.l,1,"对手记一负");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`TypeError: G.advanceLeagueRound is not a function`

- [ ] **Step 3: 实现推进**

用 Edit 工具，接在 `leagueStandings` 之后插入：

```js
/* 按「赛季 + 轮次」派生的确定性种子。
   不这么做的话，读档、切 tab、重渲染都可能让已经打完的历史比分变样——
   玩家会看到自己上个月明明赢了的那场，回头再看变成输了。 */
function leagueRng(seasonNo,round){
  let x=(((seasonNo*73856093)^(round*19349663))>>>0)||1;
  return ()=>((x=(x*1664525+1013904223)>>>0)/4294967296);
}
/* 两支球队之间踢一场。只看实力差 + 随机，不带任何玩家个人加成——
   玩家那场的比分是外面直接传进来的，不走这里。 */
function simLeagueMatch(aStr,bStr,rng){
  const edge=aStr-bStr+rndFloat(rng,-8,8);
  return {gf:poisson(clamp(1.25+edge/18,.2,3.6),rng),ga:poisson(clamp(1.2-edge/22,.2,3.4),rng)};
}
function applyLeagueResult(row,gf,ga){
  if(!row)return;
  row.p++;row.gf+=gf;row.ga+=ga;
  if(gf>ga){row.w++;row.pts+=3}else if(gf===ga){row.d++;row.pts+=1}else row.l++;
}
/* 推进一轮。play.opponent / play.result 是玩家那场的真实结果：
   传了就原样计入（榜上那一行必须和赛程页、比赛简报是同一个结果），
   传 null 表示玩家没上场——但球队不弃权，照样模拟，只是不带个人加成。
   队伍数为奇数时每轮随机一队轮空，轮空不计场次。 */
function advanceLeagueRound(s,play,round){
  const lg=ensureLeague(s);if(!lg)return lg;
  const rng=leagueRng(lg.season,round);
  const byName=n=>lg.teams.find(t=>t.name===n);
  const done=new Set();
  if(play&&play.opponent&&play.result){
    const me=byName(s.club.name),foe=byName(play.opponent);
    if(me&&foe){
      applyLeagueResult(me,play.result.gf,play.result.ga);
      applyLeagueResult(foe,play.result.ga,play.result.gf);
      done.add(me.name);done.add(foe.name);
    }
  }
  const rest=shuffled(lg.teams.filter(t=>!done.has(t.name)),rng);
  if(rest.length%2===1)rest.pop();          // 奇数：末位轮空，不计场次
  for(let i=0;i<rest.length;i+=2){
    const A=rest[i],B=rest[i+1];
    const r=simLeagueMatch(A.strength,B.strength,rng);
    applyLeagueResult(A,r.gf,r.ga);applyLeagueResult(B,r.ga,r.gf);
  }
  lg.played=Math.max(lg.played,round);
  return lg;
}
```

- [ ] **Step 4: 导出**

用 Edit 工具，在 `API` 里 `ensureLeague,buildLeague,leagueStandings,opponentPool,` 之后加：

```js
advanceLeagueRound,leagueRng,simLeagueMatch,
```

- [ ] **Step 5: 跑 30 次**

```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30 个点。

- [ ] **Step 6: 弯引号核对**

```bash
grep -o "“" app.js | wc -l
```
Expected: `243`

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "联赛积分榜：每轮真实两两配对推进

不能逐队独立掷 W/D/L——那样全联盟总胜场不等于总负场、进球总数
也对不上，玩家一加总就发现这榜是假的。改成真实配对：每一分都
来自一场真实模拟的比赛。

玩家那场的比分外面直接传进来、原样计入，不重新模拟——榜上那一行
必须和赛程页、比赛简报是同一个结果，不能三处对不上。

种子按「赛季+轮次」派生：读档、切 tab、重渲染都不会让历史比分变样。
奇数队（校园11队）每轮随机一队轮空，轮空不计场次。"
```

---

### Task 3: 接进两条比赛路径

玩家可能上场（`resolveMatch`），也可能伤停/被雪藏而缺阵（`advanceMonth` 里标 `missed` 那处）。**两条路都要推进联赛**，否则伤停三个月的玩家会看到自己球队场次比别人少三场，榜看起来就是坏的。

**Files:**
- Modify: `app.js` 的 `resolveMatch`
- Modify: `app.js` 的 `advanceMonth`（标记 `missed` 那一段）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 两条路径都要推进联赛 =====
{
  // 路径一：玩家上场
  const t=G.createInitialState("上场",allocation,[],"standard","mid");
  ATTR_KEYS.forEach(k=>t.attrs[k]=80);
  t.totalMonth=59;t.flags.pro18=true;t.route="pro";
  t.club={name:"上海海港",league:"中超",strength:79};
  G.ensureSchedule(t);G.ensureLeague(t);
  G.setState(t);G.clearModalQueue();
  G.advanceMonth();
  await driveMatch(t,()=>0);
  const fx=t.schedule.fixtures.find(f=>f.month===60);
  const me=t.league.teams.find(x=>x.name===t.club.name);
  assert.ok(t.league.played>=1,"打完一场后联赛必须推进了一轮");
  if(fx&&fx.status==="played"&&fx.result){
    assert.equal(me.gf,fx.result.gf,"榜上的进球必须与赛程页那场完全一致");
    assert.equal(me.ga,fx.result.ga,"榜上的失球同理");
  }
}
{
  // 路径二：玩家伤停缺阵，球队照样要踢
  const t=G.createInitialState("伤停",allocation,[],"standard","mid");
  t.totalMonth=59;t.flags.pro18=true;t.route="pro";
  t.club={name:"上海海港",league:"中超",strength:79};
  G.ensureSchedule(t);G.ensureLeague(t);
  t.injury={name:"膝伤",months:3,risk:0};
  G.setState(t);G.clearModalQueue();
  G.advanceMonth();
  await driveMatch(t,()=>0);
  const fx=t.schedule.fixtures.find(f=>f.month===60);
  const me=t.league.teams.find(x=>x.name===t.club.name);
  assert.equal(fx.status,"missed","sanity: 伤停那场标 missed");
  assert.equal(me.p,1,
    "玩家缺阵，球队仍要踢这一轮——否则伤停三个月后他的球队场次会比别人少三场，榜就是坏的");
  // 全联盟仍然自洽
  const T=t.league.teams;
  assert.equal(T.reduce((n,x)=>n+x.w,0),T.reduce((n,x)=>n+x.l,0),"缺阵那轮也要保持胜负配平");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`打完一场后联赛必须推进了一轮`

- [ ] **Step 3: 先加轮次换算函数**

`advanceLeagueRound` 的第三个参数是「这是本季第几轮」。**这一步必须排在接线之前**——否则下面两步引用它时会 `ReferenceError`。用 Edit 工具，在 `advanceLeagueRound` 定义之前插入：

```js
/* 这场联赛是本季第几轮。按赛程里 type==="club" 的场次顺序数，
   跳过被大赛占用的月份——所以第几轮≠第几个月。 */
function clubRoundOf(s,month){
  const sc=s.schedule;if(!sc)return 1;
  const list=sc.fixtures.filter(f=>f.type==="club").sort((a,b)=>a.month-b.month);
  const i=list.findIndex(f=>f.month===month);
  return i<0?list.length+1:i+1;
}
```

并在 `API` 里加 `clubRoundOf,`。

- [ ] **Step 4: 接进 `resolveMatch`**

用 Edit 工具，在 `resolveMatch` 里找到这一段（把比分写回 fixture 的那块）：

```js
  if(pm.fixture){const fx=(s.schedule&&s.schedule.fixtures.find(f=>f.month===pm.fixture.month))||pm.fixture;
    fx.status="played";
    fx.result={gf:report.gf,ga:report.ga,goals:report.goals,assists:report.assists,rating:report.rating}}
```

在它**之后**插入：

```js
  /* 联赛只认 type==="club" 的场次：大赛月你不在俱乐部，那一轮不推进。
     玩家的真实比分直接传进去，不让联赛模块重算——榜、赛程页、比赛简报
     必须是同一个结果。 */
  if(pm.fixture&&pm.fixture.type==="club")
    advanceLeagueRound(s,{opponent:pm.fixture.opponent,result:{gf:report.gf,ga:report.ga}},clubRoundOf(s,pm.fixture.month));
```

- [ ] **Step 5: 接进 missed 分支**

用 Edit 工具，在 `advanceMonth` 里找到标记 missed 那一段：

```js
  {const _fx=fixtureOfMonth(S);
   if(_fx&&(S.retired||!shouldPlayMatch(S))){_fx.status="missed";
     _fx.missReason=(S.injury.months||0)>0?`伤停·${S.injury.name||"伤病"}`:(S.flags&&S.flags.washedOut)?"被雪藏":"未出战"}}
```

替换为：

```js
  {const _fx=fixtureOfMonth(S);
   if(_fx&&(S.retired||!shouldPlayMatch(S))){_fx.status="missed";
     _fx.missReason=(S.injury.months||0)>0?`伤停·${S.injury.name||"伤病"}`:(S.flags&&S.flags.washedOut)?"被雪藏":"未出战";
     /* 你没上场，但球队不弃权：这一轮照样模拟，只是不带你的个人加成。
        不这么做，伤停三个月的玩家会看到自己球队场次比别人少三场。 */
     if(_fx.type==="club")advanceLeagueRound(S,{opponent:null,result:null},clubRoundOf(S,_fx.month));}}
```

- [ ] **Step 6: 跑 30 次**

```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30 个点。**老断言必须全绿**——尤其跑 180 个月的「状态均衡点」和跑 120 个月的「赛程不堆积」。

- [ ] **Step 7: 弯引号核对 + 提交**

```bash
grep -o "“" app.js | wc -l   # 应为 243
git add app.js tests/structure.test.mjs
git commit -m "联赛积分榜接进两条比赛路径

玩家可能上场（resolveMatch），也可能伤停/被雪藏而缺阵（missed 分支）。
两条路都要推进联赛：只接前者的话，伤停三个月的玩家会看到自己球队
场次比别人少三场，榜看起来就是坏的。

缺阵时球队照样踢，只是不带玩家的个人加成。
只认 type===\"club\" 的场次——大赛月你不在俱乐部，那一轮不推进；
轮次由 clubRoundOf 按赛程里 club 场次的顺序数，第几轮≠第几个月。"
```

---

### Task 4: 渲染在「赛程」页顶部

**Files:**
- Modify: `app.js` 的 `renderMatches`
- Modify: `style.css`
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 积分榜渲染 =====
{
  assert.ok(/积分榜|本赛季 · /.test(code),"赛程页顶部要有积分榜");
  assert.ok(/\.league-table/.test(css),"积分榜要有样式");
  assert.equal(typeof G.leagueTableHTML,"function","渲染要抽成可测的函数");
  const t=G.createInitialState("渲染",allocation,[],"standard","mid");
  t.totalMonth=60;t.flags.pro18=true;t.route="pro";
  t.club={name:"上海海港",league:"中超",strength:79};
  G.ensureSchedule(t);G.ensureLeague(t);
  G.advanceLeagueRound(t,{opponent:null,result:null},1);
  const html=G.leagueTableHTML(t);
  assert.match(html,/league-table/,"要用 .league-table 类");
  assert.match(html,/上海海港/,"玩家球队要在榜上");
  assert.match(html,/class="me"/,"玩家那一行要高亮");
  assert.match(html,/第 1 轮/,"要标出当前轮次");
  assert.match(html,/共 \d+ 轮/,"要标出总轮数——梯队一季只有3轮，玩家得知道样本就这么小");
  // 榜上的行数 = 参赛队数
  const rows=(html.match(/<tr/g)||[]).length-1;   // 减掉表头
  assert.equal(rows,t.league.teams.length,`榜上应有 ${t.league.teams.length} 行，实际 ${rows}`);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`赛程页顶部要有积分榜`

- [ ] **Step 3: 加样式**

追加到 `style.css` 末尾：

```css
/* 联赛积分榜：复用 .rank-table 的骨架，只调列宽与强调色 */
.league-table td:nth-child(2){font-weight:800}
.league-table td:last-child{color:var(--gold);font-weight:900}
.league-table tr.me td{color:var(--green);background:#28d27d0d}
.league-table .lt-you{font-size:8px;color:var(--green);margin-left:4px}
@media (max-width:760px){
  .league-table th:nth-child(4),.league-table td:nth-child(4),
  .league-table th:nth-child(5),.league-table td:nth-child(5){display:none}
}
```

- [ ] **Step 4: 实现渲染函数**

用 Edit 工具，在 `fixtureRow` 定义之前插入：

```js
/* 梯队一季只有3轮、校园5轮，名次噪声很大。标题里标出「第N轮/共M轮」，
   让玩家知道样本就这么小，而不是以为自己稳居第4。 */
function leagueTableHTML(s){
  const lg=ensureLeague(s);
  if(!lg||!lg.teams.length)return"";
  const rows=leagueStandings(lg).map((x,i)=>{
    const me=x.name===s.club.name,gd=x.gf-x.ga;
    return `<tr class="${me?"me":""}"><td>${i+1}</td>`+
      `<td>${esc(x.name)}${me?'<span class="lt-you">你</span>':""}</td>`+
      `<td>${x.p}</td><td>${x.w}/${x.d}/${x.l}</td>`+
      `<td>${gd>0?"+":""}${gd}</td><td>${x.pts}</td></tr>`;
  }).join("");
  return `<div class="section-head"><h2>本赛季 · ${esc(s.club.league)}</h2>`+
    `<span>第 ${Math.max(1,lg.played)} 轮 / 共 ${lg.rounds} 轮</span></div>`+
    `<table class="rank-table league-table"><thead><tr>`+
    `<th>#</th><th>球队</th><th>场</th><th>胜/平/负</th><th>净胜</th><th>积分</th>`+
    `</tr></thead><tbody>${rows}</tbody></table>`;
}
```

- [ ] **Step 5: 插进 `renderMatches`**

用 Edit 工具，在 `renderMatches` 里找到 `$("panel").innerHTML=` 后面紧跟的那个 hero-panel 模板字符串开头。把 hero-panel 那一整块之后、`<div class="section-head"><h2>本赛季日程</h2>` 之前，插入 `${leagueTableHTML(S)}`。

也就是把：

```js
    `<div class="section-head"><h2>本赛季日程</h2><span>第${ageInfo(S).season}赛季 · ${fx.length}场</span></div>`+
```

改成：

```js
    leagueTableHTML(S)+
    `<div class="section-head"><h2>本赛季日程</h2><span>第${ageInfo(S).season}赛季 · ${fx.length}场</span></div>`+
```

- [ ] **Step 6: 导出**

`API` 里加 `leagueTableHTML,`。

- [ ] **Step 7: 跑 30 次 + 弯引号 + 提交**

```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
grep -o "“" app.js | wc -l   # 应为 243
git add app.js style.css tests/structure.test.mjs
git commit -m "积分榜渲染在「赛程」页顶部

复用排行页已有的 .rank-table 骨架，玩家那行套 tr.me 高亮。
移动端隐掉「胜平负」和「净胜」两列，只留 名次/球队/场/积分。

标题标出「第N轮 / 共M轮」：梯队一季只有3轮、校园5轮，名次噪声很大，
玩家得知道样本就这么小，而不是以为自己稳居第4。"
```

---

### Task 5: 冠军判定与保级目标接线

**Files:**
- Modify: `app.js` 的 `seasonAwardCheck`
- Modify: `app.js` 的 `evaluateSeasonGoal` 与 `makeSeasonGoal`
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 联赛冠军：排第一就是冠军，不再掷骰子 =====
{
  assert.ok(!/leagueTitle=ss\.matches>=7/.test(code),
    "旧的掷骰子判定要废掉：wins/matches>=.7 之后再 rng()<.48，两个赛季表现一样却结果不同");
  const mk=first=>{
    const t=G.createInitialState("冠军",allocation,[],"standard","mid");
    t.totalMonth=71;t.flags.pro18=true;t.route="pro";
    t.club={name:"上海海港",league:"中超",strength:79};
    G.ensureSchedule(t);G.ensureLeague(t);
    // 手动把榜做成「玩家第一」或「玩家垫底」
    t.league.teams.forEach(x=>{x.p=10;x.w=2;x.d=1;x.l=7;x.gf=8;x.ga=20;x.pts=7});
    const me=t.league.teams.find(x=>x.name===t.club.name);
    if(first){me.w=9;me.d=1;me.l=0;me.gf=30;me.ga=5;me.pts=28}
    t.seasonStats={matches:10,goals:12,assists:5,wins:first?9:2,ratingTotal:75,trophies:0};
    return t;
  };
  const champ=G.seasonAwardCheck(mk(true),()=>0.99);
  assert.equal(champ.leagueTitle,true,"榜首就该拿冠军，且不受随机数影响（这里 rng 固定给 0.99）");
  const nope=G.seasonAwardCheck(mk(false),()=>0.01);
  assert.equal(nope.leagueTitle,false,"不是榜首就没有冠军，同样不受随机数影响");
}
// ===== 保级目标接真实榜尾 =====
{
  assert.ok(!/kind==="survive"\)met=ss\.wins>=g\.target/.test(code),
    "保级目标不该再只看胜场数——它的文案写着「别掉进降级区」");
  const mk=rankFromBottom=>{
    const t=G.createInitialState("保级",allocation,[],"standard","mid");
    t.totalMonth=71;t.flags.pro18=true;t.route="pro";
    t.club={name:"重庆铜梁龙",league:"中超",strength:67};
    G.ensureSchedule(t);G.ensureLeague(t);
    t.league.teams.forEach((x,i)=>{x.p=10;x.pts=60-i*3;x.gf=20;x.ga=10});
    const sorted=G.leagueStandings(t.league);
    const me=t.league.teams.find(x=>x.name===t.club.name);
    const target=sorted[sorted.length-rankFromBottom];
    me.pts=target.pts-1;   // 挪到目标名次附近
    t.league.teams.filter(x=>x!==me).forEach((x,i)=>{x.pts=100-i*3});
    if(rankFromBottom<=3){me.pts=0}else{me.pts=999}
    t.seasonGoal={kind:"survive",target:6,text:"保级",season:G.ageInfo(t).season};
    t.seasonStats={matches:10,goals:2,assists:1,wins:8,ratingTotal:65,trophies:0};
    return t;
  };
  const doomed=G.evaluateSeasonGoal(mk(1));
  assert.equal(doomed.met,false,"掉进榜尾三名 = 保级失败，哪怕赢了8场");
  const safe=G.evaluateSeasonGoal(mk(9));
  assert.equal(safe.met,true,"榜上安全就是完成保级");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`旧的掷骰子判定要废掉`

- [ ] **Step 3: 换掉冠军判定**

用 Edit 工具，在 `seasonAwardCheck` 里把：

```js
leagueTitle=ss.matches>=7&&ss.wins/ss.matches>=.7&&overall(s)>=currentClub(s).strength-2&&rng()<.48-diffOf(s).threshold*.02;
```

替换为：

```js
leagueTitle=leagueChampion(s);
```

并在 `seasonAwardCheck` 定义之前插入：

```js
/* 排第一就是冠军。旧写法是满足胜率与实力门槛后再掷一次 48% 的骰子——
   两个赛季表现一模一样、一个拿冠军一个没拿，玩家无从理解。
   现在你把重庆铜梁龙（实力67）带到榜首，那是真的打出来的。 */
function leagueChampion(s){
  const lg=s.league;if(!lg||!lg.teams.length||!lg.played)return false;
  return leagueStandings(lg)[0].name===s.club.name;
}
```

- [ ] **Step 4: 保级目标接真实榜尾**

用 Edit 工具，在 `evaluateSeasonGoal` 里把：

```js
else if(g.kind==="survive")met=ss.wins>=g.target;
```

替换为：

```js
else if(g.kind==="survive")met=!inRelegationZone(s);
```

并在 `evaluateSeasonGoal` 定义之前插入：

```js
/* 榜尾三名算降级区。这条目标的文案一直写着「别掉进降级区」，
   但判定只看 wins>=6，跟排名毫无关系——现在它第一次名副其实。
   注意：这里不引入真正的升降级机制（那会牵动转会、合同、声望一整串），
   只是让目标判定对得上它自己的文案。 */
function inRelegationZone(s){
  const lg=s.league;if(!lg||!lg.teams.length||!lg.played)return false;
  const st=leagueStandings(lg);
  return st.slice(-3).some(x=>x.name===s.club.name);
}
```

- [ ] **Step 5: 保级目标的文案跟上**

用 Edit 工具，在 `makeSeasonGoal` 里把：

```js
goal={kind:"survive",target:6,text:`帮${s.club.name}至少赢下6场，别掉进降级区`};
```

替换为：

```js
goal={kind:"survive",target:3,text:`帮${s.club.name}守住联赛排名，赛季末别落进榜尾三名`};
```

- [ ] **Step 6: 导出**

`API` 里加 `leagueChampion,inRelegationZone,`。

- [ ] **Step 7: 跑 30 次 + 弯引号 + 提交**

```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
grep -o "“" app.js | wc -l   # 应为 243
git add app.js tests/structure.test.mjs
git commit -m "联赛冠军从掷骰子改成榜首判定，保级目标接真实榜尾

旧判定：胜率>=70% 且 综合>=队伍实力-2 之后，再掷一次 48% 的骰子。
两个赛季表现一模一样，一个拿冠军一个没拿，玩家无从理解。
现在你把重庆铜梁龙（实力67）带到榜首，那是真的打出来的。

保级目标的文案一直写着「别掉进降级区」，判定却只看 wins>=6，
跟排名毫无关系。改成看真实榜尾三名，文案同步改。
不引入真正的升降级机制——那会牵动转会、合同、声望一整串。"
```

---

### Task 6: 存档兼容、长跑回归、分发版

**Files:**
- Modify: `app.js` 的 `normalizeSave`
- Modify: `README.md`
- Regenerate: `模拟球员-单文件版.html`
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败的测试**

```js
// ===== 存档兼容 + 长跑不留脏数据 =====
{
  const old={version:3,club:{name:"上海海港",league:"中超",strength:79},
    national:{},attrs:{},styles:{},relationship:{},injury:{},risks:{}};
  const mig=G.normalizeSave(old);
  assert.equal(mig.league,null,"老档补 league:null，下次自动重建");
}
{
  const drive=async t=>{const q=G.getModalQueue();let n=0;
    while(n++<600){if(!q.length){await new Promise(r=>setTimeout(r,0));if(!q.length)break}
      const m=q.shift();const o=typeof m.options==="function"?m.options(t):m.options;
      if(o&&o[0]&&o[0].apply)try{o[0].apply()}catch(e){}}};
  const t=G.createInitialState("长跑",allocation,[],"standard","mid");
  G.setState(t);G.clearModalQueue();
  for(let m=0;m<120&&!t.retired;m++){G.setState(t);G.advanceMonth();await drive(t)}
  const lg=t.league;
  assert.ok(lg,"跑完还该有积分榜");
  assert.equal(lg.season,G.ageInfo(t).season,"积分榜必须是本赛季的，不能残留上赛季");
  assert.ok(lg.played<=lg.rounds,`已打轮次(${lg.played})不该超过总轮数(${lg.rounds})`);
  const T=lg.teams;
  assert.equal(T.reduce((n,x)=>n+x.w,0),T.reduce((n,x)=>n+x.l,0),"长跑后仍要胜负配平");
  assert.equal(T.reduce((n,x)=>n+x.gf,0),T.reduce((n,x)=>n+x.ga,0),"长跑后仍要进失球配平");
  T.forEach(x=>assert.ok(x.p<=lg.rounds,`${x.name} 场次 ${x.p} 超过了总轮数 ${lg.rounds}`));
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，`老档补 league:null，下次自动重建`

- [ ] **Step 3: `normalizeSave` 补字段**

用 Edit 工具，在 `normalizeSave` 里 `return d;` 之前插入：

```js
  if(d.league===undefined)d.league=null;
```

- [ ] **Step 4: 跑 30 次**

```bash
node --check app.js && for i in $(seq 1 30); do node tests/structure.test.mjs >/dev/null 2>&1 && printf "." || printf "F"; done; echo
```
Expected: 30 个点。

- [ ] **Step 5: 长跑回归（10局完整生涯）**

```bash
cat > /tmp/league_regression.cjs <<'EOF'
const fs=require("fs"),vm=require("vm");
(async()=>{
let g=1;const lcg=()=>((g=(g*1664525+1013904223)>>>0)/4294967296);
const M=Object.create(Math);M.random=lcg;
const s={console,Date,Math:M,setTimeout,clearTimeout,window:{},globalThis:{}};
vm.createContext(s);vm.runInContext(fs.readFileSync("app.js","utf8"),s);
const G=s.window.PlayerLife;
const a={PAC:4,SHO:4,PAS:3,DRI:3,DEF:3,PHY:3,WIL:4};
const drive=async t=>{const q=G.getModalQueue();let n=0;
 while(n++<600){if(!q.length){await new Promise(r=>setTimeout(r,0));if(!q.length)break}
  const m=q.shift();const o=typeof m.options==="function"?m.options(t):m.options;
  if(o&&o.length){const c=o[Math.floor(lcg()*o.length)]||o[0];if(c&&c.apply)try{c.apply()}catch(e){}}}};
let crash=0,bad=0,titles=0;
for(let seed=1;seed<=10;seed++){
  g=seed*70001;
  const t=G.createInitialState("g"+seed,a,G.TALENTS.slice(seed%18,seed%18+3).map(x=>x.id),"standard","mid");
  G.setState(t);G.clearModalQueue();
  try{for(let m=0;m<260&&!t.retired;m++){
    const av=G.ACTIONS.filter(x=>x.phases.includes(G.phaseOf(t)));
    for(let k=0;k<3&&av.length;k++){try{av[Math.floor(lcg()*av.length)].run(t)}catch(e){}}
    G.setState(t);G.advanceMonth();await drive(t);
    const lg=t.league;
    if(lg){
      const T=lg.teams;
      if(T.reduce((n,x)=>n+x.w,0)!==T.reduce((n,x)=>n+x.l,0))bad++;
      if(T.reduce((n,x)=>n+x.gf,0)!==T.reduce((n,x)=>n+x.ga,0))bad++;
      if(T.some(x=>x.p>lg.rounds))bad++;
      if(lg.season!==G.ageInfo(t).season)bad++;
    }
  }}catch(e){crash++;console.log(" 崩:",e.message.slice(0,80))}
  titles+=(t.honours||[]).filter(h=>/联赛冠军|中超冠军|英超冠军/.test(h.title)).length;
}
console.log(`崩溃 ${crash} | 榜不自洽 ${bad} | 10局共夺得联赛冠军 ${titles} 次`);
console.log(crash===0&&bad===0?"✓ 通过":"✗ 有问题");
})();
EOF
node /tmp/league_regression.cjs
```
Expected: `崩溃 0 | 榜不自洽 0 | ...` 且 `✓ 通过`

夺冠次数只作观察，不设硬门槛——废掉骰子后弱队夺冠本来就该变难。但如果 10 局一次都没夺冠，说明门槛可能过严，把数字报出来讨论。

- [ ] **Step 6: 更新 README**

在 `README.md` 的「已实现」里，找到这一行：

```
- 金球奖、联赛冠军、奖杯陈列、21项成就、本地生涯排行
```

替换为：

```
- 联赛积分榜：梯队/校园/中超/英超各有一张，你的每一场都直接算进排名；赛季末排第一就是冠军（不再掷骰子），榜尾三名算保级失败
- 金球奖、联赛冠军、奖杯陈列、21项成就、本地生涯排行
```

- [ ] **Step 7: 重建单文件版并提交**

```bash
python3 build_single.py
node --check app.js && node tests/structure.test.mjs
git add app.js README.md tests/structure.test.mjs 模拟球员-单文件版.html
git commit -m "积分榜存档兼容与回归

不升 VERSION（升了会清档），normalizeSave 补 league:null 自动重建。
补长跑断言：跑120个月后榜仍要胜负配平、进失球配平、场次不超总轮数、
不残留上赛季数据。
README 补上积分榜与新的冠军/保级规则。"
```

---

## 自查

**Spec 覆盖：**

| Spec 要求 | 任务 |
|---|---|
| `s.league` 结构与建表 | T1 |
| 压缩赛制，轮次取 `type==="club"` 场次数 | T1 |
| 懒重建（签名未变返回同一对象） | T1 |
| 排序：积分→净胜球→进球→队名 | T1 |
| 每轮真实两两配对 | T2 |
| 玩家比分直接进榜、不重算 | T2 + T3 |
| 「赛季+轮次」派生种子 | T2 |
| 奇数队轮空、不计场次 | T2 |
| 玩家缺阵时球队照样踢、不带个人加成 | T3 |
| 只有 `type==="club"` 算一轮 | T3 |
| 渲染在「赛程」页顶部、复用 `.rank-table` | T4 |
| 移动端隐列 | T4 |
| 标注「第N轮/共M轮」 | T4 |
| 冠军 = 榜首，废掉骰子 | T5 |
| 保级目标接真实榜尾三名 | T5 |
| 存档兼容 `league:null` | T6 |
| 长跑回归 | T6 |
| 重建单文件版 | T6 |

**跨任务名字一致性：**
- `ensureLeague` / `leagueStandings` / `buildLeague`（T1 定义 → T2/T3/T4/T5 使用）✓
- `advanceLeagueRound` / `leagueRng` / `simLeagueMatch` / `applyLeagueResult`（T2 定义 → T3 使用）✓
- `clubRoundOf`（T3 Step 3 定义 → T3 Step 4/5 使用）✓ 已把定义步骤提到接线之前，执行顺序不会撞 ReferenceError
- `leagueTableHTML`（T4 定义 → T4 内部使用）✓
- `leagueChampion` / `inRelegationZone`（T5 定义 → T5 内部使用）✓
- `opponentPool`（T1 Step 4 导出 → T1 的 `leagueTeams` 使用；它本来就在 `app.js` 里，导出只为测试）✓

**占位符扫描：** 全文无 TBD/TODO；每个改代码的步骤都带完整代码块；唯一没有硬门槛的是 T6 Step 5 的「夺冠次数」，那是有意作为观察量而非断言，理由已写明。
